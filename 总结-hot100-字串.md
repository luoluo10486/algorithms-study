# 总结 — Hot100（4）· 字串（子串 / 字符串操作）

> 覆盖 LeetCode 560 / 239 / 76 / 344 / 541 / 151。源文件：`day20-子串hot100.md`（560、239、76）、`day8-哈希表hot100+字符串代码随想录-part1.md`（344、541）、`day9-字符串代码随想录-part2.md`（151）。
>
> 说明："字串"在 Hot100 里实际分两类——**子串/子数组问题**（连续段，常用前缀和/滑动窗口）与**字符串操作**（反转、单词翻转）。本篇各归其位。

---

## 一、解题模型总述

1. **子数组和 → 前缀和 + HashMap**（560）：`s[i+1]-s[j]==k` 即 `s[j]==s[i+1]-k`，边遍历边查计数。
2. **滑动窗口极值/覆盖**（239、76）：见《总结-hot100-滑动窗口》，本篇不复述，仅标注归属。
3. **字符串反转类**（344、541、151）：核心是**双指针相向交换**；多段反转靠"整体反转 + 局部反转"两步抵消。

> 一句话：**子串看连续段用前缀和/窗口，字符串看反转用双指针。**

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 归类 | 面试最优解 | 时间 | 空间 |
|------|------|------|-----------|------|------|
| 560 | 和为 K 的子数组 | 子串（前缀和） | 前缀和 + HashMap | O(n) | O(n) |
| 239 | 滑动窗口最大值 | 子串（滑动窗口） | 单调队列 | O(n) | O(k) |
| 76 | 最小覆盖子串 | 子串（滑动窗口） | 滑动窗口 + 计数数组 | O(m·Σ) | O(1) |
| 344 | 反转字符串 | 字符串操作 | 双指针相向交换 | O(n) | O(1) |
| 541 | 反转字符串 II | 字符串操作 | 分段反转 | O(n) | O(n) |
| 151 | 反转字符串中的单词 | 字符串操作 | 三步反转 | O(n) | O(n) |

---

## 三、各题解法详情

### 1. 和为 K 的子数组（LeetCode 560 | 🟡 中等）

**简单思路**：以当前前缀和为终点，查有多少个更早前缀和等于 `cur-k`。HashMap 记各前缀和出现次数，先累加 `cnt[cur-k]` 再 `cnt[cur]++`。

**对应解法**（取自 day20）：

```java
class Solution {
    public int subarraySum(int[] nums, int k) {
        int n = nums.length;
        int[] s = new int[n + 1];
        for (int i = 0; i < n; i++) s[i + 1] = s[i] + nums[i];
        Map<Integer, Integer> cnt = new HashMap<>();
        int ans = 0;
        for (int sc : s) {
            ans += cnt.getOrDefault(sc - k, 0);
            cnt.put(sc, cnt.getOrDefault(sc, 0) + 1);
        }
        return ans;
    }
}
```

- ✅ 面试最优解：前缀和 + HashMap O(n)/O(n)。同思路搬到二叉树就是 437 路径总和 III。
- 时间 O(n) / 空间 O(n)。

### 2. 滑动窗口最大值（LeetCode 239 | 🔴 困难）

**简单思路 / 解法**：单调队列，详见《总结-hot100-滑动窗口》第 2 题。本篇仅标注归属。

- ✅ 面试最优解：单调队列 O(n)/O(k)。

### 3. 最小覆盖子串（LeetCode 76 | 🔴 困难）

**简单思路 / 解法**：滑动窗口 + 计数数组，详见《总结-hot100-滑动窗口》第 3 题。本篇仅标注归属。

- ✅ 面试最优解：滑动窗口 + 计数数组 O(m·Σ)/O(1)。

### 4. 反转字符串（LeetCode 344 | 🟢 简单）

**简单思路**：一头一尾双指针相向而行，逐个交换；`left < right` 保证中间元素不自我交换。

**对应解法**（取自 day8）：

```java
class Solution {
    public void reverseString(char[] s) {
        int n = s.length;
        for (int left = 0, right = n - 1; left < right; left++, right--) {
            char temp = s[left];
            s[left] = s[right];
            s[right] = temp;
        }
    }
}
```

- ✅ 面试最优解：双指针交换 O(n)/O(1)。同源思路用于链表反转（206）。
- 时间 O(n) / 空间 O(1)。

### 5. 反转字符串 II（LeetCode 541 | 🟢 简单）

**简单思路**：每 `2k` 个字符为一组，反转组内前 `k` 个；最后一段不足 `k` 个用 `Math.min(i+k, n)-1` 兜底。

**对应解法**（取自 day8）：

```java
class Solution {
    public String reverseStr(String s, int k) {
        char[] c = s.toCharArray();
        int n = c.length;
        for (int i = 0; i < n; i += 2 * k) {
            reverse(c, i, Math.min(i + k, n) - 1);
        }
        return new String(c);
    }
    private void reverse(char[] c, int left, int right) {
        while (left < right) {
            char temp = c[left];
            c[left++] = c[right];
            c[right--] = temp;
        }
    }
}
```

- ✅ 面试最优解：分段反转 O(n)/O(n)。
- 时间 O(n) / 空间 O(n)。

### 6. 反转字符串中的单词（LeetCode 151 | 🟡 中等）

**简单思路**：三步走——去多余空格 → 整体反转 → 单词内逐个反转（两步反转抵消，只剩单词顺序颠倒）。

**对应解法**（取自 day9）：

```java
class Solution {
    public String reverseWords(String s) {
        StringBuilder sb = removeSpace(s);
        reverseString(sb, 0, sb.length() - 1);
        reversePerWords(sb);
        return sb.toString();
    }
    private StringBuilder removeSpace(String s) {
        int start = 0, end = s.length() - 1;
        while (start <= end && s.charAt(start) == ' ') start++;
        while (start <= end && s.charAt(end) == ' ') end--;
        StringBuilder sb = new StringBuilder();
        while (start <= end) {
            char c = s.charAt(start);
            if (c != ' ') sb.append(c);
            else if (sb.length() > 0 && sb.charAt(sb.length() - 1) != ' ') sb.append(c);
            start++;
        }
        return sb;
    }
    private void reverseString(StringBuilder sb, int start, int end) {
        while (start < end) {
            char t = sb.charAt(start);
            sb.setCharAt(start, sb.charAt(end));
            sb.setCharAt(end, t);
            start++; end--;
        }
    }
    private void reversePerWords(StringBuilder sb) {
        int start = 0, end = 1, n = sb.length();
        while (start < n) {
            while (end < n && sb.charAt(end) != ' ') end++;
            reverseString(sb, start, end - 1);
            start = end + 1; end = start + 1;
        }
    }
}
```

- ✅ 面试最优解：三步反转 O(n)/O(n)。比 `split` 逆序更显基本功。
- 时间 O(n) / 空间 O(n)。

---

*整理自 `day20-子串hot100.md`、`day8-哈希表hot100+字符串代码随想录-part1.md`、`day9-字符串代码随想录-part2.md` · 汇总日期：2026-08-17*
