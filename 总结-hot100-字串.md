# 总结 — Hot100 · 子串

> 覆盖 LeetCode **560 / 239 / 76**（Hot100「子串」板块全部三题）。源文件：`day20-子串hot100.md`。
> 本板块都是**连续子段**问题，两条主线：**前缀和 + HashMap**（560 求和类）与**滑动窗口**（239 求极值 / 76 求覆盖）。

---

## 一、解题模型总述

「子串 / 子数组」问题的对象是**连续子段**，两类套路：

1. **前缀和 + HashMap**（560）：子数组和 = 两前缀和之差。`s[i+1] - s[j] = k` 即 `s[j] = s[i+1] - k`，边遍历边用 HashMap 统计历史前缀和出现次数。
2. **滑动窗口 · 单调队列**（239）：窗口最大值用**单调递减双端队列**，队首恒为窗口最大值；入队清掉 ≤ 当前元素的队尾，出队看队首下标是否越过左界。
3. **滑动窗口 · 计数覆盖**（76）：右指针扩到窗口**涵盖** t，一旦涵盖就左指针收缩求**最短**；用两个计数数组配合 `isCovered` 判定覆盖。

> 一句话：**求和看前缀和差，求极值看单调队列，求覆盖看右扩左缩。**

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 面试最优解 | 时间 | 空间 |
|------|------|-----------|------|------|
| 560 | 和为 K 的子数组 | 前缀和 + HashMap | O(n) | O(n) |
| 239 | 滑动窗口最大值 | 单调队列（双端队列） | O(n) | O(k) |
| 76 | 最小覆盖子串 | 滑动窗口 + 计数数组 | O(m·Σ) | O(1) |

---

## 三、各题解法详情

### 1. 和为 K 的子数组（LeetCode 560 | 🟡 中等）

**简单思路**：以当前前缀和 `sc` 为终点，查有多少个更早的前缀和等于 `sc - k`。HashMap 记各前缀和出现次数，先累加 `cnt[sc - k]` 再 `cnt[sc]++`（遍历 `s[0]=0` 时自动初始化 `cnt[0]=1`，使单元素子数组也能被统计）。

**对应解法**（取自 day20）：

```java
class Solution {
    public int subarraySum(int[] nums, int k) {
        int n = nums.length;
        int[] s = new int[n + 1];          // 前缀和数组，s[0]=0
        for (int i = 0; i < n; i++) s[i + 1] = s[i] + nums[i];

        Map<Integer, Integer> cnt = new HashMap<>();
        int ans = 0;
        for (int sc : s) {                 // sc = s[i+1]
            ans += cnt.getOrDefault(sc - k, 0);   // 有多少起点使子数组和为 k
            cnt.put(sc, cnt.getOrDefault(sc, 0) + 1);
        }
        return ans;
    }
}
```

- ✅ 面试最优解：前缀和 + HashMap O(n)/O(n)。同思路搬到二叉树即为 437 路径总和 III。
- 时间 O(n) / 空间 O(n)。

### 2. 滑动窗口最大值（LeetCode 239 | 🔴 困难）

**简单思路**：单调递减队列存**下标**。入队时清掉所有值 ≤ 当前元素的队尾（它们不可能再当最大）；队首下标超出窗口左界则出队；窗口成型后队首即最大值。

**对应解法**（取自 day20）：

```java
class Solution {
    public int[] maxSlidingWindow(int[] nums, int k) {
        int n = nums.length;
        int[] ans = new int[n - k + 1];
        Deque<Integer> q = new ArrayDeque<>();  // 存下标，单调递减
        for (int i = 0; i < n; i++) {
            while (!q.isEmpty() && nums[q.getLast()] < nums[i]) q.removeLast();
            q.addLast(i);
            int left = i - k + 1;
            if (q.getFirst() < left) q.removeFirst();
            if (left >= 0) ans[left] = nums[q.getFirst()];
        }
        return ans;
    }
}
```

- ✅ 面试最优解：单调队列 O(n)/O(k)。比「每个窗口扫一遍」O(n·k) 更优，是面试必考。
- 关键：必须存**下标**，否则无法判断元素是否已滑出窗口。
- 时间 O(n) / 空间 O(k)。

### 3. 最小覆盖子串（LeetCode 76 | 🔴 困难）

**简单思路**：`cntT` 记目标字符需求，`cntS` 记窗口内字符。`right` 纳入、`isCovered` 为真就收缩 `left` 求最短覆盖。

**对应解法**（取自 day20）：

```java
class Solution {
    public String minWindow(String s, String t) {
        int[] cntS = new int[128], cntT = new int[128];
        for (char c : t.toCharArray()) cntT[c]++;
        char[] S = s.toCharArray();
        int m = S.length, ansLeft = -1, ansRight = m, left = 0;
        for (int right = 0; right < m; right++) {
            cntS[S[right]]++;                       // 右端点字母进入窗口
            while (isCovered(cntS, cntT)) {         // 窗口已涵盖 t
                if (right - left < ansRight - ansLeft) {
                    ansLeft = left; ansRight = right;
                }
                cntS[S[left]]--;                    // 左端点字母移出窗口
                left++;
            }
        }
        return ansLeft < 0 ? "" : s.substring(ansLeft, ansRight + 1);
    }
    private boolean isCovered(int[] cntS, int[] cntT) {
        for (int i = 'A'; i <= 'Z'; i++) if (cntS[i] < cntT[i]) return false;
        for (int i = 'a'; i <= 'z'; i++) if (cntS[i] < cntT[i]) return false;
        return true;
    }
}
```

- ✅ 面试最优解：滑动窗口 + 计数数组 O(m·Σ)/O(1)。`int[128]` 比 HashMap 快；字符数组 `S` 比 `charAt` 快。
- 时间 O(m·Σ)（Σ=52 大小写）/ 空间 O(1)。

---

## 四、模型缺口与提醒

- 本板块 Hot100 仅 **560、239、76** 三题，均已收录，**无缺口**。
- **命名澄清**：Hot100 官方此板块名为「**子串**」（本文件沿用既有文件名「字串」）。它**不含**字符串反转类题目（344 反转字符串、541 反转字符串 II、151 翻转单词）——那些是「代码随想录 · 字符串」内容，已从本篇移除。
- **与「滑动窗口」板块的边界**：3、438 属 Hot100「滑动窗口」板块（见《总结-hot100-滑动窗口》）；但 239、76 本质上也是滑动窗口题，只是按官方归属列入「子串」。复习时建议三者连起来看——3/438/76 同属「右进左缩 + 计数」家族。
- 同族可延伸（源文档未收录）：**974 和可被 K 整除的子数组 / 523 连续的子数组和**（前缀和 + 同余）、**713 乘积小于 K 的子数组 / 1658**（变长窗口）。

---

*整理自 `day20-子串hot100.md` · 汇总日期：2026-09-16（按 Hot100 官方分组重排）*
