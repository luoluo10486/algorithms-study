# 总结 — 贪心算法 Hot100

> 覆盖 LeetCode 121 / 55 / 45 / 763（Hot100），解法代码取自 `day27-贪心算法hot100.md`，只保留「简单思路 + 解法代码 + 面试最优解标注 + 复杂度」。

---

## 一、解题模型总述

贪心类 Hot100 的共性：**每一步做局部最优选择，不回退，累积成全局最优**。识别特征是「求最大利润 / 能否到达 / 最少步数 / 最多不重叠片段」。横向规律四条口诀：

1. **维护历史极值 + 当天决策**（121）：最大利润 = 「今天卖价 − 之前最低买价」取最大。只扫一遍，O(1) 空间。
2. **维护最远可达边界 `mx`**（55 / 763）：55 用 `mx` 记录能到的最远下标，`i > mx` 即断点失败；763 同构——用 `last[]` 记录每个字符最后出现位置，遍历时 `end = max(end, last[s[i]])`，`end==i` 即切片。本质是「走到边界就结算」。
3. **BFS 按层跳跃**（45）：在 55 的 `mx` 基础上加 `curEnd`（本层边界）+ `nextEnd`（下一层边界），`i == curEnd` 时步数 +1。每跳 = BFS 一层。
4. **贪心 vs DP**：121 也常用 DP（`dp[i]=max(dp[i-1], prices[i]-min)`），但贪心 O(n)/O(1) 等价更优，**本模块只收贪心写法**（见脚注）。

> 一句话：**贪心 = 扫一遍 + 维护一个「最远/最低/最后」的边界或极值 + 走到边界就结算。**

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 面试最优解 | 时间 | 空间 |
|------|------|-----------|------|------|
| 121 | 买卖股票的最佳时机 | 历史最低价贪心 | O(n) | O(1) |
| 55 | 跳跃游戏 | 最远可达 `mx` | O(n) | O(1) |
| 45 | 跳跃游戏 II | BFS 按层（curEnd/nextEnd） | O(n) | O(1) |
| 763 | 划分字母区间 | last[] + 最远右边界 | O(n) | O(26)=O(1) |

> 四题在源文档中均为单解法，已全部列出。

---

## 三、各题解法详情

### 1. 买卖股票的最佳时机（LeetCode 121 | 🟢 简单）
**简单思路**：最大利润 = 某天卖出价 − 之前某天最低买入价。维护 `minPrice`（历史最低）与 `ans`（最大利润），每天算「今天卖能赚多少」取最大。

**对应解法**（取自 day27）：
```java
class Solution {
    public int maxProfit(int[] prices) {
        int ans = 0;
        int minPrice = prices[0];   // 截止当前天的最低买入价
        for (int p : prices) {
            ans = Math.max(ans, p - minPrice);   // 今天卖出能赚多少
            minPrice = Math.min(minPrice, p);    // 更新最低买入价
        }
        return ans;
    }
}
```
- ✅ 面试最优解：一次遍历贪心，O(n)/O(1)。`minPrice` 只记录**之前**的最低价，天然保证先买后卖。
- ⚠️ 模型提示：121 也有 DP 写法（`dp[i]=max(dp[i-1], prices[i]-min)`），但贪心已等价更优，**本模块按源文档只收贪心**。若要多次交易（可重复买卖）是 122（累加每天正利润），不在本日文档内。
- 时间 O(n) / 空间 O(1)。

---

### 2. 跳跃游戏（LeetCode 55 | 🟡 中等）
**简单思路**：只关心「最远能到哪」，维护 `mx`。`i > mx` 说明当前位置都到不了（存在断点）→ false；否则 `mx = max(mx, i+nums[i])`。遍历完能覆盖终点即 true。

**对应解法**（取自 day27）：
```java
class Solution {
    public boolean canJump(int[] nums) {
        int mx = 0;   // 最远能到达的下标
        for (int i = 0; i < nums.length; i++) {
            if (i > mx) return false;            // 当前位置到不了
            mx = Math.max(mx, i + nums[i]);      // 更新最远可达距离
        }
        return true;
    }
}
```
- ✅ 面试最优解：贪心 O(n)/O(1)，不关心具体跳法。时间 O(n) / 空间 O(1)。

---

### 3. 跳跃游戏 II（LeetCode 45 | 🟡 中等）
**简单思路**：在 55 基础上统计最少跳跃次数。`curEnd` = 当前跳能到的边界，`nextEnd` = 从 `[0..curEnd]` 出发下一跳的最远点。`i == curEnd` 表示走到本层边界，必须再跳一次（`ans++`，`curEnd=nextEnd`）。本质是 BFS 一层一跳。

**对应解法**（取自 day27）：
```java
class Solution {
    public int jump(int[] nums) {
        int ans = 0;
        int curEnd = 0;    // 当前这一跳能到达的最远下标
        int nextEnd = 0;   // 下一跳能到达的最远下标
        for (int i = 0; i < nums.length - 1; i++) {   // 不必遍历最后一个位置
            nextEnd = Math.max(nextEnd, i + nums[i]);
            if (i == curEnd) {        // 走到当前这一跳的边界
                ans++;                // 必须再跳一次
                curEnd = nextEnd;     // 进入下一层
            }
        }
        return ans;
    }
}
```
- ✅ 面试最优解：BFS 按层贪心，O(n)/O(1)。注意遍历到 `n-2` 即可（到达终点不用再跳）。时间 O(n) / 空间 O(1)。

---

### 4. 划分字母区间（LeetCode 763 | 🟡 中等）
**简单思路**：先预处理 `last[]` 记录每个字符最后出现下标。遍历字符串，`end = max(end, last[s[i]])` 扩展当前片段右边界；`end == i` 时片段内所有字符最后出现位置都不超过当前点，可安全切片，`start = end+1` 开启下一段。

**对应解法**（取自 day27）：
```java
class Solution {
    public List<Integer> partitionLabels(String s) {
        char[] sc = s.toCharArray();
        int n = sc.length;
        int[] last = new int[26];                  // 每个字符最后出现的位置
        for (int i = 0; i < n; i++) last[sc[i] - 'a'] = i;
        List<Integer> ans = new ArrayList<>();
        int start = 0, end = 0;
        for (int i = 0; i < n; i++) {
            end = Math.max(end, last[sc[i] - 'a']); // 扩展当前片段右边界
            if (end == i) {                         // 片段结束
                ans.add(end - start + 1);
                start = end + 1;
            }
        }
        return ans;
    }
}
```
- ✅ 面试最优解：与 55 的 `mx` 思路同构（维护最远边界、走到边界结算），O(n)/O(26)。时间 O(n) / 空间 O(26)=O(1)。

---

*整理自 day27-贪心算法hot100.md · 汇总日期：2026-08-07*

> 模型缺口提示（仅指出，未补充解法）：本模块只含 4 题单解法。贪心 Hot100 常见题如 **134 加油站**、**135 分发糖果**、**406 根据身高重建队列**、**621 任务调度器**、**455 分发饼干**、**860 柠檬水找零**、**738 单调递增的数字** 在源文档中暂未出现；121 的 DP 写法亦未收录。如需补全可后续补 day 文档再汇总。
