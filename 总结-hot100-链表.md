# 总结 — 链表 Hot100

> 覆盖 LeetCode 160 / 206 / 234 / 141 / 142 / 21 / 2 / 19 / 24 / 25 / 138 / 148 / 23 / 146，解法代码取自 `day4-链表hot100.md` 与 `day5-链表hot100剩余.md`，只保留「简单思路 + 解法代码 + 面试最优解标注 + 复杂度」。

---

## 一、解题模型总述

链表题的共同点是**不能随机访问**，所有技巧都围绕「如何用指针在 O(1) 空间内完成任务」展开。横向规律可归纳成四条口诀：

1. **虚拟头 `dummy` 统一边界**：凡是涉及「头节点可能被删 / 被改」的操作（删除倒数第 N 个、两两交换、K 个一组反转、合并链表、LRU），一律加 `dummy`，最后返回 `dummy.next`，省掉一堆 `head == null` 判空。
2. **快慢指针消除长度差 / 找中点 / 判环**：
   - 找中点（876 / 234 / 148）：`slow` 走 1 步、`fast` 走 2 步，`fast` 到尾时 `slow` 在中点。
   - 倒数第 N 个（19）：`fast` 先走 `n+1` 步，再同步走，让 `slow` 落在待删节点的**前驱**。
   - 判环 / 找入口（141 / 142）：Floyd 快慢指针，相遇后一头一相遇点同速走找入口。
3. **双指针「走对方的路」消除长度差**：相交链表（160）靠切换头指针让两指针总路程相等；本质与快慢指针同类。
4. **迭代 vs 递归权衡**：能迭代就迭代（O(1) 空间）。递归写起来优雅但吃调用栈（O(n) 空间），面试首选迭代，除非题目容忍栈空间。两数相加（2）、合并 K 个有序链表（23）、排序链表（148）都体现了这一点。
5. **特殊结构技巧**：带随机指针的深拷贝（138）用「原链表穿插拷贝节点」做到 O(1) 空间；LRU（146）是「哈希表定位 + 双向链表排序」的经典组合设计题。

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 面试最优解（写哪个） | 时间复杂度 | 空间复杂度 |
|------|------|---------------------|-----------|-----------|
| 160 | 相交链表 | 双指针走对方路（浪漫相遇） | O(m+n) | O(1) |
| 206 | 反转链表 | 迭代三指针（递归也行但耗栈） | O(n) | O(1) |
| 234 | 回文链表 | 中点切断 + 反转后半段比较 | O(n) | O(1) |
| 141 | 环形链表 | Floyd 快慢指针判环 | O(n) | O(1) |
| 142 | 环形链表 II | Floyd + 一头一相遇点找入口 | O(n) | O(1) |
| 21 | 合并两个有序链表 | 双指针 + dummy（递归备选） | O(m+n) | O(1) |
| 2 | 两数相加 | 迭代竖式（递归备选） | O(max(m,n)) | O(1) |
| 19 | 删除倒数第 N 个 | 快慢指针 + dummy | O(n) | O(1) |
| 24 | 两两交换节点 | 四指针迭代 | O(n) | O(1) |
| 25 | K 个一组翻转 | 分段反转 + 组间拼接 | O(n) | O(1) |
| 138 | 随机链表复制 | 穿插法 O(1) 空间（哈希表备选） | O(n) | O(1) |
| 148 | 排序链表 | 自顶向下归并（⚠️ 未达常数空间） | O(n log n) | O(log n) |
| 23 | 合并 K 个有序链表 | 小顶堆（分治备选） | O(n log k) | O(k) |
| 146 | LRU 缓存 | 哈希表 + 双向链表 + dummy | O(1) | O(capacity) |

---

## 三、各题解法详情

### 1. 相交链表（LeetCode 160 | 🟢 简单）
**简单思路**：两链表长度不同导致无法同步到达交点。让 `a` 走完 A 切到 B、`b` 走完 B 切到 A，二者总路程相等，若有交点必在交点相遇，若无交点同时到 null。

**对应解法**（取自 day4）：
```java
public class Solution {
    public ListNode getIntersectionNode(ListNode headA, ListNode headB) {
        ListNode a = headA;
        ListNode b = headB;
        while (a != b) {
            a = (a == null) ? headB : a.next;
            b = (b == null) ? headA : b.next;
        }
        return a;  // 相遇点 or null
    }
}
```
- ✅ 面试最优解：双指针走对方路，O(1) 空间，无需算长度差，面试首选。
- 时间 O(m+n) / 空间 O(1)。

---

### 2. 反转链表（LeetCode 206 | 🟢 简单）
**简单思路**：把每个节点的 `next` 指向前驱，用 `temp` 暂存下一个节点防断链，三指针 `pre/cur/temp` 整体右移。

**解法一 · 迭代三指针**（取自 day4）：
```java
class Solution {
    public ListNode reverseList(ListNode head) {
        ListNode pre = null;
        ListNode cur = head;
        while (cur != null) {
            ListNode temp = cur.next;  // 先保存下一个节点，防止断链
            cur.next = pre;            // 当前节点指向前一个节点（反转）
            pre = cur;                 // pre 右移
            cur = temp;                // cur 右移
        }
        return pre;  // pre 就是反转后的新头
    }
}
```
- 时间 O(n) / 空间 O(1)。

**解法二 · 递归（源文档仅提及，未附代码）**：`head.next.next = head; head.next = null;` 递归反转后返回新头。代码更短但空间来自递归栈 **O(n)**。

- ✅ 面试最优解：迭代三指针 O(1) 空间，面试首选；递归理解模型即可，写时优先迭代。

---

### 3. 回文链表（LeetCode 234 | 🟢 简单）
**简单思路**：回文需「两端往中间比」，但链表不能随机访问末尾。组合两道基础题——快慢指针找中点（876）+ 反转后半段（206），再同步比较，达到 O(1) 空间。

**对应解法**（取自 day4）：
```java
class Solution {
    public boolean isPalindrome(ListNode head) {
        ListNode mid = middleNode(head);      // 快慢指针找中点
        ListNode head2 = reverseList(mid);    // 反转后半段
        while (head2 != null) {               // 前半段与反转后后半段比较
            if (head.val != head2.val) return false;
            head = head.next;
            head2 = head2.next;
        }
        return true;
    }
    private ListNode middleNode(ListNode head) {
        ListNode slow = head, fast = head;
        while (fast != null && fast.next != null) {
            slow = slow.next;
            fast = fast.next.next;
        }
        return slow;
    }
    private ListNode reverseList(ListNode head) {
        ListNode pre = null, cur = head;
        while (cur != null) {
            ListNode nxt = cur.next;
            cur.next = pre;
            pre = cur;
            cur = nxt;
        }
        return pre;
    }
}
```
- ✅ 面试最优解：中点切断 + 反转后半段，O(1) 空间满足进阶要求，面试首选。
- 时间 O(n) / 空间 O(1)。

---

### 4. 环形链表（LeetCode 141 | 🟢 简单）
**简单思路**：Floyd 判圈——`slow` 走 1 步、`fast` 走 2 步，有环必套圈相遇，无环 `fast` 先到 null。

**对应解法**（取自 day4）：
```java
public class Solution {
    public boolean hasCycle(ListNode head) {
        ListNode fast = head, slow = head;
        while (fast != null && fast.next != null) {
            slow = slow.next;
            fast = fast.next.next;
            if (slow == fast) return true;
        }
        return false;
    }
}
```
- ✅ 面试最优解：Floyd 快慢指针 O(1) 空间，面试首选。注意 `while` 条件必须含 `fast.next != null` 防 NPE。
- 时间 O(n) / 空间 O(1)。

---

### 5. 环形链表 II（LeetCode 142 | 🟡 中等）
**简单思路**：在判环基础上，相遇后让一指针从头、一指针从相遇点同步走 1 步，再次相遇即环入口（数学结论：头到入口距离 = 相遇到入口距离）。

**对应解法**（取自 day4）：
```java
public class Solution {
    public ListNode detectCycle(ListNode head) {
        ListNode fast = head, slow = head;
        while (fast != null && fast.next != null) {
            slow = slow.next;
            fast = fast.next.next;
            if (slow == fast) {                 // 有环
                ListNode index1 = fast;          // 相遇点
                ListNode index2 = head;          // 头节点
                while (index1 != index2) {
                    index1 = index1.next;
                    index2 = index2.next;
                }
                return index1;                   // 环入口
            }
        }
        return null;
    }
}
```
- ✅ 面试最优解：Floyd + 数学推导找入口，O(1) 空间，面试首选。
- 时间 O(n) / 空间 O(1)。

---

### 6. 合并两个有序链表（LeetCode 21 | 🟢 简单）
**简单思路**：「拉链」式——`dummy` 起头，`cur` 每次接两链表当前较小者，对应指针后移，剩下一个链表直接拼接。

**解法一 · 双指针 + dummy**（取自 day4）：
```java
class Solution {
    public ListNode mergeTwoLists(ListNode list1, ListNode list2) {
        ListNode dummy = new ListNode();
        ListNode cur = dummy;
        while (list1 != null && list2 != null) {
            if (list1.val < list2.val) {
                cur.next = list1; list1 = list1.next;
            } else {
                cur.next = list2; list2 = list2.next;
            }
            cur = cur.next;
        }
        cur.next = (list1 != null) ? list1 : list2;
        return dummy.next;
    }
}
```
- 时间 O(m+n) / 空间 O(1)。

**解法二 · 递归（源文档仅提及，未附代码）**：`if l1==null return l2; if l2==null return l1; 取较小者并把 next 指向递归结果`。代码更短但空间 O(m+n)（递归栈）。

- ✅ 面试最优解：双指针 + dummy O(1) 空间，面试首选；递归作对比理解。本题是「合并 K 个有序链表」的基础操作。

---

### 7. 两数相加（LeetCode 2 | 🟡 中等）
**简单思路**：链表逆序存储，表头即个位，从头遍历就是竖式加法，满十进一。递归天然契合「每层一位 + carry 传递」。

**解法一 · 递归逐位相加**（取自 day4）：
```java
class Solution {
    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {
        return addTwo(l1, l2, 0);
    }
    private ListNode addTwo(ListNode l1, ListNode l2, int carry) {
        if (l1 == null && l2 == null && carry == 0) return null;
        int sum = carry;
        if (l1 != null) { sum += l1.val; l1 = l1.next; }
        if (l2 != null) { sum += l2.val; l2 = l2.next; }
        return new ListNode(sum % 10, addTwo(l1, l2, sum / 10));
    }
}
```
- 时间 O(max(m,n)) / 空间 O(max(m,n))（递归栈）。

**解法二 · 迭代竖式（省递归栈）**（取自 day4）：
```java
class Solution {
    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {
        ListNode dummy = new ListNode();
        ListNode cur = dummy;
        int carry = 0;
        while (l1 != null || l2 != null || carry != 0) {
            int sum = carry;
            if (l1 != null) { sum += l1.val; l1 = l1.next; }
            if (l2 != null) { sum += l2.val; l2 = l2.next; }
            cur = cur.next = new ListNode(sum % 10);
            carry = sum / 10;
        }
        return dummy.next;
    }
}
```
- 时间 O(max(m,n)) / 空间 O(1)。

- ✅ 面试最优解：迭代版 O(1) 空间，面试首选（把 `carry != 0` 纳入循环条件自动处理末尾进位）；递归版更易写对，可作对比。

---

### 8. 删除链表的倒数第 N 个结点（LeetCode 19 | 🟡 中等）
**简单思路**：倒数第 N 个 = 距末尾 N 步。`fast` 先走 `n+1` 步（`n+1` 而非 `n`，让 `slow` 落在待删节点**前驱**），再同步走，`fast` 到 null 时 `slow.next` 即待删节点。配 `dummy` 统一删头边界。

**对应解法**（取自 day4）：
```java
class Solution {
    public ListNode removeNthFromEnd(ListNode head, int n) {
        ListNode dummy = new ListNode(-1);
        dummy.next = head;
        ListNode fast = dummy, slow = dummy;
        for (int i = 0; i <= n; i++) fast = fast.next;  // fast 先走 n+1 步
        while (fast != null) {
            fast = fast.next;
            slow = slow.next;
        }
        slow.next = slow.next.next;  // 删除 slow 后面的节点
        return dummy.next;
    }
}
```
- ✅ 面试最优解：快慢指针 + dummy，一趟扫描 O(1) 空间，面试首选。易错点：步数是 `n+1`，必须用 dummy。
- 时间 O(n) / 空间 O(1)。

---

### 9. 两两交换链表中的节点（LeetCode 24 | 🟡 中等）
**简单思路**：每次处理一对相邻节点，交换会影响四个节点的指针，用 `node0(前驱)/node1/node2/node3(后继)` 四指针维护，三步重连后 `node0` 移到 `node1` 准备下一组。

**对应解法**（取自 day4）：
```java
class Solution {
    public ListNode swapPairs(ListNode head) {
        ListNode dummy = new ListNode(-1);
        dummy.next = head;
        ListNode node0 = dummy, node1 = head;
        while (node1 != null && node1.next != null) {
            ListNode node2 = node1.next;
            ListNode node3 = node2.next;
            node0.next = node2;   // 前驱指向 node2
            node2.next = node1;   // node2 指向 node1
            node1.next = node3;   // node1 指向后继
            node0 = node1;        // 前驱更新为 node1（不是 node2）
            node1 = node3;
        }
        return dummy.next;
    }
}
```
- ✅ 面试最优解：四指针迭代，O(1) 空间，面试首选。注意 `while` 条件保整对，前驱更新为 `node1` 而非 `node2`。
- 时间 O(n) / 空间 O(1)。

---

### 10. K 个一组翻转链表（LeetCode 25 | 🔴 困难）
**简单思路**：反转链表（206）+ 两两交换（24）的泛化。先统计长度 `n` 确定组数 `n/k`，每组用标准三指针反转，反转后通过 `p0` 衔接前后组。

**对应解法**（取自 day4）：
```java
class Solution {
    public ListNode reverseKGroup(ListNode head, int k) {
        int n = 0;
        for (ListNode cur = head; cur != null; cur = cur.next) n++;
        ListNode dummy = new ListNode(0, head);
        ListNode p0 = dummy, pre = null, cur = head;
        for (; n >= k; n -= k) {
            for (int i = 0; i < k; i++) {        // 标准反转 k 个
                ListNode next = cur.next;
                cur.next = pre;
                pre = cur;
                cur = next;
            }
            ListNode next = p0.next;             // 当前组反转后的尾部
            p0.next.next = cur;                  // 尾部指向下一组头部
            p0.next = pre;                       // 前驱指向当前组新头
            p0 = next;                           // 前驱移到下一组前一个节点
        }
        return dummy.next;
    }
}
```
- ✅ 面试最优解：分段反转 + 组间拼接，O(1) 空间，面试首选。易错点：`p0.next.next = cur` 不能丢否则断链；剩余不足 k 个保持原序。
- 时间 O(n) / 空间 O(1)。

---

### 11. 随机链表的复制（LeetCode 138 | 🟡 中等）
**简单思路**：常规哈希表映射 `原→新` 需 O(n) 空间。巧妙做法三步——① 每原节点后插拷贝节点；② 拷贝节点的 `random` 指向 `cur.random.next`；③ 分离交错链表恢复原链。

**解法一 · 穿插法（O(1) 空间）**（取自 day4）：
```java
class Solution {
    public Node copyRandomList(Node head) {
        if (head == null) return null;
        for (Node cur = head; cur != null; cur = cur.next.next)
            cur.next = new Node(cur.val, cur.next);          // Step1 插拷贝
        for (Node cur = head; cur != null; cur = cur.next.next)
            if (cur.random != null)
                cur.next.random = cur.random.next;           // Step2 设 random
        Node dummy = new Node(0), tail = dummy;
        for (Node cur = head; cur != null; cur = cur.next, tail = tail.next) {
            Node copy = cur.next;
            tail.next = copy;                                 // 接拷贝节点
            cur.next = copy.next;                             // 恢复原节点 next
        }
        return dummy.next;
    }
}
```
- 时间 O(n) / 空间 O(1)（不计输出链表）。

**解法二 · 哈希表法（源文档仅提及，未附代码）**：`HashMap<Node,Node>` 先建映射再连 `next/random`，逻辑直观但空间 O(n)。

- ✅ 面试最优解：穿插法 O(1) 空间满足进阶要求，面试首选；哈希表法作对比（更好写对）。易错点：Step3 分离时 `cur.next = copy.next` 不能省，否则原链表被破坏。

---

### 12. 排序链表（LeetCode 148 | 🟡 中等）
**简单思路**：数组常用快排，但链表随机访问 O(n) 使 partition 低效；归并排序天然适配——快慢指针找中点断开 + 递归排序左右 + 合并两有序链表。

**对应解法 · 自顶向下归并**（取自 day5）：
```java
class Solution {
    public ListNode sortList(ListNode head) {
        if (head == null || head.next == null) return head;   // 递归基
        ListNode head2 = middleNode(head);                     // 找中点断开
        head = sortList(head);
        head2 = sortList(head2);
        return mergeTwoLists(head, head2);
    }
    private ListNode middleNode(ListNode head) {
        ListNode pre = head, slow = head, fast = head;
        while (fast != null && fast.next != null) {
            pre = slow; slow = slow.next; fast = fast.next.next;
        }
        pre.next = null;          // ★ 断开，否则合并时互相干扰死循环
        return slow;
    }
    private ListNode mergeTwoLists(ListNode list1, ListNode list2) {
        ListNode dummy = new ListNode(), cur = dummy;
        while (list1 != null && list2 != null) {
            if (list1.val < list2.val) { cur.next = list1; list1 = list1.next; }
            else { cur.next = list2; list2 = list2.next; }
            cur = cur.next;
        }
        cur.next = (list1 != null) ? list1 : list2;
        return dummy.next;
    }
}
```
- 时间 O(n log n) / 空间 O(log n)（递归调用栈）。

> ⚠️ **合规说明（按用户要求只指出、未改）**：本题进阶要求是 **O(n log n) 时间 + 常数级空间**。当前自顶向下归并空间是 **O(log n)**（递归栈），未达「常数级」要求。要做到 O(1) 空间需改为**自底向上（迭代）归并**——从步长 1 开始两两合并、步长翻倍，全程指针操作无递归栈。该解法源文档未收录，待用户补充。

- ✅ 面试最优解（当前文档内）：自顶向下归并 O(n log n) 时间，面试已能写出即可；若面试官追问常数空间，需补自底向上迭代版。

---

### 13. 合并 K 个升序链表（LeetCode 23 | 🔴 困难）
**简单思路**：多路有序归并，本质是「每次从各链表头取最小接上」（21 的推广）。K 很大时遍历找最小 O(kn) 太慢，用小顶堆维护 K 个头节点取最小（O(log k)）。

**解法一 · 优先队列（小顶堆）**（取自 day5）：
```java
class Solution {
    public ListNode mergeKLists(ListNode[] lists) {
        PriorityQueue<ListNode> pq = new PriorityQueue<>((a, b) -> a.val - b.val);
        for (ListNode head : lists) if (head != null) pq.offer(head);
        ListNode dummy = new ListNode(0), cur = dummy;
        while (!pq.isEmpty()) {
            ListNode node = pq.poll();
            if (node.next != null) pq.offer(node.next);
            cur.next = node; cur = cur.next;
        }
        return dummy.next;
    }
}
```
- 时间 O(n log k) / 空间 O(k)（堆最多存 k 节点）。

**解法二 · 分治两两归并**（取自 day5）：
```java
class Solution {
    public ListNode mergeKLists(ListNode[] lists) {
        int m = lists.length;
        if (m == 0) return null;
        for (int step = 1; step < m; step *= 2) {
            for (int i = 0; i < m - step; i += step * 2) {
                lists[i] = mergeTwoLists(lists[i], lists[i + step]);
            }
        }
        return lists[0];
    }
    private ListNode mergeTwoLists(ListNode list1, ListNode list2) {
        ListNode dummy = new ListNode(), cur = dummy;
        while (list1 != null && list2 != null) {
            if (list1.val < list2.val) { cur.next = list1; list1 = list1.next; }
            else { cur.next = list2; list2 = list2.next; }
            cur = cur.next;
        }
        cur.next = (list1 != null) ? list1 : list2;
        return dummy.next;
    }
}
```
- 时间 O(n log k) / 空间 O(log k)（递归）或 O(1)（迭代实现）。

- ✅ 面试最优解：小顶堆写法最简单、最易写对，面试首选；分治版空间更优（O(1) 迭代），可作对比拓展。

---

### 14. LRU 缓存（LeetCode 146 | 🟡 中等）
**简单思路**：O(1) 的 `get/put` 需两结构配合——哈希表 O(1) 找 `key→节点`，双向链表 O(1) 移动节点并维护「头=最近、尾=最久」。节点须存 `key`（淘汰尾部时才能从哈希表删对应 key），`dummy` 哨兵环形链表省判空。

**对应解法**（取自 day5）：
```java
class LRUCache {
    private static class Node {
        int key, value; Node prev, next;
        Node(int k, int v) { key = k; value = v; }
    }
    private final int capacity;
    private final Node dummy = new Node(0, 0);
    private final Map<Integer, Node> keyToNode = new HashMap<>();
    public LRUCache(int capacity) {
        this.capacity = capacity;
        dummy.prev = dummy; dummy.next = dummy;
    }
    public int get(int key) {
        Node node = getNode(key);
        return node != null ? node.value : -1;
    }
    public void put(int key, int value) {
        Node node = getNode(key);
        if (node != null) { node.value = value; return; }
        node = new Node(key, value);
        keyToNode.put(key, node);
        pushFront(node);
        if (keyToNode.size() > capacity) {
            Node backNode = dummy.prev;       // 最久未使用
            keyToNode.remove(backNode.key);
            remove(backNode);
        }
    }
    private Node getNode(int key) {
        if (!keyToNode.containsKey(key)) return null;
        Node node = keyToNode.get(key);
        remove(node); pushFront(node);         // 移到最上面（最近使用）
        return node;
    }
    private void remove(Node x) { x.prev.next = x.next; x.next.prev = x.prev; }
    private void pushFront(Node x) {
        x.prev = dummy; x.next = dummy.next;
        x.prev.next = x; x.next.prev = x;
    }
}
```
- ✅ 面试最优解：哈希表 + 双向链表 + dummy，get/put 均 O(1)，面试首选。易错点：节点必须存 key；dummy 环形初始化时自己指向自己；双向删除改两个方向指针。
- 时间 O(1) / 空间 O(capacity)。

---

## 四、模型缺口与提醒（基于现有 day 文档）

- **148 排序链表**：当前仅自顶向下归并（O(log n) 栈空间），不满足进阶「常数空间」，需补自底向上迭代版（源文档未收录，待用户补充）。
- **递归解法占位**：206、21 在源文档中递归版本仅文字提及未附代码；138 哈希表法同理。总结如实标注，未擅自补代码。

*整理自 day4-链表hot100.md、day5-链表hot100剩余.md · 汇总日期：2026-08-17*
