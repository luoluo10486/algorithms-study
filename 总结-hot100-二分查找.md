# 总结 — 二分查找 Hot100

> 覆盖 LeetCode 35 / 74 / 34 / 153 / 33 / 4（Hot100），解法代码取自 `day22-二分查找hot100-part1.md`、`day23-二分查找hot100-part2.md`，只保留「简单思路 + 解法代码 + 面试最优解标注 + 复杂度」。

---

## 一、解题模型总述

二分查找 Hot100 本质是四类套路，识别特征是「数组有序 / 部分有序 / 两数组有序，要求 O(log n)」。横向规律四条口诀：

1. **左边界二分 lowerBound（35 / 34）**：找「第一个 ≥ target 的下标」。`nums[mid] < target → left=mid+1`；否则 `right=mid-1`；循环结束 `left` 即答案。插入位置、区间查找都靠它。闭区间模板 `while(left<=right)`，返回 `left`。
2. **开区间二分 + 哨兵（74 / 153 / 33 / 4）**：`left=-1, right=边界, while(left+1<right)`，终止时 `left+1==right`。二维矩阵展平、旋转数组找最小、双数组划分都靠它消除越界判断。
3. **与右端点比较（153 / 33）**：旋转数组被最小值切成「大段 + 小段」，`nums[mid] < nums[right] → right=mid`（mid 可能在最小侧）；否则 `left=mid`。永远和 `right` 比，因为 `nums[right]` 一定落在递增的右段，比较语义明确。
4. **二分「划分」而非「查找」（4）**：两有序数组求中位，不在数组里找数，而是在短数组上二分「分割点 i」，令左半装下较小一半（j=(m+n+1)/2-i），用 `a[i]≤b[j+1]` 判定合法划分。难度最高，是二分的进阶形态。

> 一句话：**有序就找边界（lowerBound），部分有序就和右端点比（找旋转点），两数组有序就二分分割点（划分法）。**

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 面试最优解 | 时间 | 空间 |
|------|------|-----------|------|------|
| 35 | 搜索插入位置 | 左边界二分（lowerBound） | O(log n) | O(1) |
| 74 | 搜索二维矩阵 | 展平 + 开区间二分 | O(log(mn)) | O(1) |
| 34 | 在排序数组中查找区间 | 两次 lowerBound | O(log n) | O(1) |
| 153 | 旋转数组最小值 | 开区间二分（与右端点比） | O(log n) | O(1) |
| 33 | 搜索旋转排序数组 | 找最小值 + 分段二分 | O(log n) | O(1) |
| 4 | 寻找两个正序数组的中位数 | 二分划分（哨兵 + 开区间） | O(log(min(m,n))) | O(m+n) |

> 153、4 在源文档中各含多解法，详情见第 3 节（已全列）。

---

## 三、各题解法详情

### 1. 搜索插入位置（LeetCode 35 | 🟢 简单）
**简单思路**：插入位置 = 第一个 ≥ target 的下标（左边界二分）。`nums[mid] < target` 收缩左、`else` 收缩右，结束返回 `left`。

**对应解法**（取自 day22）：
```java
class Solution {
    public int searchInsert(int[] nums, int target) {
        int left = 0;
        int right = nums.length - 1;
        while (left <= right) {
            int mid = left + (right - left) / 2;  // 防溢出
            if (nums[mid] < target) {
                left = mid + 1;     // target 在右半区
            } else {
                right = mid - 1;    // target 在左半区（含 mid）
            }
        }
        return left;  // 第一个 ≥ target 的位置
    }
}
```
- ✅ 面试最优解：左闭右闭二分，结束返回 `left`（全部小于 target 时 `left==nums.length`，正好插末尾）。
- 时间 O(log n) / 空间 O(1)。

---

### 2. 搜索二维矩阵（LeetCode 74 | 🟡 中等）
**简单思路**：矩阵「每行递增 + 行首 > 行尾」，展平后是严格递增一维数组。`mid/n` 得行、`mid%n` 得列，开区间二分一次搞定（区别于 240，240 不能展平）。

**对应解法**（取自 day23）：
```java
class Solution {
    public boolean searchMatrix(int[][] matrix, int target) {
        int m = matrix.length;
        int n = matrix[0].length;
        int left = -1;       // 开区间左哨兵
        int right = m * n;   // 开区间右哨兵
        while (left + 1 < right) {
            int mid = left + (right - left) / 2;
            int x = matrix[mid / n][mid % n];
            if (x == target) {
                return true;
            } else if (x < target) {
                left = mid;
            } else {
                right = mid;
            }
        }
        return false;
    }
}
```
- ✅ 面试最优解：展平 + 开区间二分，O(log(mn))、O(1) 空间，易写对。
- 时间 O(log(mn)) / 空间 O(1)。

---

### 3. 在排序数组中查找元素的第一个和最后一个位置（LeetCode 34 | 🟡 中等）
**简单思路**：左边界 = `lowerBound(target)`；右边界 = `lowerBound(target+1)-1`（第一个大于 target 的位置前移一位）。判断不存在时**先判越界（短路）再访问数组**，否则 `start==nums.length` 时 `nums[start]` 越界。

**对应解法**（取自 day23）：
```java
class Solution {
    public int[] searchRange(int[] nums, int target) {
        int start = lowerBound(nums, target);      // 第一个 ≥ target 的位置
        if (start == nums.length || nums[start] != target) {  // ✅ 先判越界，短路
            return new int[]{-1, -1};
        }
        int end = lowerBound(nums, target + 1) - 1;  // 第一个 > target 的位置前移一位
        return new int[]{start, end};
    }
    private int lowerBound(int[] nums, int target) {
        int left = 0;
        int right = nums.length - 1;
        while (left <= right) {
            int mid = left + (right - left) / 2;
            if (nums[mid] >= target) {
                right = mid - 1;
            } else {
                left = mid + 1;
            }
        }
        return left;
    }
}
```
- ⚠️ 易错点：`if (nums[start] != target || start == nums.length)` 会越界（`||` 先算左边，越界后才轮到右边短路）。必须写成 `start == nums.length || nums[start] != target`。
- ✅ 面试最优解：两次 `lowerBound` 复用模板，O(log n)。
- 时间 O(log n) / 空间 O(1)。

---

### 4. 寻找旋转排序数组中的最小值（LeetCode 153 | 🟡 中等）

**简单思路**：旋转数组是「大段 + 小段」，最小值在分界点。核心：`nums[mid] < nums[right] → right=mid`（mid 可能在最小侧）；否则 `left=mid`。源文档给了**开区间**和**闭区间**两种写法，都正确，任选其一即可。

**解法一 · 开区间二分（与右端点比较）（取自 day23）**
```java
class Solution {
    public int findMin(int[] nums) {
        int n = nums.length;
        int left = -1;      // 开区间左哨兵
        int right = n - 1;  // 右边界初始化为 n-1（nums[n-1] 一定在右段）
        while (left + 1 < right) {
            int mid = left + (right - left) / 2;
            if (nums[mid] < nums[right]) {
                right = mid;      // mid 在最小值右侧，收缩右边界
            } else {
                left = mid;       // mid 在最小值左侧，收缩左边界
            }
        }
        return nums[right];
    }
}
```
- 时间 O(log n) / 空间 O(1)；终止时 `left+1==right`，答案 `nums[right]`。

**解法二 · 闭区间写法（取自 day23）**
```java
class Solution {
    public int findMin(int[] nums) {
        int left = 0;
        int right = nums.length - 1;
        while (left < right) {          // 闭区间，收敛到 left==right
            int mid = left + (right - left) / 2;
            if (nums[mid] < nums[right]) {
                right = mid;            // mid 可能是最小值，右边界收到 mid
            } else {
                left = mid + 1;         // mid 已排除，左边界跳过 mid（防死循环）
            }
        }
        return nums[left];              // left == right == 最小值
    }
}
```
- 时间 O(log n) / 空间 O(1)；终止时 `left==right`，答案 `nums[left]`。注意闭区间里 `nums[mid] >= nums[right]` 时写 `left = mid+1`（mid 不可能是最小），而 `<` 时写 `right = mid`（mid 可能是最小）。

| | 开区间（left=-1） | 闭区间（left=0） |
|---|----------------|----------------|
| 循环条件 | `left + 1 < right` | `left < right` |
| 收缩规则 | 两侧都 `right=mid / left=mid` | `right=mid`，`left=mid+1` |
| 终止状态 | 返回 `nums[right]` | 返回 `nums[left]` |

- ✅ 面试最优解：两种写法等价，面试任选。开区间哨兵思路更统一（与 74/33/4 一致），闭区间更省一个哨兵变量。**务必和 `nums[right]` 比，不要和 `nums[left]` 比**——`nums[right]` 永远落在递增的右段，语义明确。
- 时间 O(log n) / 空间 O(1)。

---

### 5. 搜索旋转排序数组（LeetCode 33 | 🟡 中等）
**简单思路**：旋转点 = 最小值下标 `i`，把数组分成大段 `[0,i)`、小段 `[i,n-1]`。先 `findMin` 定位 `i`，用 `target > nums[n-1]` 判断落在哪段，再对该段做开区间 `lowerBound`。

**对应解法**（取自 day23）：
```java
class Solution {
    public int search(int[] nums, int target) {
        int i = findMin(nums);     // 最小值下标（= 旋转点）
        int n = nums.length;
        if (target > nums[n - 1]) {
            return lowerBound(nums, -1, i, target);      // 前半段（大段）
        }
        return lowerBound(nums, i - 1, n, target);       // 后半段（小段）
    }
    private int lowerBound(int[] nums, int left, int right, int target) {
        while (left + 1 < right) {
            int mid = left + (right - left) / 2;
            if (nums[mid] < target) {
                left = mid;
            } else {
                right = mid;
            }
        }
        return nums[right] == target ? right : -1;
    }
    private int findMin(int[] nums) {
        int n = nums.length;
        int left = -1;
        int right = n - 1;
        while (left + 1 < right) {
            int mid = left + (right - left) / 2;
            if (nums[mid] < nums[n - 1]) {
                right = mid;
            } else {
                left = mid;
            }
        }
        return right;
    }
}
```
- ✅ 面试最优解：复用 153 的 `findMin` + 开区间 `lowerBound`，O(log n)。区间定义 `(-1,i)` 与 `(i-1,n)` 不能重叠。
- 时间 O(log n) / 空间 O(1)。

---

### 6. 寻找两个正序数组的中位数（LeetCode 4 | 🔴 困难）

**简单思路**：不合并数组，在较短数组上二分「分割点 i」，令 `j=(m+n+1)/2-i` 使左半装下较小一半；合法划分判定 `a[i]≤b[j+1]`。用 `±∞` 哨兵数组包住消除越界。源文档含**两种解法**，解法一不合规、解法二合规。

**解法一 · 合并 + 排序（取自 day23，直观法）**
```java
class Solution {
    public double findMedianSortedArrays(int[] a, int[] b) {
        int m = a.length;
        int n = b.length;
        int[] merged = new int[m + n];
        System.arraycopy(a, 0, merged, 0, m);
        System.arraycopy(b, 0, merged, m, n);
        Arrays.sort(merged);
        int s = m + n;
        int k = (s - 1) / 2;
        return s % 2 > 0 ? merged[k] : (merged[k] + merged[k + 1]) / 2.0;
    }
}
```
- 时间 **O((m+n)log(m+n))** / 空间 O(m+n)。代码正确但**没利用「两数组已有序」的性质**，且新建 O(m+n) 数组。
- ❌ **不满足官方 O(log(m+n)) 要求**（连 O(m+n) 也达不到，多一个 log 因子）。仅作破冰/对照，正式提交勿用。

**解法二 · 二分划分（取自 day23，合规）**
```java
class Solution {
    public double findMedianSortedArrays(int[] nums1, int[] nums2) {
        if (nums1.length > nums2.length) {  // 交换，保证 i 从 0 枚举
            int[] tmp = nums1; nums1 = nums2; nums2 = tmp;
        }
        int m = nums1.length;
        int n = nums2.length;
        int[] a = new int[m + 2];
        int[] b = new int[n + 2];
        a[0] = b[0] = Integer.MIN_VALUE;
        a[m + 1] = b[n + 1] = Integer.MAX_VALUE;
        System.arraycopy(nums1, 0, a, 1, m);
        System.arraycopy(nums2, 0, b, 1, n);
        int left = 0;
        int right = m + 1;
        while (left + 1 < right) { // 开区间 (left, right)
            int i = left + (right - left) / 2;
            int j = (m + n + 1) / 2 - i;
            if (a[i] <= b[j + 1]) {
                left = i;  // 缩小区间为 (i, right)
            } else {
                right = i; // 缩小区间为 (left, i)
            }
        }
        int i = left;
        int j = (m + n + 1) / 2 - i;
        int max1 = Math.max(a[i], b[j]);
        int min2 = Math.min(a[i + 1], b[j + 1]);
        return (m + n) % 2 > 0 ? max1 : (max1 + min2) / 2.0;
    }
}
```
- 时间 **O(log(min(m,n)))** / 空间 O(m+n)（哨兵数组；若改回 `±∞` 变量哨兵可压到 O(1)，进阶可补）。
- ✅ **满足官方 O(log(m+n)) 要求**（≤ O(log(m+n))）。用 `±∞` 哨兵后判定式全程不越界、无需任何 `if` 边界判断。

> ⚠️ **复杂度要求与合规说明（重点）**：本题官方要求时间 **O(log(m+n))**——是对数级，不是 O(m+n)（线性级）。解法二已达标 ✅；解法一 O((m+n)log(m+n)) 不达标 ❌。你给的二分划分版已满足要求，无需再优化；需优化的是解法一（归并），它才是那个不合规的。

- ✅ 面试最优解：**解法二（二分划分）**，是唯一满足官方对数级要求的写法。解法一只作对比（暴露「没利用有序性」的代价）。
- 中位数取法：奇数 → `max(左半)`；偶数 → `(max(左半)+min(右半))/2.0`。

---

*整理自 day22-二分查找hot100-part1.md、day23-二分查找hot100-part2.md · 汇总日期：2026-07-31~08-01*
