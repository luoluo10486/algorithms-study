# 总结 — 多维动态规划 Hot100

> 覆盖 LeetCode 62 / 64 / 5 / 1143 / 72（Hot100），解法代码取自 `day31-多维动态规划hot100-part1.md`、`day32-多维动态规划hot100-part2.md`，只保留「简单思路 + 解法代码 + 面试最优解标注 + 复杂度」。

---

## 一、解题模型总述

多维（二维）DP 的本批可分两族，识别特征是「状态取决于两个下标 (i, j)」：

1. **网格路径 DP（62 / 64）**：状态是网格坐标 `(i, j)`，转移只来自「上方 `dfs(i-1,j)` + 左方 `dfs(i,j-1)`」。62 是路径**条数相加**（`+`），64 是路径权重取**最小**（`min` + 自身权重）。哨兵对称：求和用 `0`（无害），取最小用 `Integer.MAX_VALUE`（无害）。
2. **字符串对齐 DP（1143 / 72 / 5）**：状态是 `(i, j)` = 两串前缀的某种最值。1143/72 是「二维表配对」标杆——**相等走对角、不相等取上/左的 max 或 min**；5（最长回文子串）虽常作二维 DP 入门，但**源文档用中心扩展法**（O(1) 空间更优），DP 写法仅提及。

> 一句话：**二维 DP = 状态是 (i,j)，转移来自 上/左/对角；网格题求和或取最小，字符串题相等对角、不等取上左极值。**

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 模型 | 面试最优解 | 时间 | 空间 |
|------|------|------|-----------|------|------|
| 62 | 不同路径 | 网格二维 DP | 记忆化 / 组合公式 | O(mn) | O(mn) |
| 64 | 最小路径和 | 网格二维 DP | 记忆化（min） | O(mn) | O(mn) |
| 5 | 最长回文子串 | 中心扩展（DP 另解仅提及） | 中心扩展法 | O(n²) | O(1) |
| 1143 | 最长公共子序列 | 字符串二维 DP | 记忆化（max） | O(nm) | O(nm) |
| 72 | 编辑距离 | 字符串二维 DP | 记忆化（min 删/插/改） | O(nm) | O(nm) |

> 5 的「二维 DP（`dp[i][j]` 回文）」解法源文档仅提及未附代码，已标注；1143/72 的递推等价写法源文档仅内联提及。

---

## 三、各题解法详情

### 1. 不同路径（LeetCode 62 | 🟡 中等）
**简单思路**：到达 `(i,j)` 的路径数 = 上方 + 左方。边界：越界返回 0，起点 `(0,0)` 返回 1。

**对应解法**（取自 day31，记忆化）：
```java
class Solution {
    public int uniquePaths(int m, int n) {
        int[][] memo = new int[m][n];
        return dfs(m - 1, n - 1, memo);
    }
    private int dfs(int i, int j, int[][] memo) {
        if (i < 0 || j < 0) return 0;          // 越界
        if (i == 0 && j == 0) return 1;        // 起点
        if (memo[i][j] != 0) return memo[i][j]; // 已算
        return memo[i][j] = dfs(i - 1, j, memo) + dfs(i, j - 1, memo);
    }
}
```
- ✅ 面试最优解：记忆化 O(mn) 直观；进阶用组合数学 `C(m+n-2, m-1)` 一行 O(min(m,n)) 或滚动数组 O(n)。时间 O(mn) / 空间 O(mn)。

---

### 2. 最小路径和（LeetCode 64 | 🟡 中等）
**简单思路**：62 的孪生题 —— `dfs(i,j) = min(上, 左) + grid[i][j]`。越界返回 `Integer.MAX_VALUE`（对取最小无害），起点返回 `grid[0][0]`。

**对应解法**（取自 day32，记忆化）：
```java
class Solution {
    public int minPathSum(int[][] grid) {
        int m = grid.length, n = grid[0].length;
        int[][] memo = new int[m][n];
        for (int[] row : memo) Arrays.fill(row, -1);
        return dfs(m - 1, n - 1, grid, memo);
    }
    private int dfs(int i, int j, int[][] grid, int[][] memo) {
        if (i < 0 || j < 0) return Integer.MAX_VALUE;  // 越界，不可走
        if (i == 0 && j == 0) return grid[i][j];        // 起点
        if (memo[i][j] != -1) return memo[i][j];        // 已算
        return memo[i][j] = Math.min(dfs(i - 1, j, grid, memo),
                                     dfs(i, j - 1, grid, memo)) + grid[i][j];
    }
}
```
- ✅ 面试最优解：记忆化 O(mn)；递推可压一维 `dp[j] = grid[i][j] + min(dp[j], dp[j-1])`，或原地改 grid 做到 O(1) 额外空间。时间 O(mn) / 空间 O(mn)。

---

### 3. 最长回文子串（LeetCode 5 | 🟡 中等）
**简单思路**：回文中心对称，枚举每个中心向两侧扩张。奇数长度中心 `l=r=i`，偶数长度中心 `l=i, r=i+1`。退出循环后真实回文为 `[l+1, r-1]`，长度 `r-l-1`，`ansLeft=l+1, ansRight=r` 正好对齐 `substring` 左闭右开。

**对应解法**（取自 day32，中心扩展法）：
```java
class Solution {
    public String longestPalindrome(String s) {
        char[] st = s.toCharArray();
        int n = st.length;
        int ansLeft = 0, ansRight = 0;
        for (int i = 0; i < n; i++) {            // 奇数长度
            int l = i, r = i;
            while (l >= 0 && r < n && st[l] == st[r]) { l--; r++; }
            if (r - l - 1 > ansRight - ansLeft) { ansLeft = l + 1; ansRight = r; }
        }
        for (int i = 0; i < n - 1; i++) {        // 偶数长度
            int l = i, r = i + 1;
            while (l >= 0 && r < n && st[l] == st[r]) { l--; r++; }
            if (r - l - 1 > ansRight - ansLeft) { ansLeft = l + 1; ansRight = r; }
        }
        return s.substring(ansLeft, ansRight);
    }
}
```
- ✅ 面试最优解：中心扩展 O(n²)/O(1)，比二维 DP（空间 O(n²)）更优，面试首选。⚠️ **源文档提及的二维 DP 写法（`dp[i][j]` 表示 `s[i..j]` 是否回文，转移 `dp[i][j]=s[i]==s[j] && dp[i+1][j-1]`）仅提及未附代码**——同族还有 647（回文子串计数）。
- 时间 O(n²) / 空间 O(1)。

---

### 4. 最长公共子序列（LeetCode 1143 | 🟡 中等）
**简单思路**：`s[i]==t[j]` 走对角 +1；否则取 `max(上, 左)`（抛弃 s[i] 或 t[j] 之一）。任一方为空返回 0。二维 DP 配对问题的标准模板。

**对应解法**（取自 day32，记忆化）：
```java
class Solution {
    public int longestCommonSubsequence(String text1, String text2) {
        char[] s = text1.toCharArray(), t = text2.toCharArray();
        int n = s.length, m = t.length;
        int[][] memo = new int[n][m];
        for (int[] row : memo) Arrays.fill(row, -1);
        return dfs(n - 1, m - 1, s, t, memo);
    }
    private int dfs(int i, int j, char[] s, char[] t, int[][] memo) {
        if (i < 0 || j < 0) return 0;            // 有一方空了
        if (memo[i][j] != -1) return memo[i][j];
        if (s[i] == t[j])                         // 相等 → 对角 +1
            return memo[i][j] = dfs(i - 1, j - 1, s, t, memo) + 1;
        return memo[i][j] = Math.max(dfs(i - 1, j, s, t, memo),  // 抛弃 s[i]
                                     dfs(i, j - 1, s, t, memo)); // 抛弃 t[j]
    }
}
```
- ✅ 面试最优解：记忆化 O(nm)；递推等价 `dp[i][j]=s[i]==t[j]?dp[i-1][j-1]+1:max(dp[i-1][j],dp[i][j-1])`，可滚动压成 O(min(n,m))。注意「子序列不连续」所以不等时保留 `max(上,左)` 而非归零。时间 O(nm) / 空间 O(nm)。

---

### 5. 编辑距离（LeetCode 72 | 🔴 困难）
**简单思路**：`s[i]==t[j]` 直接对角（白送，+0）；不等取 `min(删 i, 插 j, 改)+1`。空串边界：`i<0 → j+1`（剩 t 全插入）、`j<0 → i+1`（剩 s 全删除）。是二维 DP 的封神题，LCS 的升级版。

**对应解法**（取自 day32，记忆化）：
```java
class Solution {
    public int minDistance(String word1, String word2) {
        char[] s = word1.toCharArray(), t = word2.toCharArray();
        int n = s.length, m = t.length;
        int[][] memo = new int[n][m];
        for (int[] row : memo) Arrays.fill(row, -1);
        return dfs(n - 1, m - 1, s, t, memo);
    }
    private int dfs(int i, int j, char[] s, char[] t, int[][] memo) {
        if (i < 0) return j + 1;                 // s 空，t 全插入
        if (j < 0) return i + 1;                 // t 空，s 全删除
        if (memo[i][j] != -1) return memo[i][j];
        if (s[i] == t[j])                         // 相同，白送
            return dfs(i - 1, j - 1, s, t, memo);
        return memo[i][j] = Math.min(Math.min(
                dfs(i - 1, j, s, t, memo),        // 删除 s[i]
                dfs(i, j - 1, s, t, memo)),       // 插入 t[j]
                dfs(i - 1, j - 1, s, t, memo))    // 替换 s[i]→t[j]
                + 1;
    }
}
```
- ✅ 面试最优解：记忆化 O(nm)；递推等价（第 0 行/列初始化为 `j`/`i`），可滚动压成 O(min(n,m))。`i<0`/`j<0` 用 early return 保证不越界访问 memo。时间 O(nm) / 空间 O(nm)。

| | LCS (1143) | 编辑距离 (72) |
|---|---|---|
| 相等时 | 对角 +1 | 对角 +0（白送） |
| 不相等 | `max(上, 左)` | `min(上, 左, 对角) + 1` |
| 空串边界 | 返回 0 | 返回 `i+1` / `j+1` |

---

*整理自 day31-多维动态规划hot100-part1.md、day32-多维动态规划hot100-part2.md · 汇总日期：2026-08-14~08-15*

> **模型缺口提示（仅指出，未补充解法）**：本批多维 DP 已覆盖网格路径(62/64) 与 字符串对齐(1143/72/5)。同族常见但**尚未出现**的题：三维/二维 DP（120 三角形最小路径和、63 不同路径 II、97 交错字符串、115 不同子序列、712 最小 ASCII 删除和、583 两个字符串的删除操作、516 最长回文子序列、647 回文子串、1092 最短公共超序列）；5 与 1143/72 的纯递推二维 DP 写法及 5 的二维 DP 写法源文档仅提及，待后续补 day 文档再汇总。
