# Day 2 — 数组（代码随想录）

## 题目：长度最小的子数组（Minimum Size Subarray Sum）

**LeetCode 209 | 代码随想录 | 难度：🟡 中等**

### 题目描述

给定一个含有 `n` 个**正整数**的数组和一个正整数 `target`，找出该数组中满足其和 **≥ target** 的**长度最小**的连续子数组 `[numsl, ..., numsr]`，并返回其长度。

- 如果不存在符合条件的子数组，返回 `0`。

### 示例

```
输入：target = 7, nums = [2,3,1,2,4,3]
输出：2
解释：子数组 [4,3] 是该条件下的长度最小的子数组（和=7）

输入：target = 4, nums = [1,4,4]
输出：1
解释：子数组 [4] 长度 1 即可（和=4 ≥ 4）

输入：target = 11, nums = [1,1,1,1,1,1,1,1]
输出：0
解释：总和都不到 11，不存在符合条件的子数组
```

---

## 解法：滑动窗口（双指针）

### 思路

暴力是枚举所有子数组起点终点 O(n²)；利用「**数组全是正数**」这个关键性质可以优化成 O(n)：

- 因为元素都是正数，窗口内**加上一个正数总和必增、减去一个正数总和必减**——窗口和关于窗口大小是**单调**的。
- 用 `right` 不断向右扩展窗口，累加 `sum`；一旦 `sum >= target`，就**尝试收缩左边界** `left++`（同时减掉 `nums[left]`），在「仍满足 ≥ target」的前提下把窗口压到最短，并用 `right - left + 1` 更新最小长度。

```
nums = [2,3,1,2,4,3], target = 7

right 扩到 3: sum=2+3+1+2=8 ≥7 → 收缩: left=0→1 (sum=6<7 退出) → 长度=4
right 扩到 4: sum=6+4=10 ≥7 → 收缩: left=1→2→3 (sum 10→7→4) → 最短长度=2 ([4,3]) ✅
```

> **为什么不会漏解？** 每个 `right` 固定时，`left` 能缩到「刚好仍满足」的最左位置，此时窗口对该 `right` 是最优的；遍历完所有 `right` 就覆盖了全局最优。

### 代码实现

```java
class Solution {
    // 滑动窗口
    public int minSubArrayLen(int s, int[] nums) {
        int left = 0;
        int sum = 0;
        int result = Integer.MAX_VALUE;
        for (int right = 0; right < nums.length; right++) {
            sum += nums[right];                 // 右边界扩张，纳入新元素
            while (sum >= s) {                  // 满足要求就尽量收缩左边界
                result = Math.min(result, right - left + 1);
                sum -= nums[left++];            // 减掉左端元素，窗口左移
            }
        }
        return result == Integer.MAX_VALUE ? 0 : result;  // 没找到返回 0
    }
}
```

### 复杂度分析

| 维度 | 结果 |
|------|------|
| ⏱ 时间复杂度 | **O(n)** — 每个元素最多进窗口一次、出窗口一次，n 为数组长度 |
| 🧠 空间复杂度 | **O(1)** — 只用 `left / right / sum / result` 几个变量 |

---

## 小总结

| 要点 | 说明 |
|------|------|
| 算法名称 | 滑动窗口（双指针） |
| 算法类型 | 数组、双指针、滑动窗口 |
| 核心技巧 | **右扩左缩：右指针加元素，和≥target 就收缩左边界求最短** |
| 适用前提 | 元素**全为正**（保证窗口和单调，收缩时和确定性减小） |
| 易错点 | 找不到时返回 `0`（用 `MAX_VALUE` 做哨兵再判断）；`s` 即题目里的 `target` |
| 关联 | 与 Day1 的快慢指针同属双指针，但这里是「同向滑动窗口」而非「快慢收拢」 |

### 一句话记住

> **「正数数组求最短子段，右扩左缩滑窗口；和够就缩，缩到不能再缩。」**

---

## 题目：螺旋矩阵 II（Spiral Matrix II）

**LeetCode 59 | 代码随想录 | 难度：🟡 中等**

### 题目描述

给你一个正整数 `n`，生成一个包含 `1` 到 `n²` 所有元素，且元素按 **顺时针顺序螺旋排列** 的 `n × n` 正方形矩阵 `matrix`。

### 示例

```
输入：n = 3
输出：[[1,2,3],[8,9,4],[7,6,5]]

输入：n = 1
输出：[[1]]
```

### 图示

```
n = 3 的填充顺序（顺时针，左闭右开每一圈）：
┌─────────┐
│ 1  2  3 │
│ 8  9  4 │
│ 7  6  5 │
└─────────┘
```

---

## 解法：模拟（坚持「循环不变量」——每条边左闭右开）

### 思路

螺旋填充的本质是「**一圈一圈往里填**」。难点不在逻辑，而在**边界不重不漏**。代码随想录的解法是给每条边定一个**统一的开闭规则**：

> **每一条边都按「左闭右开」来走**（即包含起点、不包含终点），这样四条边拼接后正好不重不漏。

算法步骤：

1. `loop` 控制圈数，共 `n / 2` 圈（例如 `n=3` 只走 1 圈，中心单独填）；
2. 每一圈分四步，全部**左闭右开**：
   - **顶部**：`[startY, n-offset)` 横着填一行
   - **右列**：`[startX, n-offset)` 竖着填一列
   - **底部**：从右往左填（`j > startY`）
   - **左列**：从下往上填（`i > startX`）
3. 每圈结束 `startX++`、`startY++`、`offset++`；
4. 若 `n` 为**奇数**，循环结束后中心那个格子 `nums[startX][startY]` 单独填上最后一个数字 `count`。

```
n = 3：loop 从 1 到 1（n/2=1）
  顶部: (0,0)(0,1)          → 1,2
  右列: (1,2)(2,2)          → 3,4
  底部: (2,1)(2,0)          → 5,6
  左列: (1,0)               → 7
  start=(1,1), n 奇数 → 中心填 9（count=9）
→ [[1,2,3],[8,9,4],[7,6,5]] ✅
```

> **为什么坚持左闭右开？** 如果某条边含了终点，下一条边的起点就会重复填同一个格子；统一「左闭右开」让四条边的接缝恰好错开，是最不容易出 bug 的写法。

### 代码实现

```java
class Solution {
    public int[][] generateMatrix(int n) {
        int[][] nums = new int[n][n];
        int startX = 0, startY = 0;  // 每一圈的起始点
        int offset = 1;
        int count = 1;  // 矩阵中需要填写的数字
        int loop = 1; // 记录当前的圈数
        int i, j; // j 代表列, i 代表行;

        while (loop <= n / 2) {

            // 顶部
            // 左闭右开，所以判断循环结束时， j 不能等于 n - offset
            for (j = startY; j < n - offset; j++) {
                nums[startX][j] = count++;
            }

            // 右列
            // 左闭右开，所以判断循环结束时， i 不能等于 n - offset
            for (i = startX; i < n - offset; i++) {
                nums[i][j] = count++;
            }

            // 底部
            // 左闭右开，所以判断循环结束时， j != startY
            for (; j > startY; j--) {
                nums[i][j] = count++;
            }

            // 左列
            // 左闭右开，所以判断循环结束时， i != startX
            for (; i > startX; i--) {
                nums[i][j] = count++;
            }
            startX++;
            startY++;
            offset++;
            loop++;
        }
        if (n % 2 == 1) { // n 为奇数时，单独处理矩阵中心的值
            nums[startX][startY] = count;
        }
        return nums;
    }
}
```

### 复杂度分析

| 维度 | 结果 |
|------|------|
| ⏱ 时间复杂度 | **O(n²)** — 每个格子恰好填一次，共 n² 个 |
| 🧠 空间复杂度 | **O(n²)** — 输出矩阵本身（不含输出则为 O(1)） |

---

## 小总结

| 要点 | 说明 |
|------|------|
| 算法名称 | 模拟（螺旋填充） |
| 算法类型 | 数组、模拟、矩阵 |
| 核心技巧 | **坚持「循环不变量」：每条边统一左闭右开，四步一圈，不重不漏** |
| 圈数 | 共 `n/2` 圈，奇数时中心格子单独填 |
| 易错点 | 四条边开闭规则必须统一，否则会重复填或漏填；`offset` 每圈 +1 控制收缩 |
| 关联 | 与 Day19 的「旋转图像」「螺旋矩阵(54)」同属矩阵模拟类，都靠边界规则防错 |

### 一句话记住

> **「螺旋填充靠模拟，每条边左闭右开；转满 n/2 圈，奇数中心补一刀。」**

---

*练习日期：2026-08-17*
