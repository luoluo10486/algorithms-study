# 总结 — 回溯 Hot100

> 覆盖 LeetCode 46 / 78 / 17 / 39 / 22 / 79 / 131 / 51（Hot100），解法代码取自 `day24-回溯hot100-part1.md`、`day25-回溯hot100-part2.md`、`day26-回溯hot100-part3.md`，只保留「简单思路 + 解法代码 + 面试最优解标注 + 复杂度」。

---

## 一、解题模型总述

回溯的骨架永远是「**做选择 → 递归 → 撤销选择**」，识别特征是「求所有方案 / 组合 / 排列 / 路径」。横向规律四条口诀：

1. **path 用数组按索引填值最省事**（46 / 17 / 22）：`path.set(i, val)` 或 `path[i] = c` 直接覆盖，撤销只需改标记（如 `onPath[j]=false`）或直接被下一轮覆盖，**不需要 `add/remove`**。全排列用 `onPath[]` 防重复，字串题用 `char[]` 按 `i` 定位。
2. **选/不选 二叉决策**（78 / 39）：每步只有「取 / 不取」两个分支。子集取了 `i` 推进（`i++`）；组合总和取了 `i` **不推进**（可无限重复选），靠 `left`（剩余目标）剪枝。
3. **约束剪枝**（22 / 79 / 51）：合法路径靠约束缩小分支——括号靠 `left<n` 且 `right<left`；N 皇后靠 `col[c]` + 双斜线 `diag1[r+c]`/`diag2[r-c+n-1]`；网格靠 `board[i][j]=0` 标记已访问。
4. **切割 / 枚举起点**（131 / 79）：分割回文用双指针 `(i, start)` 维护当前子串，回文才切一刀；网格搜索从每个格子出发 DFS 四个方向。N 皇后是「逐行放、列+斜线三标记」的经典变体。

> 一句话：**回溯 = 状态数组 + 选/不选（或遍历候选）+ 约束剪枝 + 撤销；数组填值比 add/remove 更稳。**

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 面试最优解 | 时间 | 空间 |
|------|------|-----------|------|------|
| 46 | 全排列 | 回溯 + `onPath[]` + `path.set` | O(n·n!) | O(n) |
| 78 | 子集 | 选/不选 二叉回溯 | O(n·2ⁿ) | O(n) |
| 17 | 电话号码字母组合 | `char[]` path 覆盖 | O(n·4ⁿ) | O(n) |
| 39 | 组合总和 | 选/不选（`left` 剪枝）或 for+排序剪枝 | O(S)（剪枝后远小于 2ⁿ） | O(target/min) |
| 22 | 括号生成 | 左右计数约束（逐个填） | O(4ⁿ/√n) | O(n) |
| 79 | 单词搜索 | DFS 回溯 + 计数/反转预优化 | O(mn·4ᴸ) | O(L) |
| 131 | 分割回文串 | 枚举切割点 + 回文判断 | O(n·2ⁿ) | O(n) |
| 51 | N 皇后 | 逐行 + 列/双斜线标记 | O(n!) | O(n) |

> 39、22、79 在源文档中各含多解法/优化版，详情见第 3 节（已全列）。

---

## 三、各题解法详情

### 1. 全排列（LeetCode 46 | 🟡 中等）
**简单思路**：每个位置 `i` 选一个未用的数字。`path` 是固定长度 List，用 `path.set(i, val)` 填值，`onPath[j]` 标记已用，撤销只需 `onPath[j]=false`，无需 `remove`。

**对应解法**（取自 day24）：
```java
class Solution {
    public List<List<Integer>> permute(int[] nums) {
        int n = nums.length;
        List<Integer> path = Arrays.asList(new Integer[n]);  // 固定长度
        boolean[] onPath = new boolean[n];
        List<List<Integer>> ans = new ArrayList<>();
        dfs(0, nums, path, onPath, ans);
        return ans;
    }
    private void dfs(int i, int[] nums, List<Integer> path, boolean[] onPath, List<List<Integer>> ans) {
        if (i == nums.length) {
            ans.add(new ArrayList<>(path));
            return;
        }
        for (int j = 0; j < nums.length; j++) {
            if (!onPath[j]) {
                path.set(i, nums[j]);   // 做选择
                onPath[j] = true;
                dfs(i + 1, nums, path, onPath, ans);
                onPath[j] = false;      // 撤销
            }
        }
    }
}
```
- ✅ 面试最优解：`onPath[]` + `path.set` 固定长度写法，干净且不易错。
- 时间 O(n·n!) / 空间 O(n)。

---

### 2. 子集（LeetCode 78 | 🟡 中等）
**简单思路**：每个元素只决策一次「选 / 不选」，二叉决策树，叶子即全部子集。子集不在乎顺序，**不需要 `onPath`**，用 `i` 推进即可。

**对应解法**（取自 day24）：
```java
class Solution {
    public List<List<Integer>> subsets(int[] nums) {
        List<List<Integer>> ans = new ArrayList<>();
        List<Integer> path = new ArrayList<>();
        dfs(0, nums, path, ans);
        return ans;
    }
    private void dfs(int i, int[] nums, List<Integer> path, List<List<Integer>> ans) {
        if (i == nums.length) {
            ans.add(new ArrayList<>(path));
            return;
        }
        dfs(i + 1, nums, path, ans);    // 不选
        path.add(nums[i]);              // 选
        dfs(i + 1, nums, path, ans);
        path.removeLast();              // 回溯
    }
}
```
- ✅ 面试最优解：选/不选二叉回溯，最直观。时间 O(n·2ⁿ) / 空间 O(n)。

---

### 3. 电话号码的字母组合（LeetCode 17 | 🟡 中等）
**简单思路**：第 `i` 位从 `digits[i]` 对应字母集合选一个。`char[] path` 长度一开始就是 `n`，`path[i]=c` 直接覆盖，天然回溯无需撤销。空串返回 `[]`（不是 `[""]`）。

**对应解法**（取自 day25）：
```java
class Solution {
    private static final String[] MAPPING =
        new String[]{"", "", "abc", "def", "ghi", "jkl", "mno", "pqrs", "tuv", "wxyz"};
    public List<String> letterCombinations(String digits) {
        int n = digits.length();
        if (n == 0) return List.of();
        List<String> ans = new ArrayList<>();
        char[] path = new char[n];
        dfs(0, ans, path, digits.toCharArray());
        return ans;
    }
    private void dfs(int i, List<String> ans, char[] path, char[] digits) {
        if (i == digits.length) {
            ans.add(new String(path));
            return;
        }
        String letters = MAPPING[digits[i] - '0'];
        for (char c : letters.toCharArray()) {
            path[i] = c;
            dfs(i + 1, ans, path, digits);
        }
    }
}
```
- ✅ 面试最优解：`char[]` 覆盖式回溯，比 `StringBuilder` + `deleteCharAt` 更简洁。时间 O(n·4ⁿ) / 空间 O(n)。

---

### 4. 组合总和（LeetCode 39 | 🟡 中等）

**简单思路**：子集变体——数字可**无限重复**选（选了 `i` 不推进），且目标和限制靠 `left` 剪枝。源文档给**两种写法**，都正确。

**解法一 · 选/不选 二叉（取自 day25）**
```java
class Solution {
    public List<List<Integer>> combinationSum(int[] candidates, int target) {
        List<List<Integer>> ans = new ArrayList<>();
        List<Integer> path = new ArrayList<>();
        dfs(0, target, candidates, ans, path);
        return ans;
    }
    private void dfs(int i, int left, int[] candidates,
                     List<List<Integer>> ans, List<Integer> path) {
        if (left == 0) { ans.add(new ArrayList<>(path)); return; }
        if (left < 0 || i == candidates.length) return;     // 剪枝
        dfs(i + 1, left, candidates, ans, path);             // 不选
        path.add(candidates[i]);                             // 选（i 不推进）
        dfs(i, left - candidates[i], candidates, ans, path);
        path.remove(path.size() - 1);                        // 回溯
    }
}
```

**解法二 · for 循环 + 排序剪枝（取自 day25）**
```java
class Solution {
    public List<List<Integer>> combinationSum(int[] candidates, int target) {
        Arrays.sort(candidates);   // 排序为了剪枝
        List<List<Integer>> ans = new ArrayList<>();
        List<Integer> path = new ArrayList<>();
        dfs(0, target, candidates, ans, path);
        return ans;
    }
    private void dfs(int i, int left, int[] candidates,
                     List<List<Integer>> ans, List<Integer> path) {
        if (left == 0) { ans.add(new ArrayList<>(path)); return; }
        for (int j = i; j < candidates.length && candidates[j] <= left; j++) {  // 剪枝
            path.add(candidates[j]);
            dfs(j, left - candidates[j], candidates, ans, path);  // j 不推进，可重复选
            path.remove(path.size() - 1);
        }
    }
}
```

| | 选/不选 二叉 | for 循环 + 排序剪枝 |
|---|------------|------------------|
| 分支 | 每元素两分支 | 遍历 i..n-1 一个分支 |
| 剪枝 | `left<0` 或 `i==n` | `candidates[j] <= left`（提前 break） |
| 排序 | 不需要 | 需要 |

- ✅ 面试最优解：两种等价，面试任选。解法二（for+排序）更常见、剪枝更强；解法一（`left` 剪枝）无需排序更省事。
- 时间 O(S)（S 为可行组合长度之和，剪枝后远小于 2ⁿ）/ 空间 O(target/min)。

---

### 5. 括号生成（LeetCode 22 | 🟡 中等）

**简单思路**：2n 个位置填 `(`/`)`，约束「`left<n` 才能放 `(`、`right<left` 才能放 `)`」保证合法。源文档给**两种写法**。

**解法一 · 逐个位置填括号（取自 day25）**
```java
class Solution {
    public List<String> generateParenthesis(int n) {
        List<String> ans = new ArrayList<>();
        char[] path = new char[2 * n];
        dfs(0, 0, n, ans, path);
        return ans;
    }
    private void dfs(int left, int right, int n, List<String> ans, char[] path) {
        if (right == n) { ans.add(new String(path)); return; }  // 右括号满 = 完成
        if (left < n) { path[left + right] = '('; dfs(left + 1, right, n, ans, path); }
        if (right < left) { path[left + right] = ')'; dfs(left, right + 1, n, ans, path); }
    }
}
```

**解法二 · 记录左括号下标（灵茶山，取自 day25）**
```java
class Solution {
    public List<String> generateParenthesis(int n) {
        List<String> ans = new ArrayList<>();
        List<Integer> path = new ArrayList<>();   // 记录左括号的下标
        dfs(0, 0, n, path, ans);
        return ans;
    }
    private void dfs(int i, int balance, int n, List<Integer> path, List<String> ans) {
        if (path.size() == n) {                   // n 个左括号都放好
            char[] s = new char[n * 2];
            Arrays.fill(s, ')');                  // 默认全 ')'
            for (int j : path) s[j] = '(';        // 左括号下标处替换为 '('
            ans.add(new String(s));
            return;
        }
        for (int right = 0; right <= balance; right++) {  // 枚举填 right 个右括号再填 1 个左
            path.add(i + right);
            dfs(i + right + 1, balance - right + 1, n, path, ans);
            path.removeLast();
        }
    }
}
```

| | 逐个填括号 | 记录左括号下标 |
|---|-----------|---------------|
| path 存 | 每位置字符 | 左括号下标 |
| 每层放 | 1 个 | `right+1` 个（批量） |

- ✅ 面试最优解：解法一最直观、易写对，面试首选；解法二（灵茶山）分支更少、更高效，作为拓展变体了解即可。
- 时间 O(4ⁿ/√n)（卡特兰数）/ 空间 O(n)。

---

### 6. 单词搜索（LeetCode 79 | 🟡 中等）

**简单思路**：从每个格子 DFS 匹配 word，四个方向扩展；`board[i][j]=0` 标记已访问（网格只有字母，0 天然拦截重复），回溯恢复。源文档给出**基础版**和**预处理优化版**。

**解法一 · 基础 DFS 回溯（取自 day26）**
```java
class Solution {
    private static final int[][] dirs = {{0,-1},{0,1},{-1,0},{1,0}};
    public boolean exist(char[][] board, String word) {
        char[] w = word.toCharArray();
        for (int i = 0; i < board.length; i++)
            for (int j = 0; j < board[0].length; j++)
                if (dfs(i, j, 0, board, w)) return true;
        return false;
    }
    private boolean dfs(int i, int j, int k, char[][] board, char[] word) {
        if (board[i][j] != word[k]) return false;        // 不匹配
        if (k == word.length - 1) return true;           // 匹配完成
        board[i][j] = 0;                                 // 标记已访问
        for (int[] d : dirs) {
            int x = i + d[0], y = j + d[1];
            if (0 <= x && x < board.length && 0 <= y && y < board[x].length
                && dfs(x, y, k + 1, board, word)) return true;
        }
        board[i][j] = word[k];                           // 回溯
        return false;
    }
}
```

**解法二 · 预处理优化版（灵茶山，取自 day26）**
> 在解法一基础上加两个优化：① 字符计数预检查——`word` 中某字符出现次数 > `board` 中该字符总数时直接 `false`，跳过整个 DFS；② 从更稀有字符开始搜——若 `word` 末尾字符比首字符更稀有，反转 `word` 从末尾开始搜，起点数大幅下降。

```java
class Solution {
    private static final int[][] DIRS = {{0,-1},{0,1},{-1,0},{1,0}};
    public boolean exist(char[][] board, String word) {
        int[] cnt = new int[128];
        for (char[] row : board) for (char c : row) cnt[c]++;
        char[] w = word.toCharArray();
        int[] wordCnt = new int[128];
        for (char c : w) if (++wordCnt[c] > cnt[c]) return false;   // 优化一
        if (cnt[w[w.length - 1]] < cnt[w[0]])                       // 优化二
            w = new StringBuilder(word).reverse().toString().toCharArray();
        for (int i = 0; i < board.length; i++)
            for (int j = 0; j < board[i].length; j++)
                if (dfs(i, j, 0, board, w)) return true;
        return false;
    }
    private boolean dfs(int i, int j, int k, char[][] board, char[] word) {
        if (board[i][j] != word[k]) return false;
        if (k == word.length - 1) return true;
        board[i][j] = 0;
        for (int[] d : DIRS) {
            int x = i + d[0], y = j + d[1];
            if (0 <= x && x < board.length && 0 <= y && y < board[x].length
                && dfs(x, y, k + 1, board, word)) return true;
        }
        board[i][j] = word[k];
        return false;
    }
}
```
- ✅ 面试最优解：核心逻辑都是 DFS 回溯，**解法二（优化版）是面试加分项**——字符计数 + 反转从稀有字符起搜，能大幅剪枝、提升通过率；解法一作为基础框架先讲清。
- 时间 O(mn·4ᴸ)（优化在平均情况剪枝）/ 空间 O(L)。

---

### 7. 分割回文串（LeetCode 131 | 🟡 中等）
**简单思路**：双指针 `(i, start)` 维护当前子串区间 `s[start..i]`。不切割就 `i` 前进；回文才切一刀（`path.add`，下一段从 `i+1` 起）。`i` 走到末尾必须切（否则最后一段为空）。

**对应解法**（取自 day26）：
```java
class Solution {
    public List<List<String>> partition(String s) {
        List<List<String>> ans = new ArrayList<>();
        List<String> path = new ArrayList<>();
        dfs(0, 0, s, ans, path);
        return ans;
    }
    private void dfs(int i, int start, String s,
                     List<List<String>> ans, List<String> path) {
        if (i == s.length()) { ans.add(new ArrayList<>(path)); return; }
        if (i < s.length() - 1) dfs(i + 1, start, s, ans, path);   // 不切
        if (isPalindrome(s, start, i)) {                           // 切（回文）
            path.add(s.substring(start, i + 1));
            dfs(i + 1, i + 1, s, ans, path);
            path.removeLast();
        }
    }
    private boolean isPalindrome(String s, int left, int right) {
        while (left < right)
            if (s.charAt(left++) != s.charAt(right--)) return false;
        return true;
    }
}
```
- ✅ 面试最优解：双指针 + 回溯。进阶可用 DP 预处理 `isPalindrome` 表把判断降到 O(1)。
- 时间 O(n·2ⁿ) / 空间 O(n)。

---

### 8. N 皇后（LeetCode 51 | 🔴 困难）
**简单思路**：逐行放皇后，`queens[r]=c` 记录行 r 的列。冲突用三标记：`col[c]`（列）、`diag1[r+c]`（主斜线 ↘）、`diag2[r-c+n-1]`（副斜线 ↙）。`queens[r]` 每轮被覆盖无需恢复，只回溯三个标记数组。

**对应解法**（取自 day26）：
```java
class Solution {
    public List<List<String>> solveNQueens(int n) {
        List<List<String>> ans = new ArrayList<>();
        int[] queens = new int[n];
        boolean[] col = new boolean[n];
        boolean[] diag1 = new boolean[n * 2 - 1];   // 主对角线 r+c
        boolean[] diag2 = new boolean[n * 2 - 1];   // 副对角线 r-c+n-1
        dfs(0, queens, col, diag1, diag2, ans);
        return ans;
    }
    private void dfs(int r, int[] queens, boolean[] col,
                     boolean[] diag1, boolean[] diag2, List<List<String>> ans) {
        int n = col.length;
        if (r == n) {                               // 所有行放好
            List<String> board = new ArrayList<>(n);
            for (int c : queens) {
                char[] row = new char[n];
                Arrays.fill(row, '.');
                row[c] = 'Q';
                board.add(new String(row));
            }
            ans.add(board);
            return;
        }
        for (int c = 0; c < n; c++) {
            int rc = r - c + n - 1;                 // 副对角线下标（防负）
            if (!col[c] && !diag1[r + c] && !diag2[rc]) {
                queens[r] = c;
                col[c] = diag1[r + c] = diag2[rc] = true;
                dfs(r + 1, queens, col, diag1, diag2, ans);
                col[c] = diag1[r + c] = diag2[rc] = false;  // 恢复
            }
        }
    }
}
```
- ✅ 面试最优解：`col` + 双斜线三标记是标准写法，比暴力判断 O(1) 检验冲突高效。时间 O(n!)（剪枝后）/ 空间 O(n)。

---

*整理自 day24-回溯hot100-part1.md、day25-回溯hot100-part2.md、day26-回溯hot100-part3.md · 汇总日期：2026-08-02~08-06*
