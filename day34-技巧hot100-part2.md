# Day 34 — 技巧 Hot100（Part 2）

## 题目：下一个排列（Next Permutation）

**LeetCode 31 | 数组 / 数学 / 技巧 Hot100 | 难度：🟡 中等**

### 题目描述

整数数组的一个 **排列** 就是将其所有成员以任意顺序排列而成的序列。

给定一个数字数组 `nums`，找出其所有排列中，在**字典序**上紧挨着 `nums` 的下一个排列（即比当前排列**大、且最小**的那个）。

- 如果不存在下一个更大的排列，则将其重新排列为字典序最小的排列（即升序排列）。
- 必须**原地**修改，只允许使用额外常数空间。

### 示例

```
输入：nums = [1,2,3]
输出：[1,3,2]        （比 123 大、且最小的是 132）

输入：nums = [3,2,1]
输出：[1,2,3]        （已是最大排列，翻转为最小）

输入：nums = [1,1,5]
输出：[1,5,1]
```

---

## 解法：标准"下一个排列"算法（三步走）

### 思路

要在"只大一点点"的前提下求下一个排列，直觉是：**尽量不动高位，只动最低的可能位置**。标准算法分三步：

```
Step 1 — 找"转折点" i：
   从右往左找第一个 nums[i] < nums[i+1] 的位置 i。
   这代表从 i+1 到末尾是"降序/非增"的（已经是这一位能凑出的最大后缀），
   必须让 i 这位变大，才能得到一个更大的排列。

   找不到（数组全程非增）→ 说明当前就是最大排列 → 直接整个反转成最小（Step 3 兜底）。

Step 2 — 找"换位伙伴" j：
   在 i+1..末尾 这段里，从右往左找第一个 nums[j] > nums[i] 的位置 j。
   这一段是降序的，所以从右数第一个比 nums[i] 大的，就是"比 nums[i] 大、且最小"的那个数。
   交换 nums[i] 和 nums[j] —— i 这位成功变大，且变得最小。

Step 3 — 反转后缀：
   交换后，i+1..末尾 仍然是降序的；把它反转成升序，
   就得到"比原排列大、且最小"的后缀（字典序最小）。
   （当 i<0 时，i+1=0，reverse 整个数组 → 翻转成最小排列，正好覆盖"已是最大"的情况。）
```

**为什么这样是对的？** 字典序比较从高位到低位。`nums[i]` 是"能从右侧借到更大值"的最低位；把它换成刚好比它大的 `nums[j]`，并保证后缀取最小（升序），就得到了"比当前大、且最小"的排列。

### 思考方式图解

```
nums = [1, 2, 7, 4, 3, 1]    （找下一个排列）

Step 1: 从右找首个 nums[i] < nums[i+1]
   3 > 1? 否(3>1 是大于，继续左)  ← 注意：要找"小于"
   4 > 3? 是，方向反了，继续
   7 > 4? 是，方向反了
   2 < 7? ✅ i = 1（nums[1]=2）

Step 2: 在 i+1..末尾 找首个 > nums[i]=2（从右）
   后缀 [7,4,3,1] 降序，从右第一个 >2 的是 3（下标 j=4）
   交换 nums[1],nums[4] → [1, 3, 7, 4, 2, 1]

Step 3: 反转 i+1..末尾 = 反转 [7,4,2,1] → [1,2,4,7]
   结果：[1, 3, 1, 2, 4, 7] ✅
   （验证：比 127431 大，且是其中最小的）

特殊：nums = [3,2,1]
Step 1: 全程 nums[i]>=nums[i+1]，i 一路减到 -1
Step 2: i<0 跳过交换
Step 3: reverse(0, 2) → [1,2,3]（最大翻最小）✅
```

### 代码实现

```java
class Solution {
    public void nextPermutation(int[] nums) {
        int n = nums.length;
        int i = n - 2;
        // Step 1：找第一个 nums[i] < nums[i+1]（从右往左第一个"上升"点）
        while (i >= 0 && nums[i] >= nums[i + 1]) {
            i--;
        }
        if (i >= 0) {
            int j = n - 1;
            // Step 2：在右侧降序段里，从右找第一个 > nums[i] 的数
            while (nums[j] <= nums[i]) {
                j--;
            }
            swap(nums, i, j);
        }
        // Step 3：反转 i+1..末尾（i<0 时即反转整个数组，得到最小排列）
        reverse(nums, i + 1, n - 1);
    }

    private void swap(int[] nums, int i, int j) {
        int temp = nums[i];
        nums[i] = nums[j];
        nums[j] = temp;
    }

    private void reverse(int[] nums, int left, int right) {
        while (left < right) {
            swap(nums, left++, right--);
        }
    }
}
```

> **边界安全**：`i` 减到 `-1` 时，跳过交换，`reverse(0, n-1)` 把最大排列翻成最小，逻辑自洽。`j` 一定存在（因为 `nums[i] < nums[i+1]`，至少 `nums[i+1]` 就 > `nums[i]`），不会越界。

### 复杂度分析

| 维度 | 结果 |
|------|------|
| ⏱ 时间复杂度 | **O(n)** — 最多扫描两遍 + 反转一遍，各 O(n) |
| 🧠 空间复杂度 | **O(1)** — 只用几个指针，原地修改 |

---

## 小总结

| 要点 | 说明 |
|------|------|
| 算法名称 | 下一个排列（Next Permutation 标准算法） |
| 算法类型 | 数组、数学、字典序、技巧题 |
| 核心三步 | **① 找上升点 i（nums[i]<nums[i+1]）② 右侧找首个 >nums[i] 的 j 交换 ③ 反转 i+1..末尾** |
| 为何找"首个上升点" | 它是能从右侧借到更大值的最低位；动它才能"只大一点点" |
| i<0 的兜底 | 已是最大排列 → reverse 整个数组翻成最小 |
| 关键细节 | 后缀段是降序的，故"从右找首个 >nums[i]"即最小可换值；反转后缀得字典序最小 |
| 关联题目 | [60. 排列序列](https://leetcode.cn/problems/permutation-sequence/)（第 k 个排列）、[46. 全排列](https://leetcode.cn/problems/permutations/)（穷举所有） |

### 一句话记住

> **「下一个排列 = 找上升点、换最小大数、反转后缀——字典序只大一点点。」**

---

## 题目：寻找重复数（Find the Duplicate Number）

**LeetCode 287 | 数组 / 双指针 / 技巧 Hot100 | 难度：🟡 中等**

### 题目描述

给定一个包含 `n + 1` 个整数的数组 `nums`，其数字都在 `[1, n]` 范围内（包括 1 和 n）。

**已知至少有一个重复的数字**，假设**只有一个重复的数字**，但可能重复不止一次。找出那个重复的数。

**要求**：不能修改原数组（若允许修改可用排序/交换）、只能用 O(1) 额外空间、时间复杂度小于 O(n²)、且不能只用哈希表或集合（那些是 O(n) 空间）。

### 示例

```
输入：nums = [1,3,4,2,2]
输出：2

输入：nums = [3,1,3,4,2]
输出：3

输入：nums = [1,1]
输出：1
```

---

## 解法：快慢指针（Floyd 判圈 / 龟兔赛跑）

### 思路

这是本题最经典的 O(1) 空间解法，核心是把数组**当成一条链表**来看：

```
把下标 i 当成"节点"，nums[i] 当成"指向的下一个节点"。
因为值域是 [1, n]、数组长度是 n+1，由鸽巢原理必有两个下标指向同一个值
→ 链表里一定存在环，而"环的入口"就是要找的重复数字。
```

证明入口 = 重复数：重复数 `x` 被至少两个下标（比如 `a` 和 `b`）指向，于是 `a → x` 和 `b → x` 汇合，x 成为环的入口。链表其余部分是一条"尾巴 + 环"，入口正是那个被多个节点指向的值 = 重复数。

**Floyd 两步法**：

```
Phase 1（找相遇点）：
  slow 每次走 1 步：slow = nums[slow]
  fast 每次走 2 步：fast = nums[nums[fast]]
  因为有环，快慢指针终会在环内相遇。

Phase 2（找环入口 = 重复数）：
  把其中一个指针（head）拉回起点 0，
  两个指针都每次走 1 步，再次相遇处就是环入口 → 重复数。
  （结论：从起点到入口的距离 = 从相遇点到入口的距离，两者同步走必在入口相遇）
```

### 思考方式图解

```
nums = [1,3,4,2,2]（n=4，长度 5，值∈[1,4]）

当作链表（i → nums[i]）：
  0 → 1 → 3 → 2 → 4 → 2 → 4 → 2 ...
                    ↑________↑   环：2 ↔ 4
  入口 = 2 = 重复数 ✅

Phase 1：
  slow,fast 都从 0 出发
  s:0→1→3→2→4   f:0→3→4→2→4   在第 4 步 slow=f=4 相遇

Phase 2：
  head 从 0 出发，slow 从相遇点 4 出发，都每次 1 步：
  head:0→1→3→2      slow:4→2→4→2
  在某步 head=slow=2 → 入口=2 = 重复数 ✅
```

> **为什么 Phase 2 一定能相遇在入口？** 设起点到入口距离为 `a`，入口到相遇点距离为 `b`，环长为 `c`。相遇时 fast 比 slow 多走整数圈：`2(a+b) = a+b + k·c` → `a+b = k·c` → `a = k·c - b`。即"起点走 a 步" = "相遇点再走 a 步绕回入口"，故两者同步必在入口碰头。

### 代码实现

```java
class Solution {
    public int findDuplicate(int[] nums) {
        int slow = 0;
        int fast = 0;
        // Phase 1：快慢指针找环内相遇点
        while (true) {
            slow = nums[slow];          // 走 1 步
            fast = nums[nums[fast]];    // 走 2 步
            if (slow == fast) {
                break;
            }
        }
        // Phase 2：一个回起点，同步走 1 步，相遇于环入口（重复数）
        int head = 0;
        while (head != slow) {
            head = nums[head];
            slow = nums[slow];
        }
        return slow;
    }
}
```

> **初始化说明**：`slow`、`fast` 都从 `0` 起步，循环里**先移动再比较**——等价于"起点相同、起步后错开"，不影响最终在环内相遇。也可写成 `do-while` 先判断，效果一致。

> **对比其他解法**：二分答案（按值域二分，O(n log n) 时间 / O(1) 空间）、哈希表/排序（O(n) 时间但 O(n) 空间或改原数组）。快慢指针是少数同时满足"不改数组 + O(1) 空间 + O(n) 时间"的优雅方案。

### 复杂度分析

| 维度 | 结果 |
|------|------|
| ⏱ 时间复杂度 | **O(n)** — 两阶段指针最多各走 O(n) 步 |
| 🧠 空间复杂度 | **O(1)** — 只用 slow/fast/head 三个指针，满足"不改数组"要求 |

---

## 小总结

| 要点 | 说明 |
|------|------|
| 算法名称 | 快慢指针（Floyd 判圈 / 龟兔赛跑） |
| 算法类型 | 数组、双指针、链表化、技巧题 |
| 核心技巧 | **把数组当链表（i→nums[i]）；Phase1 快慢相遇，Phase2 一指针回起点同步走找入口** |
| 为何入口=重复数 | 重复值被多个下标指向 → 成为环的入口；鸽巢原理保证有环 |
| 前提 | 值∈[1,n]、长度 n+1、恰一个重复值（可能重复多次） |
| 优势 | 不改原数组 + O(1) 空间 + O(n) 时间，吊打哈希/排序 |
| 关联题目 | [142. 环形链表 II](https://leetcode.cn/problems/linked-list-cycle-ii/)（同算法，找环入口）、[141. 环形链表](https://leetcode.cn/problems/linked-list-cycle/)（仅判圈） |

### 一句话记住

> **「重复数 = 把数组当链表，快慢指针相遇后，一指针回起点同步走，碰头处即环入口 = 重复值。」**

---

*练习日期：2026-08-17*
