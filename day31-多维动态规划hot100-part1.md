# Day 31 — 多维动态规划 Hot100（Part 1）

## 题目：不同路径（Unique Paths）

**LeetCode 62 | 多维动态规划 Hot100 | 难度：🟡 中等**

### 题目描述

一个机器人位于一个 `m x n` 网格的左上角（起始点在下图中标记为 "Start"）。

机器人每次只能向下或者向右移动一步。机器人试图达到网格的右下角（在下图中标记为 "Finish"）。

问总共有多少条不同的路径？

### 示例

```
输入：m = 3, n = 7
输出：28

输入：m = 3, n = 2
输出：3
解释：从左上角开始，总共有 3 条路径可以到达右下角
  1. 向右 → 向下 → 向下
  2. 向下 → 向下 → 向右
  3. 向下 → 向右 → 向下
```

---

## 解法：记忆化搜索（二维 DP）

### 思路

这是**二维动态规划**的入门题。到达格子 `(i, j)` 的路径数 = 到达上方格子的路径数 + 到达左方格子的路径数。

```
dfs(i, j) = 到达 (i, j) 的不同路径数

递推公式：
  dfs(i, j) = dfs(i-1, j) + dfs(i, j-1)
              ↑ 从上边下来      ↑ 从左边过来

边界：
  i < 0 或 j < 0 → 0（越界，没有路径）
  i == 0 且 j == 0 → 1（起点本身就是一条"路径"）
```

**为什么从 `(m-1, n-1)` 往回推？** 终点在右下角，反推时每个格子只要看它的上方和左方两个来源，方向清晰。

### 思考方式图解

```
m = 3, n = 3（3x3 网格）

从起点 (0,0) 到每个格子的路径数（正向理解）：

  1  1  1
  1  2  3
  1  3  6  ← (2,2) 有 6 条路径

递推关系：每个格子 = 上方 + 左边
  (1,1) = (0,1) + (1,0) = 1 + 1 = 2
  (2,2) = (1,2) + (2,1) = 3 + 3 = 6 ✅
```

### 代码实现

```java
class Solution {
    public int uniquePaths(int m, int n) {
        int[][] memo = new int[m][n];   // memo[i][j]：到达 (i,j) 的路径数
        return dfs(m - 1, n - 1, memo);
    }

    private int dfs(int i, int j, int[][] memo) {
        if (i < 0 || j < 0) {           // 越界
            return 0;
        }
        if (i == 0 && j == 0) {         // 起点
            return 1;
        }
        if (memo[i][j] != 0) {          // 已算过
            return memo[i][j];
        }
        // 从上边下来 + 从左边过来
        return memo[i][j] = dfs(i - 1, j, memo) + dfs(i, j - 1, memo);
    }
}
```

### 复杂度分析

| 维度 | 结果 |
|------|------|
| ⏱ 时间复杂度 | **O(mn)** — 每个格子只算一次 |
| 🧠 空间复杂度 | **O(mn)** — memo 二维数组 + 递归栈 |

> **优化**：递推 DP 可以压缩成一维数组（滚动），空间降到 **O(n)**；甚至直接用组合数学 C(m+n-2, m-1) 一行公式。

---

## 小总结

| 要点 | 说明 |
|------|------|
| 算法名称 | 记忆化搜索（二维 DP） |
| 算法类型 | 动态规划、二维 DP |
| 核心技巧 | **`dfs(i,j) = dfs(i-1,j) + dfs(i,j-1)`——从上边下来 + 从左边过来** |
| 边界 | 越界返回 0；起点 (0,0) 返回 1 |
| 与一维 DP 的区别 | 一维只依赖前两个值；二维依赖上方和左方两个格子的值 |
| 优化方向 | 滚动数组 O(n) / 组合数学 O(1) |
| 关联题目 | [64. 最小路径和](https://leetcode.cn/problems/minimum-path-sum/)——同样的网格 DP，求 min |

### 一句话记住

> **「到达 (i,j) = 上边 + 左边——二维网格 DP 入门。」**

---

*练习日期：2026-08-14*
