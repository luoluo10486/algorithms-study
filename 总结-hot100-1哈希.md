# 总结 — Hot100（1）· 哈希表

> 本篇是 Hot100 哈希表专题的解题模型总结，覆盖三道高频题：**两数之和（1）、字母异位词分组（49）、最长连续序列（128）**。
> 解法代码均取自本仓库对应文档（`day8-哈希表hot100+字符串代码随想录-part1.md`），只保留「简单思路 + 解法代码 + 面试最优解标注 + 复杂度」。

---

## 一、解题模型总述（哈希的核心套路）

哈希表的本质是 **用空间换时间**：把"线性扫描找某个值"从 O(n) 降到 O(1)。这三道题恰好对应哈希在 Hot100 里最典型的三种用法：

1. **两数之和 —「边走边查另一半」模型**
   - 遍历数组时，把"已经见过的元素"存进 HashMap（值 → 下标）。对当前数 `nums[i]`，只需查 `target - nums[i]` 是否已在 map 中。
   - **关键**：**先查后存**，避免自己匹配自己。一遍扫描即出结果。

2. **字母异位词分组 —「构造等价 key」模型**
   - 异位词的本质是"字母组成相同、排列不同"。要给它们归到同一组，就得构造一个**能抹平排列差异的 key**。
   - 最直观的 key 是 **排序后的字符串**（`"eat"`、`"tea"` 排序后都是 `"aet"`），用这个 key 当 HashMap 的键，天然分组。进阶可用**计数数组**（26 个字母频次）拼成 key，把排序的 `k log k` 降到 `O(k)`。

3. **最长连续序列 —「HashSet 去重 + 起点枚举」模型**
   - 要求 O(n)，不能排序。把所有数丢进 HashSet（O(1) 查邻居）。
   - **只从没有前驱的元素（序列起点）开始往后数**——非起点一定是某个更长序列的子集，数它纯属浪费。每个数最多被访问两次，均摊 O(n)。

> **一句话模型**：哈希 = 存已见 / 造等价 key / 去重后查邻居。暴力能降维的地方，多半就是哈希的用武之地。

---

## 二、三题速览（面试最优解 + 复杂度）

| 题号 | 题目 | 面试最优解（写哪个） | 时间复杂度 | 空间复杂度 |
|------|------|---------------------|-----------|-----------|
| 1 | 两数之和 | **一遍哈希（先查后存）**，别写暴力 O(n²) | O(n) | O(n) |
| 49 | 字母异位词分组 | **排序/计数做 key + HashMap**（计数法更优） | O(nk log k)；计数法 O(nk) | O(nk) |
| 128 | 最长连续序列 | **HashSet 起点枚举**，别写排序 O(n log n) | O(n) | O(n) |

> 标注说明："面试最优解"指官方要求 / 面试官期望的解法——两数之和和最长连续序列都有**明确更优**的哈希解；字母异位词分组的排序 key 法已是最常用最优解（计数数组 key 属于同思路下的常数优化）。

---

## 三、各题解法详情

### 1. 两数之和（LeetCode 1 | 🟢 简单）

**简单思路**：暴力两层循环 O(n²) 会超时。用 HashMap 把遍历过的元素「值 → 下标」存起来；对当前元素 `nums[i]`，先查 `target - nums[i]` 在不在 map 里，在就直接返回配对下标，不在就把当前元素存入 map。先查后存保证不会用同一个元素两次。

**对应解法（取自 day8）**：

```java
class Solution {
    public int[] twoSum(int[] nums, int target) {
        int[] res = new int[2];
        Map<Integer, Integer> map = new HashMap<>();

        for (int i = 0; i < nums.length; i++) {
            int temp = target - nums[i];       // 需要的另一个数
            if (map.containsKey(temp)) {
                res[1] = i;
                res[0] = map.get(temp);
                break;
            }
            map.put(nums[i], i);               // 把当前元素存入 map
        }

        return res;
    }
}
```

- **✅ 面试最优解**：就是上面这个**一遍哈希**写法（O(n)），不要写 O(n²) 暴力。
- **时间复杂度**：O(n) — 只遍历数组一次。
- **空间复杂度**：O(n) — 哈希表最多存 n 个元素。

---

### 2. 字母异位词分组（LeetCode 49 | 🟡 中等）

**简单思路**：异位词"字母组成相同、顺序不同"。给每个字符串排序，排序后的串就是它们共享的 key——所有异位词排序后 key 相同，自然归到 HashMap 的同一组。一次遍历即可分组。

**对应解法（取自 day8）**：

```java
class Solution {
    public List<List<String>> groupAnagrams(String[] strs) {
        Map<String, List<String>> map = new HashMap<>();

        for (String str : strs) {
            // 对每个字符串排序，得到唯一的 key
            char[] arrays = str.toCharArray();
            Arrays.sort(arrays);
            String key = new String(arrays);

            // key 不存在时创建新列表
            if (map.get(key) == null) {
                List<String> newList = new ArrayList<>();
                newList.add(str);
                map.put(key, newList);
            } else {
                map.get(key).add(str);
            }
        }

        return new ArrayList<List<String>>(map.values());
    }
}
```

- **✅ 面试最优解**：就是上面 **排序做 key + HashMap** 写法。若想更进一步，可把 key 换成 **26 个字母的计数数组**（`int[26]` 拼成字符串），排序开销 `k log k` 降为 `O(k)`，属于同思路的常数优化，面试中可作为"还能更快"的加分点主动提。
- **时间复杂度**：O(nk log k) — n 个字符串、k 为最长串长度（排序开销）；计数数组 key 法为 O(nk)。
- **空间复杂度**：O(nk) — 存储所有字符串到 HashMap。

---

### 3. 最长连续序列（LeetCode 128 | 🟡 中等）

**简单思路**：要求 O(n)，不能排序。把所有数丢进 HashSet。遍历 set，对**没有前驱**（`x-1` 不在 set 中）的元素当作起点，往后 `while` 数连续的个数，更新最大值。非起点跳过——避免重复计数。

**对应解法（取自 day8）**：

```java
class Solution {
    public int longestConsecutive(int[] nums) {
        Set<Integer> set = new HashSet<>();
        for (int i : nums) {
            set.add(i);
        }

        int ans = 0;
        for (int x : set) {                    // 遍历 set（自动去重）
            if (set.contains(x - 1)) {          // 有前驱 → 不是起点，跳过
                continue;
            }
            int y = x + 1;
            while (set.contains(y)) {            // 从起点往后数
                y++;
            }
            ans = Math.max(ans, y - x);          // 序列长度 = y - x
        }
        return ans;
    }
}
```

- **✅ 面试最优解**：就是上面这个 **HashSet 起点枚举**写法（O(n)），不要写排序 O(n log n)（题目明确要 O(n)）。
- **时间复杂度**：O(n) — 每个元素最多被检查两次（一次查前驱、一次被 while 访问），内层 while 总次数 ≤ n，均摊 O(1)。
- **空间复杂度**：O(n) — HashSet 存放所有元素。

---

*整理自 `day8-哈希表hot100+字符串代码随想录-part1.md` · 汇总日期：2026-08-17*
