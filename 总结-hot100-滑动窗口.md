# 总结 — Hot100（3）· 滑动窗口

> 覆盖 LeetCode 209 / 239 / 76。源文件分散：`day2-数组代码随想录-part2.md`（209）、`day20-子串hot100.md`（239、76）。滑动窗口核心套路：**右指针扩张纳入、左指针收缩求最优**，配合"单调性 / 单调队列 / 计数数组"三种判定方式。

---

## 一、解题模型总述

滑动窗口用来在**线性结构**上求"满足某条件的连续子段"。通用骨架：

```
right 右移纳入新元素 → 检查窗口是否"合法/达标" → 一旦达标就 left 右移收缩，更新答案
```

三种典型判定：

1. **基于单调性收缩**（209）：元素全为正时，窗口和随收缩单调减小，达标即缩到最短。
2. **单调队列维护极值**（239）：窗口最大值用单调递减队列，O(n) 内每次取队首。
3. **计数数组判涵盖**（76）：用 `cntT` 记录目标字符需求，`cntS` 记录窗口内字符，`isCovered` 判断是否满足。

> 一句话：**右扩左缩，按"单调/队列/计数"三种尺子量窗口。**

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 面试最优解 | 时间 | 空间 |
|------|------|-----------|------|------|
| 209 | 长度最小的子数组 | 滑动窗口（右扩左缩） | O(n) | O(1) |
| 239 | 滑动窗口最大值 | 单调队列（双端队列） | O(n) | O(k) |
| 76 | 最小覆盖子串 | 滑动窗口 + 计数数组 | O(m·Σ) | O(1) |

---

## 三、各题解法详情

### 1. 长度最小的子数组（LeetCode 209 | 🟡 中等）

**简单思路**：全正数保证窗口和单调。`right` 累加 `sum`，一旦 `sum >= s` 就收缩 `left` 求最短长度。

**对应解法**（取自 day2）：

```java
class Solution {
    public int minSubArrayLen(int s, int[] nums) {
        int left = 0, sum = 0, result = Integer.MAX_VALUE;
        for (int right = 0; right < nums.length; right++) {
            sum += nums[right];
            while (sum >= s) {
                result = Math.min(result, right - left + 1);
                sum -= nums[left++];
            }
        }
        return result == Integer.MAX_VALUE ? 0 : result;
    }
}
```

- ✅ 面试最优解：滑动窗口 O(n)/O(1)。前提"全正数"不可省。
- 时间 O(n) / 空间 O(1)。

### 2. 滑动窗口最大值（LeetCode 239 | 🔴 困难）

**简单思路**：单调递减队列存下标。入队时清掉所有 ≤ 当前元素的队尾（它们永不可能再当最大）；队首下标超出窗口左界则出队；窗口形成后队首即最大值。

**对应解法**（取自 day20）：

```java
class Solution {
    public int[] maxSlidingWindow(int[] nums, int k) {
        int n = nums.length;
        int[] ans = new int[n - k + 1];
        Deque<Integer> q = new ArrayDeque<>();  // 存下标，单调递减
        for (int i = 0; i < n; i++) {
            while (!q.isEmpty() && nums[q.getLast()] < nums[i]) q.removeLast();
            q.addLast(i);
            int left = i - k + 1;
            if (q.getFirst() < left) q.removeFirst();
            if (left >= 0) ans[left] = nums[q.getFirst()];
        }
        return ans;
    }
}
```

- ✅ 面试最优解：单调队列 O(n)/O(k)。比"每个窗口扫一遍" O(n·k) 更优，是面试必考。
- 时间 O(n) / 空间 O(k)。

### 3. 最小覆盖子串（LeetCode 76 | 🔴 困难）

**简单思路**：`cntT` 记目标字符需求，`cntS` 记窗口内字符。`right` 纳入、`isCovered` 为真就收缩 `left` 求最短覆盖。

**对应解法**（取自 day20）：

```java
class Solution {
    public String minWindow(String s, String t) {
        int[] cntS = new int[128], cntT = new int[128];
        for (char c : t.toCharArray()) cntT[c]++;
        char[] S = s.toCharArray();
        int m = S.length, ansLeft = -1, ansRight = m, left = 0;
        for (int right = 0; right < m; right++) {
            cntS[S[right]]++;
            while (isCovered(cntS, cntT)) {
                if (right - left < ansRight - ansLeft) {
                    ansLeft = left; ansRight = right;
                }
                cntS[S[left]]--;
                left++;
            }
        }
        return ansLeft < 0 ? "" : s.substring(ansLeft, ansRight + 1);
    }
    private boolean isCovered(int[] cntS, int[] cntT) {
        for (int i = 'A'; i <= 'Z'; i++) if (cntS[i] < cntT[i]) return false;
        for (int i = 'a'; i <= 'z'; i++) if (cntS[i] < cntT[i]) return false;
        return true;
    }
}
```

- ✅ 面试最优解：滑动窗口 + 计数数组 O(m·Σ)/O(1)。`int[128]` 比 HashMap 快；字符数组 `S` 比 `charAt` 快。
- 时间 O(m·Σ)（Σ=52 大小写）/ 空间 O(1)。

---

*整理自 `day2-数组代码随想录-part2.md`、`day20-子串hot100.md` · 汇总日期：2026-08-17*
