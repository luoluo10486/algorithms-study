# 总结 — Hot100 · 滑动窗口

> 覆盖 LeetCode **3 / 438**（Hot100「滑动窗口」板块全部两题）。源文件：`day35-滑动窗口hot100.md`。
> 滑动窗口核心套路：**右指针扩张纳入、左指针收缩，用「计数数组」量窗口是否合法**；窗口分**变长**（3、438 解法一）与**定长**（438 解法二）两类。

---

## 一、解题模型总述

滑动窗口用来在线性结构上求「满足某条件的连续子段」。通用骨架：

```
right 右移纳入新元素 → 计数/更新状态 → 一旦违反约束就 left 右移收缩 → 更新答案
```

本专题两种窗口形态、一种计数尺子：

1. **变长窗口 + 去重条件**（3）：窗口内**不得有重复字符**。`cnt[c] > 1` 即违规，围绕刚进入的 `c` 收缩左侧，直到合法。
2. **变长窗口 + 配额条件**（438 解法一）：把 `cnt` 当 `p` 的字母**配额**，进入即扣减；`cnt[c] < 0` 即某字母超额，收缩左端归还配额；窗口长度达到 `p.length()` 即为异位词。
3. **定长窗口 + 数组比对**（438 解法二）：窗口长度恒为 `p.length()`，每次 `Arrays.equals(cntS, cntP)` 判断是否异位词。

> 一句话：**变长窗口看「是否越界」收缩，定长窗口看「两数组是否相等」。**

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 面试最优解 | 时间 | 空间 |
|------|------|-----------|------|------|
| 3 | 无重复字符的最长子串 | 变长滑动窗口 + 计数数组 | O(n) | O(1) |
| 438 | 找到字符串中所有字母异位词 | 变长滑动窗口（配额扣减） | O(n + m) | O(1) |

> 438 另有「定长窗口 + `Arrays.equals`」对照解法，见下文；面试首选变长窗口版。

---

## 三、各题解法详情

### 1. 无重复字符的最长子串（LeetCode 3 | 🟡 中等）

**简单思路**：`right` 右移把 `s[right]` 纳入窗口并计数；若 `cnt[c] > 1` 出现重复，就不断右移 `left` 把 `s[left]` 移出（计数减一），直到窗口重新无重复。此时窗口即「以 `right` 结尾的最长无重复子串」，用 `right - left + 1` 更新答案。

**对应解法**（取自 day35）：

```java
class Solution {
    public int lengthOfLongestSubstring(String S) {
        char[] s = S.toCharArray(); // 转成 char[] 加快效率
        int n = s.length;
        int ans = 0;
        int left = 0;
        int[] cnt = new int[128];
        for (int right = 0; right < n; right++) {
            char c = s[right];
            cnt[c]++;
            while (cnt[c] > 1) {        // 窗口内有重复字母
                cnt[s[left]]--;         // 移除窗口左端点字母
                left++;                 // 缩小窗口
            }
            ans = Math.max(ans, right - left + 1);
        }
        return ans;
    }
}
```

- ✅ 面试最优解：变长滑动窗口 O(n)/O(1)。每个字符最多被 left/right 各访问一次；`int[128]` 为固定大小。
- 时间 O(n) / 空间 O(1)。

### 2. 找到字符串中所有字母异位词（LeetCode 438 | 🟡 中等）

#### 解法一 · 变长滑动窗口（配额扣减）✅ 推荐

**简单思路**：`cnt` 初始为 `p` 的字母计数，当作**配额**。`right` 进入即 `cnt[s[right]]--`；若某字母 `cnt[c] < 0`（超额），就右移 `left` 把 `s[left]` 还回去（`cnt[s[left]]++`），直到不再超额。当窗口长度恰为 `p.length()` 时，窗口内各字母计数与 `p` 完全一致，`left` 即一个答案。

**对应解法**（取自 day35）：

```java
class Solution {
    public List<Integer> findAnagrams(String s, String p) {
        int[] cnt = new int[26];              // 统计 p 的每种字母的出现次数
        for (char c : p.toCharArray()) cnt[c - 'a']++;

        List<Integer> ans = new ArrayList<>();
        int left = 0;
        for (int right = 0; right < s.length(); right++) {
            int c = s.charAt(right) - 'a';
            cnt[c]--;                          // 右端点字母进入窗口
            while (cnt[c] < 0) {               // 字母 c 太多了
                cnt[s.charAt(left) - 'a']++;   // 左端点字母离开窗口
                left++;
            }
            if (right - left + 1 == p.length()) // 长度等于 p，字母计数必相同
                ans.add(left);
        }
        return ans;
    }
}
```

- ✅ 面试最优解：变长窗口 O(n + m)/O(1)。只需一个计数数组、无 `Arrays.equals`，时间常数更小、写法更紧凑。
- 时间 O(n + m) / 空间 O(1)。

#### 解法二 · 定长滑动窗口（数组比对）

**简单思路**：窗口长度恒为 `p.length()`。`cntS` 记窗口计数、`cntP` 记 `p` 计数；`right` 进入时 `cntS[s[right]]++`，左端点 `left = right - p.length() + 1`，窗口成型后 `Arrays.equals(cntS, cntP)` 判异位词，随后把 `s[left]` 移出。

**对应解法**（取自 day35）：

```java
class Solution {
    public List<Integer> findAnagrams(String s, String p) {
        int[] cntP = new int[26];
        for (char c : p.toCharArray()) cntP[c - 'a']++;

        List<Integer> ans = new ArrayList<>();
        int[] cntS = new int[26];
        for (int right = 0; right < s.length(); right++) {
            cntS[s.charAt(right) - 'a']++;
            int left = right - p.length() + 1;
            if (left < 0) continue;            // 窗口长度不足
            if (Arrays.equals(cntS, cntP)) ans.add(left);
            cntS[s.charAt(left) - 'a']--;      // 左端点离开窗口
        }
        return ans;
    }
}
```

- 面试定位：定长窗口写法更直白，但每步 `Arrays.equals` 比对 26 个字母，实际 O(26·n)；作为对照理解。
- 时间 O(26·n) ≈ O(n) / 空间 O(1)。

---

## 四、模型缺口与提醒

- 本板块 Hot100 仅 **3、438** 两题，均已收录，**无缺口**。
- 易混点：**「滑动窗口」板块**（3、438）与 **「子串」板块**（560、239、76）不是一回事——后者见《总结-hot100-字串》。其中 239（单调队列）、76（计数覆盖收缩）也是滑动窗口，但按 Hot100 官方归属列入「子串」。
- 同族的「变长窗口」模板还可套用：**904 水果成篮**、**1004 最大连续 1 的个数 III**、**1658 将 x 减到 0 的最小操作数** 等（源文档未收录）。

---

*整理自 `day35-滑动窗口hot100.md` · 汇总日期：2026-09-16（按 Hot100 官方分组重排）*
