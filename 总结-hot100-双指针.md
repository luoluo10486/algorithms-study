# 总结 — Hot100（1）· 双指针

> 覆盖 LeetCode 27 / 11 / 15 / 42，解法均取自 `day10-双指针hot100+代码随想录.md`。双指针在 Hot100 里主要分两类：**同向快慢指针**（原地删除/覆盖）与**对撞双指针**（两端逼近）。

---

## 一、解题模型总述

1. **快慢指针（同向）**：快指针探路、慢指针占位。典型用于"原地删除/去重"——只保留合法元素，慢指针即新长度。核心是 `slow` 永远指向"下一个要填的位置"。
2. **对撞双指针（相向）**：`left` 从 0、`right` 从 `n-1` 向中间靠拢。每步根据"短板/和大小/接水量"决定移动哪一侧。核心洞察是**移动短板收益可能为正、移动长板收益必为负**。
3. **固定一 + 双指针扫二**：排序后固定一个数，剩余两个数在对撞区间内找（三数之和模板），复杂度从 O(n³) 降到 O(n²)。

> 一句话：**同向用来"删/覆盖"，相向用来"找最优/凑和"。**

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 面试最优解 | 时间 | 空间 |
|------|------|-----------|------|------|
| 27 | 移除元素 | 快慢指针原地删除 | O(n) | O(1) |
| 11 | 盛最多水的容器 | 对撞双指针（移短板） | O(n) | O(1) |
| 15 | 三数之和 | 排序 + 双指针 + 去重 | O(n²) | O(1) |
| 42 | 接雨水 | 对撞双指针（维护两侧最大值） | O(n) | O(1) |

---

## 三、各题解法详情

### 1. 移除元素（LeetCode 27 | 🟢 简单）

**简单思路**：快指针遍历找 `!= val` 的元素，慢指针把它们依次搬到前面；遍历完 `[0, slow)` 即结果。

**对应解法**（取自 day10）：

```java
class Solution {
    public int removeElement(int[] nums, int val) {
        int slow = 0;
        for (int fast = 0; fast < nums.length; fast++) {
            if (nums[fast] != val) {
                nums[slow] = nums[fast];
                slow++;
            }
        }
        return slow;
    }
}
```

- ✅ 面试最优解：就是上面这个**快慢指针**原地写法，O(n)/O(1)。同源还有 26 删除有序数组重复项。
- 时间 O(n) / 空间 O(1)。

### 2. 盛最多水的容器（LeetCode 11 | 🟡 中等）

**简单思路**：面积 = 宽 × 短板高。每次移动**较短**的那条边——移长板面积必减，移短板才可能变大。

**对应解法**（取自 day10）：

```java
class Solution {
    public int maxArea(int[] height) {
        int ans = 0, left = 0, right = height.length - 1;
        while (left < right) {
            int area = (right - left) * Math.min(height[left], height[right]);
            ans = Math.max(ans, area);
            if (height[left] < height[right]) left++;
            else right--;
        }
        return ans;
    }
}
```

- ✅ 面试最优解：对撞双指针，O(n)/O(1)。两边相等时移哪边都行。
- 时间 O(n) / 空间 O(1)。

### 3. 三数之和（LeetCode 15 | 🟡 中等）

**简单思路**：先排序，外层固定一个数 `x`，内层在 `x` 之后的区间用对撞双指针找 `j,k` 使 `x+nums[j]+nums[k]==0`。三层去重（i/j/k 各一层）。

**对应解法**（取自 day10）：

```java
class Solution {
    public List<List<Integer>> threeSum(int[] nums) {
        Arrays.sort(nums);
        List<List<Integer>> ans = new ArrayList<>();
        int n = nums.length;
        for (int i = 0; i < n - 2; i++) {
            int x = nums[i];
            if (i > 0 && nums[i - 1] == nums[i]) continue;   // i 去重
            if (x + nums[i + 1] + nums[i + 2] > 0) break;     // 剪枝
            if (x + nums[n - 1] + nums[n - 2] < 0) continue;  // 剪枝
            int j = i + 1, k = n - 1;
            while (j < k) {
                int s = x + nums[j] + nums[k];
                if (s > 0) k--;
                else if (s < 0) j++;
                else {
                    ans.add(List.of(x, nums[j], nums[k]));
                    for (j++; j < k && nums[j] == nums[j - 1]; j++);   // j 去重
                    for (k--; j < k && nums[k] == nums[k + 1]; k--);   // k 去重
                }
            }
        }
        return ans;
    }
}
```

- ✅ 面试最优解：排序 + 双指针，O(n²)/O(1)（不计排序与输出）。这是 nSum 系列的核心模板（四数之和在此基础上再套一层）。
- 时间 O(n²) / 空间 O(1)。

### 4. 接雨水（LeetCode 42 | 🔴 困难）

**简单思路**：每个位置接水量 = `min(左最大, 右最大) - 自身高度`。对撞双指针同时维护 `premax/sufmax`，比较两者较小者即当前短板，直接结算该侧。

**对应解法**（取自 day10）：

```java
class Solution {
    public int trap(int[] height) {
        int ans = 0, premax = 0, sufmax = 0, left = 0, right = height.length - 1;
        while (left < right) {
            premax = Math.max(premax, height[left]);
            sufmax = Math.max(sufmax, height[right]);
            if (premax < sufmax) ans += premax - height[left++];
            else ans += sufmax - height[right--];
        }
        return ans;
    }
}
```

- ✅ 面试最优解：对撞双指针 O(n)/**O(1)**。比"预处理前后缀最大值"的 O(n) 空间更优，是面试首选。
- 时间 O(n) / 空间 O(1)。

---

*整理自 `day10-双指针hot100+代码随想录.md` · 汇总日期：2026-08-17*
