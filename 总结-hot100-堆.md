# 总结 — 堆 Hot100

> 覆盖 LeetCode 215 / 347 / 295，解法代码取自 `day14-堆hot100.md`，只保留「简单思路 + 解法代码 + 面试最优解标注 + 复杂度」。

---

## 一、解题模型总述

堆类 Hot100 的核心套路是 **「用堆（优先队列）动态维护一个有序/极值集合」**，或 **「用 QuickSelect 代替全排序」**。横向规律三条口诀：

1. **Top-K 问题**：找第 K 大 / 前 K 频繁——要么 **小顶堆** 维护大小为 K 的集合（O(n log k)），要么 **桶排序**（频次范围可控时 O(n)），要么 **QuickSelect** 直接定位第 K 大（期望 O(n)）。注意题目若显式要求 O(n)，应优先 QuickSelect（215）。
2. **双堆求中位数（295）**：大顶堆存左半、小顶堆存右半，保持平衡，中位数由两堆顶决定——`addNum` O(log n)、`findMedian` O(1)。这是「堆维护数据流有序」的经典范式。
3. **堆 ≠ 万能**：很多「Top-K」题用桶/计数或单调队列（239）更高效，面试时按数据特征选结构（见速览表）。

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 面试最优解 | 时间 | 空间 |
|------|------|-----------|------|------|
| 215 | 数组中的第 K 大元素 | QuickSelect（小顶堆备选） | O(n) 期望 | O(1) |
| 347 | 前 K 个高频元素 | 桶排序（小顶堆备选） | O(n) | O(n) |
| 295 | 数据流的中位数 | 双堆（大顶 + 小顶） | add O(log n)/find O(1) | O(n) |

---

## 三、各题解法详情

### 1. 数组中的第 K 个最大元素（LeetCode 215 | 🟡 中等）
**简单思路**：第 k 大 = 升序第 `n-k` 个。QuickSelect 借 partition 分区，按枢轴位置只递归一半，随机枢轴保证期望 O(n)。

**解法一 · 快速选择（最优）**（取自 day14）：
```java
class Solution {
    private static final Random rand = new Random();
    public int findKthLargest(int[] nums, int k) {
        int n = nums.length, targetIndex = n - k;
        int left = 0, right = n - 1;
        while (true) {
            int pivotIdx = partition(nums, left, right);
            if (pivotIdx == targetIndex) return nums[pivotIdx];
            if (pivotIdx > targetIndex) right = pivotIdx - 1;
            else left = pivotIdx + 1;
        }
    }
    private int partition(int[] nums, int left, int right) {
        int i = left + rand.nextInt(right - left + 1);
        int pivot = nums[i];
        swap(nums, i, left);
        i = left + 1; int j = right;
        while (true) {
            while (i <= j && nums[i] < pivot) i++;
            while (i <= j && nums[j] > pivot) j--;
            if (i >= j) break;
            swap(nums, i, j); i++; j--;
        }
        swap(nums, left, j);
        return j;
    }
    private void swap(int[] nums, int a, int b) {
        int t = nums[a]; nums[a] = nums[b]; nums[b] = t;
    }
}
```
- 时间 O(n) 期望 / 最坏 O(n²)（随机化后极低）/ 空间 O(1)。

**解法二 · 小顶堆（源文档仅提及，未附代码）**：维护大小为 k 的小顶堆，遍历数组，堆顶即第 k 大，O(n log k)。

- ✅ 面试最优解：题目要求 O(n)，首选 **QuickSelect**（解法一）；若没要求 O(n) 或代码求稳，小顶堆更易写对。

---

### 2. 前 K 个高频元素（LeetCode 347 | 🟡 中等）
**简单思路**：HashMap 计数后以频次为下标建桶，从高到低取前 k 即 O(n)。

**对应解法 · 桶排序**（取自 day14）：
```java
class Solution {
    public int[] topKFrequent(int[] nums, int k) {
        Map<Integer, Integer> cnt = new HashMap<>();
        for (int x : nums) cnt.put(x, cnt.getOrDefault(x, 0) + 1);
        int maxCnt = Collections.max(cnt.values());
        List<Integer>[] buckets = new ArrayList[maxCnt + 1];
        Arrays.setAll(buckets, i -> new ArrayList<>());
        for (var e : cnt.entrySet()) buckets[e.getValue()].add(e.getKey());
        int[] ans = new int[k]; int j = 0;
        for (int i = maxCnt; j < k; i--)
            for (int x : buckets[i]) ans[j++] = x;
        return ans;
    }
}
```
> 对比：小顶堆（频率）O(n log k) 更通用；桶排序 O(n) 但需频次范围可控。

- ✅ 面试最优解：桶排序 O(n)（频次范围不大时）；否则小顶堆 O(n log k)。本解法与 `day11` 栈文件一致。
- 时间 O(n) / 空间 O(n)。

---

### 3. 数据流的中位数（LeetCode 295 | 🔴 困难）
**简单思路**：左半大顶堆 + 右半小顶堆，保持左堆 ≥ 右堆且至多多 1；奇数时左堆顶即中位数，偶数时两堆顶平均。

**对应解法 · 双堆**（取自 day14）：
```java
class MedianFinder {
    private final PriorityQueue<Integer> left = new PriorityQueue<>((a, b) -> b - a); // 大顶堆
    private final PriorityQueue<Integer> right = new PriorityQueue<>();              // 小顶堆
    public void addNum(int num) {
        if (left.size() == right.size()) {
            right.offer(num);            // 平衡时先进右，再弹最小到左（左 +1）
            left.offer(right.poll());
        } else {
            left.offer(num);             // 左多时先进左，再弹最大到右（平衡）
            right.offer(left.poll());
        }
    }
    public double findMedian() {
        if (left.size() > right.size()) return left.peek();
        return (left.peek() + right.peek()) / 2.0;
    }
}
```
- ✅ 面试最优解：双堆法，`addNum` O(log n)、`findMedian` O(1)，面试首选。注意偶数时 `/2.0`；大顶堆用 `(a,b)->b-a`。
- 时间 add O(log n) / find O(1) / 空间 O(n)。

---

## 四、模型缺口与提醒（基于现有 day 文档）

- **215**：小顶堆写法仅文字提及未附代码。
- **347**：小顶堆（频率）写法未附代码。
- 堆 Hot100 经典题如 **1046 最后一块石头的重量（大顶堆）**、**703 数据流中的第 K 大元素（小顶堆）**、**23 合并 K 个有序链表（小顶堆，已在 day5 链表总结收录）**、**373 查找和最小的 K 对数字** 等，源文档未单独收录（23 已在链表总结），建议后续按需补充。

*整理自 day14-堆hot100.md · 汇总日期：2026-08-17*
