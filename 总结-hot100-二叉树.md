# 总结 — 二叉树 Hot100

> 覆盖 LeetCode 94 / 104 / 226 / 101 / 543 / 102 / 108 / 98 / 230 / 199 / 114 / 105 / 437 / 236 / 124，解法代码取自 `day15`~`day18` 二叉树系列，只保留「简单思路 + 解法代码 + 面试最优解标注 + 复杂度」。

---

## 一、解题模型总述

二叉树 Hot100 的套路本质是「遍历 + 递归返回值设计」。横向规律五条口诀：

1. **三种 DFS 顺序（94 是基础）**：前序（根→左→右）、中序（左→根→右）、后序（左→右→根）。**调换三行代码位置即可互转**。后序（左右结果先算出来再处理当前节点）是「求子树信息再合并」类题（543 直径 / 124 路径和 / 236 LCA / 114 展开）的核心。
2. **全局变量 + 后序「返回单边贡献」**（543 / 124 / 114）：`dfs` 返回「向上能贡献的最大单边值」，同时在递归内用全局 `ans` 记录「经过当前节点的最优（含左右）值」。直径用边数（-1 技巧），路径和用和（负数截断为 0）。
3. **上下界 / 约束传递**（98 / 230 / 108）：BST 相关题靠「给每个节点传合法取值范围」或「利用中序=升序」解决；构建平衡 BST 取中间元素为根。
4. **BFS 层序**（102 / 199）：队列 + `int n = q.size()` 固定当前层边界，逐层处理。右视图本质就是层序取每层最后一个（也可用 DFS 根→右→左）。
5. **HashMap / 前缀和 / 分治重构**（105 / 437）：由遍历序列重建靠「先序/后序定根 + 中序分左右」；路径统计靠「前缀和 + 回溯」把 O(n²) 降到 O(n)。

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 面试最优解 | 时间 | 空间 |
|------|------|-----------|------|------|
| 94 | 二叉树的中序遍历 | 递归 DFS（迭代栈备选） | O(n) | O(h) |
| 104 | 二叉树的最大深度 | 递归分治 | O(n) | O(h) |
| 226 | 翻转二叉树 | 递归交换左右 | O(n) | O(h) |
| 101 | 对称二叉树 | 递归交叉比较 | O(n) | O(h) |
| 543 | 二叉树的直径 | 后序 DFS（-1 边数） | O(n) | O(h) |
| 102 | 二叉树的层序遍历 | 队列 BFS（双列表备选） | O(n) | O(n) |
| 108 | 有序数组转 BST | 递归取中分治 | O(n) | O(log n) |
| 98 | 验证二叉搜索树 | 递归上下界约束 | O(n) | O(h) |
| 230 | BST 第 K 小元素 | 中序遍历计数 + 剪枝 | O(k) | O(h) |
| 199 | 二叉树的右视图 | DFS 根右左 / BFS 取尾（二选一） | O(n) | O(h)/O(n) |
| 114 | 二叉树展开为链表 | 反向先序递归（迭代 O(1) 备选） | O(n) | O(h) |
| 105 | 前序+中序构造二叉树 | HashMap+指针 O(n)（数组拷贝备选） | O(n) | O(n) |
| 437 | 路径总和 III | 前缀和 + 回溯 | O(n) | O(n) |
| 236 | 二叉树的最近公共祖先 | 后序递归 | O(n) | O(h) |
| 124 | 二叉树最大路径和 | 后序 DFS + 全局最大 | O(n) | O(h) |

---

## 三、各题解法详情

### 1. 二叉树的中序遍历（LeetCode 94 | 🟢 简单）
**简单思路**：中序 = 左→根→右。递归最直观，调换三行位置即得前序/后序。

**对应解法**（取自 day15）：
```java
class Solution {
    public List<Integer> inorderTraversal(TreeNode root) {
        List<Integer> ans = new ArrayList<>();
        dfs(ans, root);
        return ans;
    }
    private void dfs(List<Integer> ans, TreeNode node) {
        if (node == null) return;
        dfs(ans, node.left);    // 左
        ans.add(node.val);      // 根
        dfs(ans, node.right);   // 右
    }
}
```
- ✅ 面试最优解：递归 DFS，三行调换即得三种顺序；迭代栈写法理解即可。
- 时间 O(n) / 空间 O(h)（最坏 O(n)）。

---

### 2. 二叉树的最大深度（LeetCode 104 | 🟢 简单）
**简单思路**：分治——当前深度 = `max(左子树深度, 右子树深度) + 1`，空节点返回 0。

**对应解法**（取自 day15）：
```java
class Solution {
    public int maxDepth(TreeNode root) {
        if (root == null) return 0;
        int ldepth = maxDepth(root.left);
        int rdepth = maxDepth(root.right);
        return Math.max(ldepth, rdepth) + 1;
    }
}
```
- ✅ 面试最优解：递归分治 O(h) 空间；也可 BFS 层序数层数。
- 时间 O(n) / 空间 O(h)。

---

### 3. 翻转二叉树（LeetCode 226 | 🟢 简单）
**简单思路**：每个节点递归翻转左右子树后交换它们。

**对应解法**（取自 day15）：
```java
class Solution {
    public TreeNode invertTree(TreeNode root) {
        if (root == null) return null;
        TreeNode left = invertTree(root.left);
        TreeNode right = invertTree(root.right);
        root.left = right;
        root.right = left;
        return root;
    }
}
```
- ✅ 面试最优解：递归交换，面试首选。
- 时间 O(n) / 空间 O(h)。

---

### 4. 对称二叉树（LeetCode 101 | 🟢 简单）
**简单思路**：比较左子树与右子树是否镜像——`p.left` 比 `q.right`，`p.right` 比 `q.left`，值相等且都对称。

**对应解法**（取自 day15）：
```java
class Solution {
    public boolean isSymmetric(TreeNode root) {
        return isSample(root.left, root.right);
    }
    private boolean isSample(TreeNode p, TreeNode q) {
        if (p == null || q == null) return p == q;   // 一个为 null 则必须都 null
        return p.val == q.val
            && isSample(p.left, q.right)
            && isSample(p.right, q.left);
    }
}
```
- ✅ 面试最优解：递归交叉比较，面试首选。易错点：`p==null||q==null` 时返回 `p==q`（都空才对称）。
- 时间 O(n) / 空间 O(h)。

---

### 5. 二叉树的直径（LeetCode 543 | 🟢 简单）
**简单思路**：直径 = 经过某节点的「左边数 + 右边数」。空节点返回 -1（使叶子到父为 1 条边），`dfs` 返回单边最大边数，全局 `ans` 记 `llen+rlen` 最大。

**对应解法**（取自 day16）：
```java
class Solution {
    private int ans;
    public int diameterOfBinaryTree(TreeNode root) {
        dfs(root);
        return ans;
    }
    private int dfs(TreeNode node) {
        if (node == null) return -1;            // 空节点 -1 + 1 = 0 条边
        int llen = dfs(node.left) + 1;
        int rlen = dfs(node.right) + 1;
        ans = Math.max(ans, llen + rlen);
        return Math.max(llen, rlen);
    }
}
```
- ✅ 面试最优解：后序 DFS + 全局变量，面试首选。注意空节点返回 -1（按边数计）。
- 时间 O(n) / 空间 O(h)。

---

### 6. 二叉树的层序遍历（LeetCode 102 | 🟡 中等）
**简单思路**：BFS 队列，每轮先用 `int n = q.size()` 固定当前层节点数，逐一出队收集值并压入子节点。

**解法一 · 队列 BFS**（取自 day16）：
```java
class Solution {
    public List<List<Integer>> levelOrder(TreeNode root) {
        if (root == null) return List.of();
        List<List<Integer>> ans = new ArrayList<>();
        Queue<TreeNode> q = new ArrayDeque<>();
        q.add(root);
        while (!q.isEmpty()) {
            int n = q.size();                     // 固定当前层节点数
            List<Integer> vals = new ArrayList<>(n);
            while (n-- > 0) {
                TreeNode node = q.poll();
                vals.add(node.val);
                if (node.left != null) q.add(node.left);
                if (node.right != null) q.add(node.right);
            }
            ans.add(vals);
        }
        return ans;
    }
}
```
- 时间 O(n) / 空间 O(n)。

**解法二 · 双列表法（源文档仅提及，未附代码）**：用 `cur/next` 两个 ArrayList 交替，不含队列，原理相同。

- ✅ 面试最优解：队列 BFS（解法一），`size()` 固定层边界最易写对；双列表法作对比。

---

### 7. 有序数组转换为二叉搜索树（LeetCode 108 | 🟢 简单）
**简单思路**：有序数组即 BST 中序。每次取中间元素为根，左右半区间递归建左右子树，自然平衡。

**对应解法**（取自 day16）：
```java
class Solution {
    public TreeNode sortedArrayToBST(int[] nums) {
        return dfs(nums, 0, nums.length);   // 左闭右开
    }
    private TreeNode dfs(int[] nums, int left, int right) {
        if (left == right) return null;
        int m = (left + right) / 2;          // 中间元素为根
        return new TreeNode(
            nums[m],
            dfs(nums, left, m),
            dfs(nums, m + 1, right)
        );
    }
}
```
- ✅ 面试最优解：递归取中分治，面试首选。易错点：右子树是 `dfs(m+1, right)` 否则死循环。
- 时间 O(n) / 空间 O(log n)。

---

### 8. 验证二叉搜索树（LeetCode 98 | 🟡 中等）
**简单思路**：BST 性质 = 左子树全 < 根 < 右子树全。递归给每个节点传合法范围 `(left, right)`，左子树收紧右界、右子树收紧左界。用 `long` 防边界击穿。

**对应解法**（取自 day16）：
```java
class Solution {
    public boolean isValidBST(TreeNode root) {
        return isValidBST(root, Long.MIN_VALUE, Long.MAX_VALUE);
    }
    private boolean isValidBST(TreeNode node, long left, long right) {
        if (node == null) return true;
        long x = node.val;
        return x > left && x < right
            && isValidBST(node.left, left, x)
            && isValidBST(node.right, x, right);
    }
}
```
- ✅ 面试最优解：上下界约束，面试首选。易错点：不能只比当前节点的左右孩子（需全局有序）；用 `long` 防 `Integer.MAX_VALUE` 击穿。
- 时间 O(n) / 空间 O(h)。

---

### 9. 二叉搜索树中第 K 小的元素（LeetCode 230 | 🟡 中等）
**简单思路**：BST 中序 = 升序，第 k 个访问的节点即第 k 小。计数器 `k--`，`k==0` 记录答案，`k<=0` 剪枝提前返回。

**对应解法**（取自 day16）：
```java
class Solution {
    private int ans, k;
    public int kthSmallest(TreeNode root, int k) {
        this.k = k;
        dfs(root);
        return ans;
    }
    private void dfs(TreeNode node) {
        if (node == null || k <= 0) return;
        dfs(node.left);
        if (--k == 0) ans = node.val;
        dfs(node.right);
    }
}
```
- ✅ 面试最优解：中序遍历 + 计数剪枝，面试首选（O(h) 空间）。若需 O(1) 空间+可重复查询，需 BST 增删改 + 维护子树大小（源文档未收录）。
- 时间 O(k)（最坏 O(n)）/ 空间 O(h)。

---

### 10. 二叉树的右视图（LeetCode 199 | 🟡 中等）
**简单思路**：右视图是每层最右侧节点。两种等价视角：DFS「根→右→左」每层第一个访问的节点；或 BFS 层序取每层最后一个。

**解法一 · DFS 根右左**（取自 day16）：
```java
class Solution {
    public List<Integer> rightSideView(TreeNode root) {
        List<Integer> ans = new ArrayList<>();
        dfs(root, 0, ans);
        return ans;
    }
    private void dfs(TreeNode root, int depth, List<Integer> ans) {
        if (root == null) return;
        if (depth == ans.size()) ans.add(root.val);  // 本层首个访问节点
        dfs(root.right, depth + 1, ans);
        dfs(root.left, depth + 1, ans);
    }
}
```
- 时间 O(n) / 空间 O(h)。

**解法二 · BFS 层序取最后**（取自 day16）：
```java
class Solution {
    public List<Integer> rightSideView(TreeNode root) {
        if (root == null) return List.of();
        List<Integer> ans = new ArrayList<>();
        List<TreeNode> cur = List.of(root);
        while (!cur.isEmpty()) {
            ans.add(cur.getLast().val);              // 当前层最后一个 = 右视图节点
            List<TreeNode> nxt = new ArrayList<>();
            for (TreeNode node : cur) {
                if (node.left != null) nxt.add(node.left);
                if (node.right != null) nxt.add(node.right);
            }
            cur = nxt;
        }
        return ans;
    }
}
```
- 时间 O(n) / 空间 O(n)。

- ✅ 面试最优解：DFS 根右左更简洁（O(h)）；BFS 更直观（O(n)）。二者任选，面试说清「每层最右」即可。

---

### 11. 二叉树展开为链表（LeetCode 114 | 🟡 中等）
**简单思路**：展开顺序 = 先序（根→左→右）。优雅做法：反向先序（右→左→根），用全局 `head` 从尾部向前串成链表。

**解法一 · 反向先序递归**（取自 day16）：
```java
class Solution {
    private TreeNode head;
    public void flatten(TreeNode root) {
        if (root == null) return;
        flatten(root.right);   // 先右
        flatten(root.left);    // 再左
        root.left = null;
        root.right = head;     // 右指针指向已构建链表头
        head = root;
    }
}
```
- 时间 O(n) / 空间 O(h)。

**解法二 · 迭代 O(1) 空间**（取自 day16）：
```java
class Solution {
    public void flatten(TreeNode root) {
        TreeNode cur = root;
        while (cur != null) {
            if (cur.left != null) {
                TreeNode a = cur.left;
                while (a.right != null) a = a.right;  // 左子树最右节点
                a.right = cur.right;                   // 接右子树
                cur.right = cur.left;
                cur.left = null;
            }
            cur = cur.right;
        }
    }
}
```
- 时间 O(n) / 空间 O(1)。

- ✅ 面试最优解：递归反向先序（解法一）最易写对，面试首选；若追问 O(1) 空间用迭代版（解法二）。易错点：必须先 flatten 右再左。

---

### 12. 从前序与中序遍历序列构造二叉树（LeetCode 105 | 🟡 中等）
**简单思路**：先序首元素为根；在中序找根位置得左子树大小，从而切分先序/中序，递归建树。

**解法一 · 数组拷贝基础版**（取自 day17）：
```java
class Solution {
    public TreeNode buildTree(int[] preorder, int[] inorder) {
        int n = preorder.length;
        if (n == 0) return null;
        int leftsize = indexOf(inorder, preorder[0]);
        int[] pre1 = Arrays.copyOfRange(preorder, 1, 1 + leftsize);
        int[] pre2 = Arrays.copyOfRange(preorder, 1 + leftsize, n);
        int[] in1  = Arrays.copyOfRange(inorder, 0, leftsize);
        int[] in2  = Arrays.copyOfRange(inorder, 1 + leftsize, n);
        return new TreeNode(preorder[0], buildTree(pre1, in1), buildTree(pre2, in2));
    }
    private int indexOf(int[] a, int x) {
        for (int i = 0; ; i++) if (a[i] == x) return i;
    }
}
```
- 时间 O(n²) / 空间 O(n²)（拷贝数组）。

**解法二 · HashMap + 指针 O(n)**（取自 day17）：
```java
class Solution {
    public TreeNode buildTree(int[] preorder, int[] inorder) {
        int n = preorder.length;
        Map<Integer, Integer> index = HashMap.newHashMap(n);
        for (int i = 0; i < n; i++) index.put(inorder[i], i);
        return dfs(0, n, 0, preorder, index);
    }
    private TreeNode dfs(int preL, int preR, int inL,
                         int[] preorder, Map<Integer, Integer> index) {
        if (preL == preR) return null;
        int leftSize = index.get(preorder[preL]) - inL;
        TreeNode left = dfs(preL + 1, preL + 1 + leftSize, inL, preorder, index);
        TreeNode right = dfs(preL + 1 + leftSize, preR, inL + 1 + leftSize, preorder, index);
        return new TreeNode(preorder[preL], left, right);
    }
}
```
- 时间 O(n) / 空间 O(n)。

- ✅ 面试最优解：HashMap + 指针 O(n)（解法二），面试首选（避免 O(n²)）；基础数组拷贝版便于理解，可作对比。同源思路可推广到「中序+后序」构造（根取后序末位）。

---

### 13. 路径总和 III（LeetCode 437 | 🟡 中等）
**简单思路**：路径可任意起止但必须向下。把「根到当前节点前缀和 s」存入 HashMap，以当前节点为终点、满足 `s - targetSum` 曾出现过的次数即路径数；离开分支时 `cnt[s]--` 回溯。

**对应解法**（取自 day18）：
```java
class Solution {
    private int ans;
    public int pathSum(TreeNode root, int targetSum) {
        Map<Long, Integer> cnt = new HashMap<>();
        cnt.put(0L, 1);                       // 空路径前缀和 0
        dfs(root, 0, targetSum, cnt);
        return ans;
    }
    private void dfs(TreeNode node, long s, int targetSum, Map<Long, Integer> cnt) {
        if (node == null) return;
        s += node.val;
        ans += cnt.getOrDefault(s - targetSum, 0);
        cnt.put(s, cnt.getOrDefault(s, 0) + 1);
        dfs(node.left, s, targetSum, cnt);
        dfs(node.right, s, targetSum, cnt);
        int c = cnt.getOrDefault(s, 0) - 1;   // 回溯
        if (c == 0) cnt.remove(s); else cnt.put(s, c);
    }
}
```
- ✅ 面试最优解：前缀和 + 回溯，O(n) 时间，面试首选。思路与数组 560 完全一致（数组→树）。用 `long` 防溢出；`cnt[0]=1` 使以根起点的路径可统计。
- 时间 O(n) / 空间 O(n)。

---

### 14. 二叉树的最近公共祖先（LeetCode 236 | 🟡 中等）
**简单思路**：后序——左右子树各找一次 p/q，左右都找到则当前节点是 LCA；只在一侧则返回那侧（两目标同侧）。遇 `null/p/q` 提前返回。

**对应解法**（取自 day18）：
```java
class Solution {
    public TreeNode lowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) {
        if (root == null || root == p || root == q) return root;
        TreeNode left = lowestCommonAncestor(root.left, p, q);
        TreeNode right = lowestCommonAncestor(root.right, p, q);
        if (left != null && right != null) return root;  // 一边一个 = 当前根
        return left != null ? left : right;
    }
}
```
- ✅ 面试最优解：后序递归，面试首选。注意 p 在 q 子树中时递归到 p 即返回（p 自身即 LCA）。
- 时间 O(n) / 空间 O(h)。

---

### 15. 二叉树中的最大路径和（LeetCode 124 | 🔴 困难）
**简单思路**：与 543 同模板。每个节点当「拐点」：路径和 = `max(0,lVal)+max(0,rVal)+val`，更新全局 `ans`；向上返回 `max(max(l,r)+val, 0)`（负数不贡献）。`ans` 初值 `Integer.MIN_VALUE`（全负情形）。

**对应解法**（取自 day18）：
```java
class Solution {
    private int ans = Integer.MIN_VALUE;
    public int maxPathSum(TreeNode root) {
        dfs(root);
        return ans;
    }
    private int dfs(TreeNode node) {
        if (node == null) return 0;
        int lVal = dfs(node.left);
        int rVal = dfs(node.right);
        ans = Math.max(ans, lVal + rVal + node.val);     // 拐点处的最优
        return Math.max(Math.max(lVal, rVal) + node.val, 0);
    }
}
```
- ✅ 面试最优解：后序 DFS + 全局最大，面试首选。易错点：与 543 区别——这里求和、空返回 0、负数贡献截断为 0；`ans` 初值不能设 0（全负）。
- 时间 O(n) / 空间 O(h)。

---

## 四、模型缺口与提醒（基于现有 day 文档）

- **94 中序遍历**：源文档仅递归版，迭代栈写法（用显式栈模拟）未附代码，面试时如需可补。
- **102 层序遍历**：双列表法仅文字提及，未附代码。
- 其余各题源文档解法已较完整；BST 相关（98/230/108）均已覆盖，未涉及「BST 维护子树大小做 O(1) 第 K 小查询」等进阶变体（源文档未收录）。

*整理自 day15-二叉树hot100-part1.md、day16-二叉树hot100-part2.md、day17-二叉树hot100-part3.md、day18-二叉树hot100-part4.md · 汇总日期：2026-08-17*
