# 总结 — 栈 Hot100

> 覆盖 LeetCode 20 / 155 / 394 / 739 / 84 / 239 / 347（Hot100），并附带 232 / 225 / 1047 / 150（代码随想录基础栈操作）。解法代码取自 `day11`~`day13`，只保留「简单思路 + 解法代码 + 面试最优解标注 + 复杂度」。

---

## 一、解题模型总述

栈类 Hot100 分两派：**「栈当辅助结构做匹配 / 展开」** 与 **「单调栈找边界」**。横向规律四条口诀：

1. **栈做匹配 / 消除 / 展开**（20 / 1047 / 394）：括号匹配存「期望的右括号」；相邻重复删除遇相同就消；字符串解码遇 `[` 存现场、`]` 展开嵌套——核心是「后进先出天然处理嵌套与对称」。
2. **单调栈找边界**（739 / 84 / 42）：维护栈内单调性，新元素破坏单调时弹出栈顶并结算。找「下一个更大」用**递减栈**；找「左右第一个更小」用**递增栈**。`84` 用哨兵栈 + 单遍扫描最精简。
3. **特殊栈设计**（155 最小栈）：每个元素打包「当前最小值」或用辅助栈，使 `getMin` 常数时间。
4. **单调队列 / 堆是栈的近亲**（239 / 347）：滑动窗口最大值用**单调递减双端队列**（存下标）；前 K 高频用**桶排序** O(n)（或堆 O(n log k)）。严格说它们是队列/堆，但常与栈一起训练，故列入。

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 面试最优解 | 时间 | 空间 |
|------|------|-----------|------|------|
| 20 | 有效的括号 | 栈存期望右括号 | O(n) | O(n) |
| 155 | 最小栈 | 辅助栈 / 二元组 | O(1) 均摊 | O(n) |
| 394 | 字符串解码 | 单栈存 Pair | O(n) | O(n) |
| 739 | 每日温度 | 单调递减栈 | O(n) | O(n) |
| 84 | 柱状图最大矩形 | 单调栈（哨兵单遍） | O(n) | O(n) |
| 239 | 滑动窗口最大值 | 单调递减双端队列 | O(n) | O(k) |
| 347 | 前 K 个高频元素 | 桶排序（堆备选） | O(n) | O(n) |

---

## 三、各题解法详情

### 1. 有效的括号（LeetCode 20 | 🟢 简单）
**简单思路**：遇左括号把对应右括号入栈，遇右括号弹出对比；栈空（右多）/ 不匹 / 结束栈非空（左多）则 false。

**对应解法**（取自 day12）：
```java
class Solution {
    public boolean isValid(String s) {
        Deque<Character> deque = new LinkedList<>();
        for (int i = 0; i < s.length(); i++) {
            char ch = s.charAt(i);
            if (ch == '(') deque.push(')');
            else if (ch == '[') deque.push(']');
            else if (ch == '{') deque.push('}');
            else if (deque.isEmpty() || ch != deque.pop()) return false;
        }
        return deque.isEmpty();
    }
}
```
- ✅ 面试最优解：栈存期望右括号，最易写对。用 `Deque` 不用 `Stack`（后者同步类性能差）。
- 时间 O(n) / 空间 O(n)。

---

### 2. 最小栈（LeetCode 155 | 🟡 中等）
**简单思路**：栈中存二元组 `[值, 入栈时刻最小值]`，`getMin` 直接取栈顶最小值；哨兵 `[0, MAX]` 让栈永不空省判空。

**对应解法**（取自 day12）：
```java
class MinStack {
    private final Deque<int[]> st = new ArrayDeque<>();
    public MinStack() { st.push(new int[]{0, Integer.MAX_VALUE}); }
    public void push(int value) {
        st.push(new int[]{value, Math.min(getMin(), value)});
    }
    public void pop() { st.pop(); }
    public int top() { return st.peek()[0]; }
    public int getMin() { return st.peek()[1]; }
}
```
- ✅ 面试最优解：二元组辅助栈，所有操作 O(1)。也可用「主栈 + 辅助最小栈」（当前值 ≤ 辅助栈顶才压入，弹出不匹配则辅助栈不弹）。
- 时间 O(1) / 空间 O(n)。

---

### 3. 字符串解码（LeetCode 394 | 🟡 中等）
**简单思路**：嵌套 `k[...]` 用栈处理。遇 `[` 把当前已拼字符串和 k 存栈、重置；遇 `]` 弹出 Pair，把当前 res 重复 k 次拼回上一层。`k*10+d` 处理多位数。

**对应解法**（取自 day12）：
```java
class Solution {
    private record Pair(String s, int k) {}
    public String decodeString(String s) {
        Deque<Pair> stack = new ArrayDeque<>();
        StringBuilder res = new StringBuilder();
        int k = 0;
        for (char c : s.toCharArray()) {
            if (Character.isLetter(c)) res.append(c);
            else if (Character.isDigit(c)) k = k * 10 + (c - '0');
            else if (c == '[') { stack.push(new Pair(res.toString(), k)); res.setLength(0); k = 0; }
            else { Pair p = stack.pop(); res = new StringBuilder(p.s).repeat(res, p.k); }
        }
        return res.toString();
    }
}
```
- ✅ 面试最优解：单栈存 Pair，面试首选。也可双栈（数字栈 + 字符串栈）等价。
- 时间 O(n) / 空间 O(n)。

---

### 4. 每日温度（LeetCode 739 | 🟡 中等）
**简单思路**：找「下一个更高温度」，单调递减栈存下标；新温度 > 栈顶对应温度时弹出结算天数差 `i-j`。

**对应解法**（取自 day13）：
```java
class Solution {
    public int[] dailyTemperatures(int[] temperatures) {
        int n = temperatures.length;
        int[] ans = new int[n];
        Deque<Integer> st = new ArrayDeque<>();
        for (int i = 0; i < n; i++) {
            int t = temperatures[i];
            while (!st.isEmpty() && t > temperatures[st.peek()]) {
                int j = st.pop();
                ans[j] = i - j;
            }
            st.push(i);
        }
        return ans;
    }
}
```
- ✅ 面试最优解：单调递减栈 O(n)，面试首选。同源可推广到「下一个更大元素 I/II」。
- 时间 O(n) / 空间 O(n)。

---

### 5. 柱状图中最大的矩形（LeetCode 84 | 🔴 困难）
**简单思路**：以 `heights[i]` 为高的最大矩形 = `高 × (右边界 - 左边界 - 1)`，左右边界是第一个更矮的柱子。单调递增栈找边界。

**解法一 · 两遍扫描（left/right 数组）**（取自 day13）：
```java
class Solution {
    public int largestRectangleArea(int[] heights) {
        int n = heights.length;
        int[] left = new int[n], right = new int[n];
        Deque<Integer> st = new ArrayDeque<>();
        for (int i = 0; i < n; i++) {                 // 左边界
            while (!st.isEmpty() && heights[st.peek()] >= heights[i]) st.pop();
            left[i] = st.isEmpty() ? -1 : st.peek();
            st.push(i);
        }
        st.clear();
        for (int i = n - 1; i >= 0; i--) {            // 右边界
            while (!st.isEmpty() && heights[st.peek()] >= heights[i]) st.pop();
            right[i] = st.isEmpty() ? n : st.peek();
            st.push(i);
        }
        int ans = 0;
        for (int i = 0; i < n; i++) ans = Math.max(ans, heights[i] * (right[i] - left[i] - 1));
        return ans;
    }
}
```

**解法二 · 哨兵栈单遍扫描（最推荐）**（取自 day13）：
```java
class Solution {
    public int largestRectangleArea(int[] heights) {
        int n = heights.length;
        Deque<Integer> st = new ArrayDeque<>();
        st.push(-1);                                  // 哨兵，代替 left=-1 判空
        int ans = 0;
        for (int right = 0; right <= n; right++) {
            int h = right < n ? heights[right] : -1;  // 虚拟高度 -1 强制清栈
            while (st.size() > 1 && heights[st.peek()] >= h) {
                int i = st.pop();
                int left = st.peek();
                ans = Math.max(ans, heights[i] * (right - left - 1));
            }
            st.push(right);
        }
        return ans;
    }
}
```
- ✅ 面试最优解：哨兵栈单遍扫描（解法二）—— 栈底 `-1` 省判空、遍历到 `n` 用虚拟高度 `-1` 清栈，代码最精简、O(1) 额外数组。两遍扫描版便于理解。
- 时间 O(n) / 空间 O(n)。

---

### 6. 滑动窗口最大值（LeetCode 239 | 🔴 困难）
**简单思路**：单调递减双端队列存下标，新元素入队时清掉所有 ≤ 它的队尾（不再是候选），队首下标 < 左边界则过期出队；窗口形成后队首即最大值。

**对应解法**（取自 day11）：
```java
class Solution {
    public int[] maxSlidingWindow(int[] nums, int k) {
        int n = nums.length;
        int[] ans = new int[n - k + 1];
        Deque<Integer> deque = new LinkedList<>();
        for (int i = 0; i < n; i++) {
            while (!deque.isEmpty() && nums[deque.getLast()] <= nums[i]) deque.removeLast();
            deque.addLast(i);
            int left = i - k + 1;
            if (deque.getFirst() < left) deque.removeFirst();
            if (left >= 0) ans[left] = nums[deque.getFirst()];
        }
        return ans;
    }
}
```
- ✅ 面试最优解：单调双端队列 O(n)，面试首选。严格属「队列」但常与栈同训。注意存下标而非值（需判断过期）。
- 时间 O(n) / 空间 O(k)。

---

### 7. 前 K 个高频元素（LeetCode 347 | 🟡 中等）
**简单思路**：HashMap 计数后，以频次为下标建桶，从高到低取前 k 个即 O(n)。

**对应解法 · 桶排序**（取自 day11）：
```java
class Solution {
    public int[] topKFrequent(int[] nums, int k) {
        Map<Integer, Integer> cnt = new HashMap<>();
        for (int x : nums) cnt.put(x, cnt.getOrDefault(x, 0) + 1);
        int maxCnt = Collections.max(cnt.values());
        List<Integer>[] buckets = new ArrayList[maxCnt + 1];
        Arrays.setAll(buckets, i -> new ArrayList<>());
        for (var e : cnt.entrySet()) buckets[e.getValue()].add(e.getKey());
        int[] ans = new int[k]; int j = 0;
        for (int i = maxCnt; j < k; i--)
            for (int x : buckets[i]) ans[j++] = x;
        return ans;
    }
}
```
> 对比：优先队列（小顶堆）O(n log k) 更通用；桶排序 O(n) 但需频次范围可控。

- ✅ 面试最优解：桶排序 O(n)（频次范围不大时）；否则小顶堆 O(n log k)。两者均可。
- 时间 O(n) / 空间 O(n)。

---

## 四、基础栈操作（代码随想录 drills，非 Hot100）

源文档 `day11` 还包含以下基础栈/队列练习，作为 Hot100 的前置训练，解法已收录在 day11：

- **232 用栈实现队列**：双栈（输入栈收、输出栈空了才倒，惰性倒换），均摊 O(1)。
- **225 用队列实现栈**：单队列 + push 时旋转（新元素入队后把前 n-1 个移到后面），pop/top O(1)、push O(n)。
- **1047 删除相邻重复项**：栈顶相同就消、不同就压，O(n)。
- **150 逆波兰表达式求值**：数字入栈、运算符弹两个算（注意 `b-a`/`b/a` 顺序），O(n)。

---

## 五、模型缺口与提醒（基于现有 day 文档）

- **栈 Hot100 经典题**如 **402 移掉 K 位数字（单调栈贪心）**、**456 132 模式**、**224/227 基本计算器（栈 + 运算符优先级）** 等源文档未收录，建议后续补充。
- 84 已有三版写法（两遍 / 一遍 / 哨兵），面试首选哨兵版；239/347 同时归入「队列 / 堆」视角，本总结按源文件归属列入。

*整理自 day11-栈与队列代码随想录.md、day12-栈hot100-part1.md、day13-栈hot100-part2.md · 汇总日期：2026-08-17*
