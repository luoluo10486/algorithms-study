# 总结 — 图论 Hot100

> 覆盖 LeetCode 200 / 994 / 207 / 208，解法代码取自 `day21-图论hot100.md`，只保留「简单思路 + 解法代码 + 面试最优解标注 + 复杂度」。

---

## 一、解题模型总述

图论 Hot100 在 LeetCode 里主要分两块：**网格图（DFS/BFS 沉没/扩散）** 与 **抽象图（拓扑排序判环 / 字典树）**。横向规律三条口诀：

1. **网格即图，连通块用「沉没 / 扩散」**：200 岛屿数量 = 遇陆地 DFS 把整座岛沉掉计数（Flood Fill）；994 腐烂橘子 = 多源 BFS 逐层扩散，层数即分钟数。两者的核心都是「标记已访问 + 四方向扩散」，区别在单源计数 vs 多源求最短路（扩散时间）。
2. **有向图判环 = 能否拓扑排序**：207 课程表把依赖建成有向边 `b→a`，能用 DFS 三色（白/灰/黑）判环，也能用 BFS Kahn（入度剥层）。有环 = 学不完 = false。
3. **Trie（208）是树形结构**：多叉树（26 孩子 + end 标记），`insert` 沿路建节点、`search` 要走到末尾且 end、`startsWith` 走到末尾即可——用「三态 find」统一两查询。

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 面试最优解 | 时间 | 空间 |
|------|------|-----------|------|------|
| 200 | 岛屿数量 | DFS 沉没法（BFS 备选） | O(mn) | O(mn) |
| 994 | 腐烂的橘子 | 多源 BFS | O(mn) | O(mn) |
| 207 | 课程表 | DFS 三色 / BFS Kahn（二选一） | O(V+E) | O(V+E) |
| 208 | 实现 Trie | 多叉树 + 三态 find | O(L) | O(ΣL) |

---

## 三、各题解法详情

### 1. 岛屿数量（LeetCode 200 | 🟡 中等）
**简单思路**：遍历网格，遇 `'1'` 就 DFS 把整座岛相邻的 `'1'` 全标记为 `'2'`（沉没），同时岛屿计数 +1。标记与访问分离避免重复计数。

**解法一 · DFS 沉没法**（取自 day21）：
```java
class Solution {
    public int numIslands(char[][] grid) {
        int ans = 0;
        for (int i = 0; i < grid.length; i++)
            for (int j = 0; j < grid[0].length; j++)
                if (grid[i][j] == '1') {
                    dfs(grid, i, j);   // 沉没整座岛
                    ans++;
                }
        return ans;
    }
    private void dfs(char[][] grid, int i, int j) {
        if (i < 0 || i >= grid.length || j < 0 || j >= grid[0].length || grid[i][j] != '1') return;
        grid[i][j] = '2';               // 标记为已访问
        dfs(grid, i, j - 1); dfs(grid, i, j + 1);
        dfs(grid, i - 1, j); dfs(grid, i + 1, j);
    }
}
```
- 时间 O(mn) / 空间 O(mn)（最坏递归栈）。

**解法二 · BFS 沉没（源文档仅提及，未附代码）**：用队列替代递归 DFS，效果相同，可避免深网格爆栈。

- ✅ 面试最优解：DFS 沉没法最直观，面试首选；BFS 版在网格极大时更稳。

---

### 2. 腐烂的橘子（LeetCode 994 | 🟡 中等）
**简单思路**：多源 BFS——把所有初始腐烂橘子入队，逐层向四方向扩散，「新鲜→腐烂」并 `fresh--`，扩散层数即分钟数；结束仍有 fresh 返回 -1。

**对应解法**（取自 day21）：
```java
class Solution {
    private static final int[][] DIRECTION = {{-1,0},{1,0},{0,-1},{0,1}};
    public int orangesRotting(int[][] grid) {
        int m = grid.length, n = grid[0].length, fresh = 0;
        List<int[]> q = new ArrayList<>();
        for (int i = 0; i < m; i++)
            for (int j = 0; j < n; j++) {
                if (grid[i][j] == 1) fresh++;
                else if (grid[i][j] == 2) q.add(new int[]{i, j});
            }
        int ans = 0;
        while (fresh > 0 && !q.isEmpty()) {
            ans++;
            List<int[]> temp = q;
            q = new ArrayList<>();
            for (int[] pos : temp)
                for (int[] d : DIRECTION) {
                    int i = pos[0] + d[0], j = pos[1] + d[1];
                    if (i >= 0 && i < m && j >= 0 && j < n && grid[i][j] == 1) {
                        fresh--; grid[i][j] = 2; q.add(new int[]{i, j});
                    }
                }
        }
        return fresh > 0 ? -1 : ans;
    }
}
```
- ✅ 面试最优解：多源 BFS（用 `temp/q` 列表交替天然分层，等价于队列 + `size()`），面试首选。注意 `fresh==0` 时直接返回 0。
- 时间 O(mn) / 空间 O(mn)。

---

### 3. 课程表（LeetCode 207 | 🟡 中等）
**简单思路**：依赖建图 `g[b].add(a)`（先修 b 才能学 a）。能否修完 = 有向图有无环。DFS 三色：白色未访问、灰色在当前路径、黑色已安全；遇灰色邻居即环。

**解法一 · DFS 三色标记**（取自 day21）：
```java
class Solution {
    public boolean canFinish(int numCourses, int[][] prerequisites) {
        List<Integer>[] g = new ArrayList[numCourses];
        Arrays.setAll(g, i -> new ArrayList<>());
        for (int[] p : prerequisites) g[p[1]].add(p[0]);  // b → a
        int[] colors = new int[numCourses];               // 0白 1灰 2黑
        for (int i = 0; i < numCourses; i++)
            if (colors[i] == 0 && dfs(i, g, colors)) return false;
        return true;
    }
    private boolean dfs(int x, List<Integer>[] g, int[] colors) {
        colors[x] = 1;                                     // 染灰
        for (int y : g[x]) {
            if (colors[y] == 1 || (colors[y] == 0 && dfs(y, g, colors))) return true;
        }
        colors[x] = 2;                                     // 染黑
        return false;
    }
}
```
- 时间 O(V+E) / 空间 O(V+E)。

**解法二 · BFS Kahn 拓扑排序（源文档仅提及，未附代码）**：算每个节点入度，入度 0 入队逐层剥；剥完节点数 < 总课程数则有环。不用递归，更不易爆栈。

- ✅ 面试最优解：DFS 三色更直观；若图深怕爆栈，用 BFS Kahn。两者均可，说清「有环即学不完」即可。注意代码里 `&&` 优先级高于 `||`，建议加括号提升可读性（源文档已提醒）。

---

### 4. 实现 Trie（前缀树）（LeetCode 208 | 🟡 中等）
**简单思路**：每个节点挂 26 个孩子 + `end` 标记（从根到此是否完整单词）。`insert` 沿路建节点、`search` 要求走到末尾且 end、`startsWith` 走到末尾即可。三态 `find`（0=路径断 / 1=非单词 / 2=完整单词）统一两查询。

**对应解法**（取自 day21）：
```java
class Trie {
    private static class Node {
        Node[] son = new Node[26];
        boolean end = false;
    }
    private final Node root = new Node();

    public void insert(String word) {
        Node cur = root;
        for (char c : word.toCharArray()) {
            c -= 'a';
            if (cur.son[c] == null) cur.son[c] = new Node();
            cur = cur.son[c];
        }
        cur.end = true;
    }
    public boolean search(String word) { return find(word) == 2; }
    public boolean startsWith(String prefix) { return find(prefix) != 0; }
    private int find(String word) {
        Node cur = root;
        for (char c : word.toCharArray()) {
            c -= 'a';
            if (cur.son[c] == null) return 0;
            cur = cur.son[c];
        }
        return cur.end ? 2 : 1;
    }
}
```
- ✅ 面试最优解：多叉树 + 三态 find，面试首选。`search` 与 `startsWith` 差异全在 `end` 标记。每个操作 O(L)（与字典大小无关，这是 Trie 优于哈希表的场景）；空间 O(ΣL)。
- 时间 O(L) / 空间 O(ΣL)。

---

## 四、模型缺口与提醒（基于现有 day 文档）

- **200 / 207**：BFS 写法（队列沉没 / Kahn 算法）仅文字提及未附代码，面试如需可补。
- 图论 Hot100 还有多源 BFS 进阶（如 542 01 矩阵）、并查集类（如 130 被围绕的区域、547 省份数量）、最短路（Dijkstra / 743 网络延迟时间）等，源文档未收录，待用户后续补充。

*整理自 day21-图论hot100.md · 汇总日期：2026-08-17*
