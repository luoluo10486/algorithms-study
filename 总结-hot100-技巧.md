# 总结 — 技巧 Hot100

> 覆盖 LeetCode 136 / 169 / 75 / 31 / 287（Hot100），解法代码取自 `day33-技巧hot100-part1.md`、`day34-技巧hot100-part2.md`。「技巧」模块是杂项妙招集合（位运算、投票抵消、原地三指针、字典序、链表化判圈），只保留「简单思路 + 解法代码 + 面试最优解标注 + 复杂度」。（注：原列表中的 208 字典树已归入「图论」总结。）

---

## 一、解题模型总述

技巧题的共同点是**用一条巧妙的不变量 / 等价转化把复杂度压到 O(n)/O(1)**，识别特征是「要求 O(1) 空间」或「找某个特殊数」。横向规律五条口诀：

1. **位运算抵消（136）**：`x^x=0`、`x^0=x`、满足交换/结合律 —— 出现两次的数异或归零，单身的留下。O(n)/O(1)。
2. **摩尔投票抵消（169）**：多数元素 > n/2，必比其余总和还多 —— `hp==0` 立候选、`x==ans` +1、`x!=ans` -1，过半者永不死。O(n)/O(1)。
3. **双指针原地分段（75）**：荷兰国旗问题，把数组劈成 0/1/2 三段 —— 默认当 2、`x<=1` 在 p1 补 1、`x==0` 在 p0 升级 0，逐级升级维持不变量 `p0≤p1≤i`。一趟 O(n)/O(1)。
4. **字典序三步走（31）**：下一个排列 = 找最右上升点 i → 右侧首个比它大的 j 交换 → 反转 i+1..末尾（i<0 即整体反转成最小）。原地 O(n)。
5. **数组当链表判圈（287）**：值∈[1,n]、长 n+1 → 鸽巢原理必有环；把 `i→nums[i]` 当链表，Floyd 快慢指针 Phase1 相遇、Phase2 一指针回起点同步走，碰头处即环入口 = 重复数。不改数组 + O(1) 空间。

> 一句话：**技巧 = 抵消（异或/投票）、原地分段（荷兰国旗）、字典序（下一个排列）、链表化判圈（Floyd）—— 都是 O(n)/O(1) 的巧思。**

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 技巧类型 | 面试最优解 | 时间 | 空间 |
|------|------|----------|-----------|------|------|
| 136 | 只出现一次的数字 | 位运算（异或） | 全员异或 | O(n) | O(1) |
| 169 | 多数元素 | 摩尔投票 | Boyer–Moore 投票 | O(n) | O(1) |
| 75 | 颜色分类 | 双指针（荷兰国旗） | 默认当 2 逐级升级 | O(n) | O(1) |
| 31 | 下一个排列 | 字典序 | 三步走（找点/换/反转） | O(n) | O(1) |
| 287 | 寻找重复数 | 快慢指针判圈 | Floyd 龟兔赛跑 | O(n) | O(1) |

> 五题在源文档中均为单解法，已全部列出。

---

## 三、各题解法详情

### 1. 只出现一次的数字（LeetCode 136 | 🟢 简单）
**简单思路**：异或三性质 `x^x=0`、`x^0=x`、交换/结合律。`ans` 初值 0，遍历 `ans ^= x` —— 成对抵消，单身者留下。

**对应解法**（取自 day33）：
```java
class Solution {
    public int singleNumber(int[] nums) {
        int ans = 0;
        for (int x : nums) ans ^= x;   // 成对归零，单身留下
        return ans;
    }
}
```
- ✅ 面试最优解：异或 O(n)/O(1)，完美满足「线性时间 + 常数空间」。前提：每个数恰出现两次、仅一个出现一次。时间 O(n) / 空间 O(1)。

---

### 2. 多数元素（LeetCode 169 | 🟢 简单）
**简单思路**：多数元素出现 > n/2，比其他总和还多。维护候选 `ans` 与存活票 `hp`：`hp==0` 立候选、`x==ans` +1、`x!=ans` -1（抵消）。过半者永不被清零，最后存活即答案。

**对应解法**（取自 day33）：
```java
class Solution {
    public int majorityElement(int[] nums) {
        int ans = 0, hp = 0;
        for (int x : nums) {
            if (hp == 0) { ans = x; hp = 1; }      // 立候选
            else hp += x == ans ? 1 : -1;          // 同 +1，异 -1
        }
        return ans;
    }
}
```
- ✅ 面试最优解：摩尔投票 O(n)/O(1)。本题保证必存在多数元素，无需二次验证；若不确定（如 229 找 >n/3 的所有众数）投票后需再扫一遍验证。时间 O(n) / 空间 O(1)。

---

### 3. 颜色分类（LeetCode 75 | 🟡 中等）
**简单思路**：荷兰国旗问题，原地分 0/1/2 三段。默认把 `nums[i]` 当 2 兜底；`x<=1` 时先在 `p1` 处补一个 1；`x==0` 时再把 `p0` 处升级成 0。不变量 `[0,p0)=0`、`[p0,p1)=1`、`[p1,i]=2` 且 `p0≤p1≤i`。

**对应解法**（取自 day33）：
```java
class Solution {
    public void sortColors(int[] nums) {
        int p0 = 0, p1 = 0;
        for (int i = 0; i < nums.length; i++) {
            int x = nums[i];          // 必须先取出
            nums[i] = 2;              // 默认当 2（兜底）
            if (x <= 1) nums[p1++] = 1;   // 0/1：先在 p1 补 1
            if (x == 0) nums[p0++] = 0;   // 是 0：再把 p0 升级成 0（维持 1 总数）
        }
    }
}
```
- ✅ 面试最优解：双指针一趟 O(n)/O(1)。`x==0` 的「先补 1 再置 0」是为维持 1 的数量不丢。对比写法：三指针 `p0/p2` 交换法更直观但需 swap。时间 O(n) / 空间 O(1)。

---

### 4. 下一个排列（LeetCode 31 | 🟡 中等）
**简单思路**：字典序只大一点点 —— ① 从右找首个上升点 `i`（`nums[i]<nums[i+1]`）；② 在 `i+1..末尾` 降序段从右找首个 `>nums[i]` 的 `j` 交换；③ 反转 `i+1..末尾`（`i<0` 即整体反转成最小）。

**对应解法**（取自 day34）：
```java
class Solution {
    public void nextPermutation(int[] nums) {
        int n = nums.length, i = n - 2;
        while (i >= 0 && nums[i] >= nums[i + 1]) i--;   // Step1 找上升点
        if (i >= 0) {
            int j = n - 1;
            while (nums[j] <= nums[i]) j--;             // Step2 找最小可换大数
            swap(nums, i, j);
        }
        reverse(nums, i + 1, n - 1);                    // Step3 反转后缀（i<0 翻成最小）
    }
    private void swap(int[] a, int i, int j) { int t = a[i]; a[i] = a[j]; a[j] = t; }
    private void reverse(int[] a, int l, int r) {
        while (l < r) swap(a, l++, r--);
    }
}
```
- ✅ 面试最优解：标准三步走，原地 O(n)。`i<0` 时 `reverse(0,n-1)` 把最大排列翻成最小，逻辑自洽。时间 O(n) / 空间 O(1)。

---

### 5. 寻找重复数（LeetCode 287 | 🟡 中等）
**简单思路**：值∈[1,n]、长 n+1 → 鸽巢原理必有环。把 `i→nums[i]` 当链表，重复值被多个下标指向 = 环入口。Floyd：Phase1 快慢指针（`slow=nums[slow]`、`fast=nums[nums[fast]]`）相遇；Phase2 一指针回起点 0、两者同步走 1 步，碰头处即环入口 = 重复数。

**对应解法**（取自 day34）：
```java
class Solution {
    public int findDuplicate(int[] nums) {
        int slow = 0, fast = 0;
        while (true) {                  // Phase1：快慢指针找相遇点
            slow = nums[slow];
            fast = nums[nums[fast]];
            if (slow == fast) break;
        }
        int head = 0;                   // Phase2：回起点同步走，相遇于环入口
        while (head != slow) {
            head = nums[head];
            slow = nums[slow];
        }
        return slow;                    // 环入口 = 重复数
    }
}
```
- ✅ 面试最优解：Floyd 判圈 O(n)/O(1)，是少数同时满足「不改原数组 + O(1) 空间 + O(n) 时间」的方案。对比：二分答案 O(n log n)/O(1)、哈希/排序 O(n) 空间或改数组。时间 O(n) / 空间 O(1)。

---

*整理自 day33-技巧hot100-part1.md、day34-技巧hot100-part2.md · 汇总日期：2026-08-16~08-17*

> **模型缺口提示（仅指出，未补充解法）**：本「技巧」批已收 位运算(136)、投票(169)、荷兰国旗(75)、字典序(31)、判圈(287)。Hot100 中常归类于「技巧/数学」但**尚未出现**的题可后续补：41 缺失的第一个正数（原地哈希）、128 最长连续序列（虽已在哈希总结，亦可作技巧）、204/263/264/279 数论、推算类（如 172 阶乘后的零、202 快乐数判圈同族）、148 排序链表常数空间（已归入链表总结并标注 gap）。其中 287 与 142 环形链表 II 同算法，建议联动复习。
