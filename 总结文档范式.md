# 总结文档范式

本文定义「专题总结类」文档（文件名以 `总结-` 开头）的标准格式，用于把分散在多个 `day*.md` 里的同类题解，提炼成一份**解题模型 + 速览表 + 各题解法**的归纳文档。与《算法解析文档范式》的区别：后者记录**单题深挖**，本文档记录**多题横向归纳**。

---

## 一、适用场景

当你想把某一专题（如 Hot100 哈希、双指针、动态规划、技巧等）下分散在多个 `day` 文件里的题解，串成一份可速查、可背诵的总结时，使用本范式。

**命名规范**：

```
总结-{主题}.md
```

| 部分 | 说明 | 示例 |
|------|------|------|
| `总结-` | 固定前缀，区别于 `day*.md` | `总结-` |
| `{主题}` | 专题名，中文或英文短词 | `hot100-1哈希`、`双指针`、`动态规划入门` |

**示例**：
- `总结-hot100-1哈希.md`
- `总结-hot100-2双指针.md`

---

## 二、文档结构（按顺序）

每篇总结文档应包含以下五个部分：

### 1. 标题与元信息

```markdown
# 总结 — {专题名}

> 本篇覆盖 {题号/题目列表}，解法代码取自 {源 day 文件}，只保留「简单思路 + 解法代码 + 面试最优解标注 + 复杂度」。
```

### 2. 解题模型总述（核心）

用自然语言讲解该专题的**共性套路**（2~4 条），是总结区别于「逐题文档」的灵魂。要讲清：

- 这类题**长什么样**（识别特征）
- **该往哪个方向想**（切入点 / 通用模型）
- 各套路一句话口诀

> 不要逐题复述，要提炼"横向规律"。

### 3. 速览表（面试最优解 + 复杂度）

一张总表，一眼看清每题该写什么、复杂度多少：

```markdown
| 题号 | 题目 | 面试最优解（写哪个） | 时间复杂度 | 空间复杂度 |
|------|------|---------------------|-----------|-----------|
| 1 | 两数之和 | 一遍哈希（先查后存） | O(n) | O(n) |
| 49 | 字母异位词分组 | 排序/计数做 key + HashMap | O(nk log k) | O(nk) |
```

> 若有**多种解法**，速览表里可以合并写"最优解 + 备选"，详细信息留给第 4 节。

### 4. 各题解法详情（⚠️ 多解法必全列）

每题一个小节，结构固定为四块：

```markdown
### {序号}. {题目中文名}（LeetCode {题号} | 难度）

**简单思路**：一两句讲清切入点（为什么用这个算法）。

**对应解法**（取自 {源文件}）：

\`\`\`java
// 从源 day 文档原样搬入的代码
\`\`\`

- **✅ 面试最优解**：明确面试时该写哪个，并说明为什么（对比其他解法的取舍）。
- **时间复杂度**：...
- **空间复杂度**：...
```

#### ⚠️ 多解法规则（重要）

> **如果源文档（day 文件）里同一题有不止一种解法，总结里必须全部列出，不能只放一个！**

- 每条解法**独立成段**，各自标注思路、代码、复杂度。
- 在「✅ 面试最优解」处点明**哪个是面试首选**，其余作为**对比 / 拓展变体**列出（并说明它们各自的取舍，如空间换时间、常数优化等）。
- 速览表可写"最优解（备选：暴力 / 双指针 …）"提示存在多种解法。

**单解法示例**（简洁）：

```markdown
### 1. 两数之和（LeetCode 1 | 🟢 简单）
**简单思路**：HashMap 存「值→下标」，遍历时查 `target-nums[i]` 是否在 map，先查后存避免自匹配。
**对应解法**（取自 day8）：
\`\`\`java
// ... 一遍哈希代码 ...
\`\`\`
- ✅ 面试最优解：一遍哈希 O(n)，别写 O(n²) 暴力。
- 时间 O(n) / 空间 O(n)
```

**多解法示例**（务必全列）：

```markdown
### 2. 接雨水（LeetCode 42 | 🔴 困难）
**简单思路**：核心是"每个位置能接的水 = 左右最高墙的较小值 - 自身高度"，三种实现思路不同。

**解法一 · 暴力（取自 dayX）**
\`\`\`java
// 对每个位置左右扫最高墙 —— O(n²)
\`\`\`
- 时间 O(n²) / 空间 O(1)；仅作理解用，面试不要写。

**解法二 · 动态规划（取自 dayX）**
\`\`\`java
// 预处理 leftMax / rightMax 数组 —— O(n)
\`\`\`
- 时间 O(n) / 空间 O(n)；思路清晰，面试可写。

**解法三 · 双指针（取自 dayX）**
\`\`\`java
// 左右指针向中间收，O(1) 空间 —— O(n)
\`\`\`
- ✅ 面试最优解：双指针 O(n) 时间 + O(1) 空间，是面试首选；解法二作对比（更易写对）。

> 三种解法都要放：暴力理解模型、DP 易写、双指针最优。
```

### 5. 脚注

```markdown
*整理自 {源文件列表} · 汇总日期：{YYYY-MM-DD}*
```

---

## 三、完整模板骨架

```markdown
# 总结 — {专题名}

> 覆盖 {题号列表}，解法取自 {源 day 文件}。

---

## 一、解题模型总述

{2~4 条共性套路 + 一句话口诀}

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 面试最优解 | 时间 | 空间 |
|------|------|-----------|------|------|
| ... | ... | ... | ... | ... |

---

## 三、各题解法详情

### 1. {题名}（LeetCode {号} | 难度）
**简单思路**：...
**对应解法**（取自 {源}）：
\`\`\`java
...
\`\`\`
- ✅ 面试最优解：...
- 时间 / 空间：...

### 2. {题名}（多解法示例）
**简单思路**：...
**解法一 · {名称}**：代码 + 复杂度
**解法二 · {名称}**：代码 + 复杂度
**✅ 面试最优解**：点明首选 + 对比其余
...

---

*整理自 {源文件} · 汇总日期：{YYYY-MM-DD}*
```

---

## 四、写作原则

1. **总结是"提炼 + 串联"**：总述讲模型（横向规律），详情讲解法（纵向实现），不要整篇复制 day 文档。
2. **代码可追溯**：解法代码从源 day 文档**原样搬入**，在速览表 / 脚注标注来源文件，方便回查。
3. **⚠️ 多解法必全列**：源文档有几种解法，总结就列几种，并明确标注**面试首选**，避免读者只看到一个解法误以为是唯一解。
4. **复杂度写实**：含变体差异（如排序 key `O(nk log k)` vs 计数 key `O(nk)`），不要笼统写。
5. **保持聚焦**：只收与专题相关的题，不堆砌无关变体。

---

*本文档创建于 2026-08-17*
