# Day 33 — 技巧 Hot100

## 题目：只出现一次的数字（Single Number）

**LeetCode 136 | 位运算 / 技巧 Hot100 | 难度：🟢 简单**

### 题目描述

给你一个 **非空** 整数数组 `nums`，除了某个元素只出现一次以外，其余每个元素均出现**两次**。找出那个只出现了一次的元素。

**要求**：你的算法应该具有线性时间复杂度 O(n)，并且不使用额外空间（常数级空间）。

### 示例

```
输入：nums = [2,2,1]
输出：1

输入：nums = [4,1,2,1,2]
输出：4

输入：nums = [1]
输出：1
```

---

## 解法：异或（XOR）技巧

### 思路

这道题的精髓是**异或运算 `^` 的三个性质**：

```
① 任何数异或自己 = 0      x ^ x = 0
② 任何数异或 0 = 自己     x ^ 0 = x
③ 异或满足交换律、结合律   a^b^c = a^c^b = (a^b)^c
```

把数组里所有数依次异或一遍：

```
nums = [4, 1, 2, 1, 2]

ans = 4 ^ 1 ^ 2 ^ 1 ^ 2
    = 4 ^ (1 ^ 1) ^ (2 ^ 2)        ← 利用交换律/结合律重新分组
    = 4 ^ 0 ^ 0                    ← 出现两次的数两两抵消成 0
    = 4 ^ 0
    = 4                            ← 只剩那个"单身"的数
```

**为什么能"不开额外数组"？** 因为异或是就地累加的——每读一个数就和当前结果 `ans` 异或，不需要记录每个数出现了几次，空间恒为 O(1)。这正是题目"不用额外空间"要求的完美解法。

> 对比其他思路（仅作理解，不满足空间要求）：哈希表计数 O(n) 空间、排序后两两找 O(n log n) 时间。异或法是这道题的最优解。

### 思考方式图解

```
异或真值表（单 bit）：
  0 ^ 0 = 0
  1 ^ 1 = 0   ← 相同抵消
  0 ^ 1 = 1   ← 不同保留
  1 ^ 0 = 1

核心直觉：
  "出现两次" ⇒ 异或后归零（像正负抵消）
  "出现一次" ⇒ 留在最后结果里
```

### 代码实现

```java
class Solution {
    public int singleNumber(int[] nums) {
        int ans = 0;
        for (int x : nums) {
            ans ^= x;          // 每个数都和当前结果异或
        }
        return ans;            // 成对的数互相抵消，只剩单身者
    }
}
```

### 复杂度分析

| 维度 | 结果 |
|------|------|
| ⏱ 时间复杂度 | **O(n)** — 只遍历数组一次 |
| 🧠 空间复杂度 | **O(1)** — 只用了一个变量 `ans` |

---

## 小总结

| 要点 | 说明 |
|------|------|
| 算法名称 | 异或（XOR）技巧 |
| 算法类型 | 位运算、技巧题 |
| 核心技巧 | **`ans ^= x` 遍历一遍；成对元素异或归零，单身元素留下** |
| 关键性质 | `x^x=0`、`x^0=x`、交换律/结合律 |
| 为何满足要求 | 线性时间 O(n) + 常数空间 O(1)，无需哈希表 |
| 前提 | 每个元素恰好出现两次，仅一个出现一次（否则不适用） |
| 进阶变体 | [137. 只出现一次的数字 II](https://leetcode.cn/problems/single-number-ii/)（出现三次，用位计数）、[260. 只出现一次的数字 III](https://leetcode.cn/problems/single-number-iii/)（两个单身，按某 bit 分组） |

### 一句话记住

> **「单身数字 = 全员异或一遍——成对的相互抵消，剩下的就是它。」**

---

## 题目：多数元素（Majority Element）

**LeetCode 169 | 数组 / 技巧 Hot100 | 难度：🟢 简单**

### 题目描述

给定一个大小为 `n` 的数组 `nums`，返回其中的**多数元素**。

**多数元素**是指在数组中出现次数 **大于** `⌊n/2⌋` 的元素。你可以假设数组是非空的，并且给定的数组总是存在多数元素。

### 示例

```
输入：nums = [3,2,3]
输出：3

输入：nums = [2,2,1,1,1,2,2]
输出：2
```

---

## 解法：摩尔投票法（Boyer–Moore Voting）

### 思路

"多数元素"出现次数 **严格超过一半**，这是最关键的隐藏条件。基于它，可以用一个极其巧妙的**抵消**思想在 O(1) 空间里解出来：

```
维护一个"候选票数" ans 和"当前存活票数" hp：
  - 若 hp == 0：当前没人占优，把遇到的 x 立为候选 ans，hp 置 1
  - 若 x == ans：投自己一票，hp + 1
  - 若 x != ans：被反对一票，hp - 1（相当于 x 和 ans 的一票互相抵消）

遍历结束，ans 就是多数元素。
```

**为什么一定对？** 多数元素出现次数 > n/2，意味着它比"所有其他元素加起来"还多。于是无论怎么两两抵消（不同元素互相 -1），多数元素的余票永远不会被清零——它总能在最后"存活"下来当候选。

```
nums = [2, 2, 1, 1, 1, 2, 2]

i : x   hp==0?  动作                  ans   hp
0 : 2   yes     立候选 ans=2, hp=1     2     1
1 : 2   no,x==ans  hp+1               2     2
2 : 1   no,x!=ans  hp-1               2     1
3 : 1   no,x!=ans  hp-1               2     0
4 : 1   yes(hp==0)立候选 ans=1, hp=1  1     1
5 : 2   no,x!=ans  hp-1               1     0
6 : 2   yes(hp==0)立候选 ans=2, hp=1  2     1  ← 最终 ans=2 ✅

注意：中途 ans 被换成 1 又换回 2，但多数元素 2 最后仍存活。
```

> 直观比喻：每个元素是一支"军队"，不同军队相遇各死一人，同军相加。多数元素人数过半，永远不可能被全部抵消，最后剩下的军队首领就是答案。

### 代码实现

```java
class Solution {
    public int majorityElement(int[] nums) {
        int ans = 0;
        int hp = 0;                  // 候选 ans 当前的"存活票数"
        for (int x : nums) {
            if (hp == 0) {           // 当前没人占优，立 x 为新候选
                ans = x;
                hp = 1;
            } else {
                hp += x == ans ? 1 : -1;  // 同则 +1，异则 -1（抵消）
            }
        }
        return ans;
    }
}
```

> **严谨性补充**：本题保证"一定存在多数元素"，所以摩尔投票法直接返回即可，无需二次验证。若题目不保证存在（如 229. 求众数 II 找出现 >⌊n/3⌋ 的所有元素），投票后还需再扫一遍数组**验证**候选是否真的达标——因为不保证存在时，最终存活的候选可能是"假多数"。

### 复杂度分析

| 维度 | 结果 |
|------|------|
| ⏱ 时间复杂度 | **O(n)** — 只遍历数组一次 |
| 🧠 空间复杂度 | **O(1)** — 只用 `ans`、`hp` 两个变量 |

---

## 小总结

| 要点 | 说明 |
|------|------|
| 算法名称 | 摩尔投票法（Boyer–Moore） |
| 算法类型 | 数组、技巧题、抵消思想 |
| 核心技巧 | **`hp==0` 立候选、`x==ans` +1、`x!=ans` -1；最后存活者即多数** |
| 成立前提 | 多数元素出现次数 > n/2（比其他所有加起来还多），保证必存活 |
| 优势 | O(n) 时间 + O(1) 空间，吊打哈希表计数 / 排序 |
| 注意事项 | 不保证存在多数元素时，投票后需再验证（如 229 题找 >n/3 的所有众数） |
| 关联题目 | [229. 求众数 II](https://leetcode.cn/problems/majority-element-ii/)（双候选投票 + 验证） |

### 一句话记住

> **「多数元素 = 摩尔投票——不同元素两两抵消，过半者永远活到最后。」**

---

## 题目：颜色分类（Sort Colors / 荷兰国旗问题）

**LeetCode 75 | 数组 / 双指针 / 技巧 Hot100 | 难度：🟡 中等**

### 题目描述

给定一个包含红色（0）、白色（1）、蓝色（2）且长度为 `n` 的数组 `nums`，**原地**对它们进行排序，使相同颜色的元素相邻，并按照 0、1、2 的顺序排列。

必须在不使用库排序函数的情况下，用**一趟扫描**完成（即 O(n) 时间、O(1) 空间）。

### 示例

```
输入：nums = [2,0,2,1,1,0]
输出：[0,0,1,1,2,2]

输入：nums = [2,0,1]
输出：[0,1,2]
```

---

## 解法：双指针一趟扫描（Dutch National Flag）

### 思路

这是 Dijkstra 提出的**荷兰国旗问题**：把数组分成三段（0 / 1 / 2），用两个指针 `p0`、`p1` 维护三段边界：

```
约定（处理到 i 时）：
  [0, p0)      → 全是 0
  [p0, p1)     → 全是 1
  [p1, i]      → 全是 2   （已处理、确定为 2 的部分）

p0：下一个 0 该放的位置
p1：下一个 1 该放的位置
```

你这份代码的巧妙之处在于**"默认先当 2，再逐级升级"**：

```
1. nums[i] = 2;           // 先把当前位置默认当成 2（2 是"未升级"的兜底）
2. if (x <= 1)             // x 是 0 或 1，至少要在 p1 处放一个 1
       nums[p1++] = 1;
3. if (x == 0)             // x 是 0，再把 p0 处升级成 0（覆盖刚才可能放的 1）
       nums[p0++] = 0;
```

**为什么这样不会乱？** 因为 `p0 ≤ p1 ≤ i` 始终成立：
- `x == 2`：什么都不放，nums[i] 维持默认 2，[p1, i] 区间正确。
- `x == 1`：在 p1 放 1（覆盖掉之前默认的 2，或 nums[i] 刚设的 2），p1 右移，[p0, p1) 区间正确。
- `x == 0`：先在 p1 放 1（保证 1 的数量不丢），再在 p0 放 0（把最老的 1 顶替成 0），p0、p1 都右移，三段都正确。

`x==0` 时"先放 1 再放 0"看起来多此一举，但它的作用是**维持 1 的总数**：那个被放到 p1 的 1，正好补上被 p0 处新 0 顶掉的老 1，整体 [p0, p1) 的 1 数量不变。

### 思考方式图解

```
nums = [2, 0, 2, 1, 1, 0]，p0=0, p1=0

i=0  x=2 : nums[0]=2。x<=1?否。         → [2,0,2,1,1,0]
i=1  x=0 : nums[1]=2 → p1放1(nums[0]=1,p1=1) → p0放0(nums[0]=0,p0=1)
                                                            → [0,2,2,1,1,0]
i=2  x=2 : nums[2]=2。                  → [0,2,2,1,1,0]
i=3  x=1 : nums[3]=2 → p1放1(nums[1]=1,p1=2)  → [0,1,2,2,1,0]
i=4  x=1 : nums[4]=2 → p1放1(nums[2]=1,p1=3)  → [0,1,1,2,2,0]
i=5  x=0 : nums[5]=2 → p1放1(nums[3]=1,p1=4) → p0放0(nums[1]=0,p0=2)
                                                            → [0,0,1,1,2,2] ✅
```

> 对比另一种经典写法（三指针 `p0 / p2` + 遇到 0 交换、遇到 2 交换）：你的写法不用交换、纯赋值，逻辑更紧凑，但理解成本略高——核心就是"默认 2 + 升级 1 + 再升级 0"。

### 代码实现

```java
class Solution {
    public void sortColors(int[] nums) {
        int p0 = 0;     // 下一个 0 的位置
        int p1 = 0;     // 下一个 1 的位置
        for (int i = 0; i < nums.length; i++) {
            int x = nums[i];
            nums[i] = 2;                  // 默认当前位置是 2（兜底）
            if (x <= 1) {                 // 0 或 1：先在 p1 处补一个 1
                nums[p1++] = 1;
            }
            if (x == 0) {                 // 是 0：再把 p0 处升级成 0
                nums[p0++] = 0;
            }
        }
    }
}
```

> **关键点**：`x` 必须先取出再用 `nums[i]=2` 覆盖，否则会丢失当前值。指针 `p0`/`p1` 永远 ≤ `i`，所以写入 `nums[p1]`/`nums[p0]` 即使等于 `i` 也是在覆盖刚设好的 2，不会破坏已确定的 0/1 段。

### 复杂度分析

| 维度 | 结果 |
|------|------|
| ⏱ 时间复杂度 | **O(n)** — 一趟扫描，每个元素处理常数次 |
| 🧠 空间复杂度 | **O(1)** — 只用 `p0`、`p1`、`x` 三个变量，原地排序 |

---

## 小总结

| 要点 | 说明 |
|------|------|
| 算法名称 | 双指针一趟扫描（荷兰国旗问题） |
| 算法类型 | 数组、双指针、原地排序、技巧题 |
| 核心技巧 | **默认 `nums[i]=2`，`x<=1` 在 p1 放 1，`x==0` 在 p0 放 0——逐级升级** |
| 不变量 | `[0,p0)=0`、`[p0,p1)=1`、`[p1,i]=2`，且 `p0≤p1≤i` |
| `x==0` 的妙处 | 先补 1 再置 0，维持 1 的总数不丢 |
| 对比写法 | 三指针 `p0/p2` 交换法（更直观但需 swap） |
| 关联题目 | [283. 移动零](https://leetcode.cn/problems/move-zeroes/)（两段的特例）、[88. 合并两个有序数组](https://leetcode.cn/problems/merge-sorted-array/)（双指针从后填） |

### 一句话记住

> **「颜色分类 = 默认当 2，0/1 逐级升级——双指针一趟把数组劈成 0、1、2 三段。」**

---

*练习日期：2026-08-16*
