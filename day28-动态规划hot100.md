# Day 28 — 动态规划 Hot100

## 题目：爬楼梯（Climbing Stairs）

**LeetCode 70 | 动态规划 Hot100 | 难度：🟢 简单**

### 题目描述

假设你正在爬楼梯。需要 `n` 阶你才能到达楼顶。

每次你可以爬 `1` 或 `2` 个台阶。你有多少种不同的方法可以爬到楼顶呢？

### 示例

```
输入：n = 2
输出：2
解释：有两种方法可以到达楼顶
  1. 1 阶 + 1 阶
  2. 2 阶

输入：n = 3
输出：3
解释：有三种方法
  1. 1 阶 + 1 阶 + 1 阶
  2. 1 阶 + 2 阶
  3. 2 阶 + 1 阶
```

---

## 解法一：朴素递归（会超时 ❌）

### 思路

这是最直观的写法——**递归定义**：

```
爬到第 i 阶的方法数 = 爬到第 i-1 阶的方法数 + 爬到第 i-2 阶的方法数
因为最后一步要么从 i-1 跨 1 阶，要么从 i-2 跨 2 阶

dfs(i) = dfs(i-1) + dfs(i-2)    ← 本质就是斐波那契数列
dfs(0) = 1, dfs(1) = 1          ← 递归边界
```

### 代码实现

```java
// ⚠️ 会超时的递归代码——大量重复计算
class Solution {
    public int climbStairs(int n) {
        return dfs(n);
    }

    private int dfs(int i) {
        if (i <= 1) {        // 递归边界
            return 1;
        }
        return dfs(i - 1) + dfs(i - 2);
    }
}
```

### 为什么超时？

```
dfs(5)
├── dfs(4)
│   ├── dfs(3)
│   │   ├── dfs(2)
│   │   │   ├── dfs(1) ← 被重复计算！
│   │   │   └── dfs(0) ← 被重复计算！
│   │   └── dfs(1)     ← 被重复计算！
│   └── dfs(2)
│       ├── dfs(1)      ← 重复！
│       └── dfs(0)      ← 重复！
└── dfs(3)
    ├── dfs(2)           ← 整棵子树重复！
    └── dfs(1)
```

**重复计算**：`dfs(2)`、`dfs(1)`、`dfs(0)` 被算了无数次——递归树有 2ⁿ 个节点，时间复杂度 **O(2ⁿ)**，n=45 就卡死。

### 复杂度分析

| 维度 | 结果 |
|------|------|
| ⏱ 时间复杂度 | **O(2ⁿ)** — 指数级，递归树节点数 |
| 🧠 空间复杂度 | **O(n)** — 递归栈深度 |

> 这个写法在 LeetCode 上无法通过（n=45 时超时），**必须用记忆化搜索或动态规划优化**——见下方补充的写法。

---

## 小总结（第一版）

| 要点 | 说明 |
|------|------|
| 算法名称 | 朴素递归 |
| 算法类型 | 递归 |
| 核心公式 | **dfs(i) = dfs(i-1) + dfs(i-2)**，边界 dfs(0)=dfs(1)=1 |
| 问题 | O(2ⁿ) 指数级重复计算，n 大时超时 |
| 优化方向 | 记忆化搜索（备忘录）→ 递推 DP（自底向上）→ 滚动数组 → O(1) 空间 |

### 一句话记住

> **「爬楼梯 = 斐波那契，最后一步一阶或两阶——递归超时，等优化。」**

---

*练习日期：2026-08-08*
