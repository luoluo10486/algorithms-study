# Day 28 — 动态规划 Hot100

## 题目：爬楼梯（Climbing Stairs）

**LeetCode 70 | 动态规划 Hot100 | 难度：🟢 简单**

### 题目描述

假设你正在爬楼梯。需要 `n` 阶你才能到达楼顶。

每次你可以爬 `1` 或 `2` 个台阶。你有多少种不同的方法可以爬到楼顶呢？

### 示例

```
输入：n = 2
输出：2
解释：有两种方法可以到达楼顶
  1. 1 阶 + 1 阶
  2. 2 阶

输入：n = 3
输出：3
解释：有三种方法
  1. 1 阶 + 1 阶 + 1 阶
  2. 1 阶 + 2 阶
  3. 2 阶 + 1 阶
```

---

## 解法一：朴素递归（会超时 ❌）

### 思路

这是最直观的写法——**递归定义**：

```
爬到第 i 阶的方法数 = 爬到第 i-1 阶的方法数 + 爬到第 i-2 阶的方法数
因为最后一步要么从 i-1 跨 1 阶，要么从 i-2 跨 2 阶

dfs(i) = dfs(i-1) + dfs(i-2)    ← 本质就是斐波那契数列
dfs(0) = 1, dfs(1) = 1          ← 递归边界
```

### 代码实现

```java
// ⚠️ 会超时的递归代码——大量重复计算
class Solution {
    public int climbStairs(int n) {
        return dfs(n);
    }

    private int dfs(int i) {
        if (i <= 1) {        // 递归边界
            return 1;
        }
        return dfs(i - 1) + dfs(i - 2);
    }
}
```

### 为什么超时？

```
dfs(5)
├── dfs(4)
│   ├── dfs(3)
│   │   ├── dfs(2)
│   │   │   ├── dfs(1) ← 被重复计算！
│   │   │   └── dfs(0) ← 被重复计算！
│   │   └── dfs(1)     ← 被重复计算！
│   └── dfs(2)
│       ├── dfs(1)      ← 重复！
│       └── dfs(0)      ← 重复！
└── dfs(3)
    ├── dfs(2)           ← 整棵子树重复！
    └── dfs(1)
```

**重复计算**：`dfs(2)`、`dfs(1)`、`dfs(0)` 被算了无数次——递归树有 2ⁿ 个节点，时间复杂度 **O(2ⁿ)**，n=45 就卡死。

### 复杂度分析

| 维度 | 结果 |
|------|------|
| ⏱ 时间复杂度 | **O(2ⁿ)** — 指数级，递归树节点数 |
| 🧠 空间复杂度 | **O(n)** — 递归栈深度 |

> 这个写法在 LeetCode 上无法通过（n=45 时超时），**必须用记忆化搜索或动态规划优化**——见下方补充的写法。

---

## 解法二：记忆化搜索（备忘录，✅ 可通过）

### 思路

朴素递归超时的根源是**重复计算**——同一个 `dfs(i)` 被算了无数次。解决办法很简单：**把算过的结果存起来，下次直接用**。

```
用一个 memo[] 数组记录已经算过的 dfs(i)：

dfs(i):
  如果 i <= 1 → 返回 1（递归边界）
  如果 memo[i] != 0 → 直接返回 memo[i]（已经算过了！）
  否则：memo[i] = dfs(i-1) + dfs(i-2) ← 算一次，存起来
```

**memo[i] 的作用**：每个 `dfs(i)` 只被计算一次，之后所有对它的调用都是 O(1) 查表——递归树从 2ⁿ 个节点压缩到 n 个节点。

### 思考方式图解

```
n = 5 时，memo 填充过程（只算每个值一次）：

dfs(5): memo[5] = dfs(4) + dfs(3)
  dfs(4): memo[4] = dfs(3) + dfs(2)
    dfs(3): memo[3] = dfs(2) + dfs(1)
      dfs(2): memo[2] = dfs(1) + dfs(0) = 1 + 1 = 2   ← 算一次
      dfs(1): 返回 1
      → memo[3] = 2 + 1 = 3                            ← 算一次
    dfs(2): memo[2] 已有 → 直接返回 2                  ← 查表！
    → memo[4] = 3 + 2 = 5                              ← 算一次
  dfs(3): memo[3] 已有 → 直接返回 3                    ← 查表！
  → memo[5] = 5 + 3 = 8                                ← 算一次

结果：8 ✅ 每个值都只算了一次
```

### 代码实现

```java
class Solution {
    public int climbStairs(int n) {
        int[] memo = new int[n + 1];   // memo[i] = dfs(i) 的结果
        return dfs(n, memo);
    }

    private int dfs(int i, int[] memo) {
        if (i <= 1) {        // 递归边界
            return 1;
        }
        if (memo[i] != 0) {  // 已经算过 → 直接返回
            return memo[i];
        }
        // 没算过 → 计算并存入备忘录
        return memo[i] = dfs(i - 1, memo) + dfs(i - 2, memo);
    }
}
```

### 复杂度分析

| 维度 | 结果 |
|------|------|
| ⏱ 时间复杂度 | **O(n)** — 每个 i 只计算一次，其余查表 O(1) |
| 🧠 空间复杂度 | **O(n)** — memo 数组 + 递归栈 |

---

## 小总结（第二版）

| 要点 | 说明 |
|------|------|
| 算法名称 | 记忆化搜索（备忘录） |
| 算法类型 | 动态规划（自顶向下） |
| 核心技巧 | **memo[] 记录已算结果，`memo[i] != 0` 直接返回——每个子问题只算一次** |
| 与朴素递归的区别 | 朴素递归重复计算 O(2ⁿ)；记忆化把结果缓存，O(n) |
| 与递推 DP 的关系 | 记忆化是"自顶向下"的 DP；递推是"自底向上"的 DP，思路等价 |
| 下一优化 | 递推 DP + 滚动数组 → 空间可降到 O(1) |

### 一句话记住

> **「备忘录缓存已算结果，重复调用直接查表——递归树从 2ⁿ 压到 n。」**

---

## 解法三：递推 DP（自底向上，✅ 标准写法）

### 思路

记忆化搜索是**自顶向下**（从 n 递归到 0），递推 DP 反过来**自底向上**——从已知的 `f[0]`、`f[1]` 开始，一步步算出 `f[2]`、`f[3]`…直到 `f[n]`。

```
f[i] = 爬到第 i 阶的方法数

边界：f[0] = 1（0 阶，不动也是一种方法）
      f[1] = 1（1 阶，只能跨 1 步）

递推：f[i] = f[i-1] + f[i-2]    （i ≥ 2）
      最后一步从 i-1 跨 1 阶，或从 i-2 跨 2 阶
```

**和记忆化的关系**：两者计算的是同一个递推式，方向相反。记忆化是"要用的时候才算"（惰性），递推是"从底开始全都算好"（提前）。

### 思考方式图解

```
n = 5

f[0]=1, f[1]=1

i=2: f[2] = f[1] + f[0] = 1 + 1 = 2
i=3: f[3] = f[2] + f[1] = 2 + 1 = 3
i=4: f[4] = f[3] + f[2] = 3 + 2 = 5
i=5: f[5] = f[4] + f[3] = 5 + 3 = 8

结果：8 ✅
```

### 代码实现

```java
class Solution {
    public int climbStairs(int n) {
        int[] f = new int[n + 1];
        f[0] = f[1] = 1;   // 边界

        for (int i = 2; i < n + 1; i++) {
            f[i] = f[i - 1] + f[i - 2];   // 状态转移
        }

        return f[n];
    }
}
```

### 复杂度分析

| 维度 | 结果 |
|------|------|
| ⏱ 时间复杂度 | **O(n)** — 一次循环 |
| 🧠 空间复杂度 | **O(n)** — f 数组 |

> **下一优化**：递推时 `f[i]` 只依赖 `f[i-1]` 和 `f[i-2]`，不需要保留整个数组——可以用**滚动数组/两个变量**把空间降到 **O(1)**。

---

## 小总结（第三版）

| 要点 | 说明 |
|------|------|
| 算法名称 | 递推 DP（自底向上） |
| 算法类型 | 动态规划 |
| 核心技巧 | **从边界 f[0]、f[1] 出发，按递推式 f[i]=f[i-1]+f[i-2] 依次算出所有值** |
| 与记忆化的区别 | 记忆化自顶向下（惰性）、递推自底向上（提前）；计算同一个递推式 |
| 三版对比 | 朴素递归 O(2ⁿ) ❌ → 记忆化 O(n) → 递推 DP O(n)，递推更简洁 |
| 下一优化 | 滚动数组 → O(1) 空间 |

### 一句话记住

> **「从边界往上推，f[i] 只看前两个——自底向上一次算完。」**

---

*练习日期：2026-08-10*
