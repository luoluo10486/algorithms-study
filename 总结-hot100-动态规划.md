# 总结 — 动态规划 Hot100

> 覆盖 LeetCode 70 / 118 / 198 / 279 / 322 / 139 / 416 / 300 / 152 / 32（Hot100），解法代码取自 `day28-动态规划hot100-part1.md`、`day29-动态规划hot100-part2.md`、`day30-动态规划hot100-part3.md`。按你确认的口径：**背包模型 + 递推模型都收录**。只保留「简单思路 + 解法代码 + 面试最优解标注 + 复杂度」。

---

## 一、解题模型总述

动态规划 Hot100 本批可拆成**两大模型**，识别特征分别是「凑数/选子集（背包）」与「由前推后/以 i 结尾（递推）」。横向规律：

### A. 背包模型（`dfs(i, j)` = 前 i 个物品凑容量 j）
- **完全背包（279 / 322 / 139）**：物品可**无限重复**取 —— 选了之后 `i` **不推进**（`dfs(i, j-w)+1`）。`279`/`322` 求「最少数量」，选/不选取 `min`；`139` 是可行性布尔版（字典词可重复用，问能否拼出）。
- **0/1 背包（416）**：每个数**只能选一次** —— 选了之后 `i-1` **推进**。416 把「平分集合」转化为「能否凑出总和一半」。
- **编码技巧**：金额/数量可能失败，用 `INF/2`（防 +1 溢出）或 `int: -1未算/0失败/1成功` 三态，避免 `boolean` 无法区分"未算"。
- **记忆化是骨架**：`memo[i][j]` 缓存子问题，每个状态只算一次，从 `O(2ⁿ)` 压到 `O(容量×物品)`。

### B. 递推模型（1:1 线性 DP，`f[i]` 由前几个状态推得）
- **定义状态**：要么「`f[i]` = 以 `nums[i]` 结尾的某某最值」（300 / 152 / 70 步数），要么「`f[i]` = 前 i 个的答案」（118 / 198）。
- **自顶向下记忆化 vs 自底向上递推**：同一递推式，方向相反，等价。70、300 两题源文档给了**双写法**。
- **双状态技巧（152）**：乘积因负数「最大↔最小」互换，须同时维护 `fmax`/`fmin`。
- **边界与滚动**：爬楼梯 `f[i]=f[i-1]+f[i-2]` 可滚动数组降到 O(1)；打家劫舍 `f[i]=max(f[i-1], f[i-2]+nums[i])`。

> 一句话：**背包问「凑不凑得出来/最少几个」（选/不选 + 是否可重复）；递推问「以 i 结尾或前 i 个的最优」（由前推后 + 记忆化/滚动）。**

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 模型 | 面试最优解 | 时间 | 空间 |
|------|------|------|-----------|------|------|
| 279 | 完全平方数 | 完全背包 | 记忆化（完全背包） | O(√n·n) | O(√n·n) |
| 322 | 零钱兑换 | 完全背包 | 记忆化（完全背包） | O(n·amount) | O(n·amount) |
| 139 | 单词拆分 | 完全背包（可行性） | 记忆化 + maxLen剪枝 | O(n·maxLen) | O(n) |
| 416 | 分割等和子集 | 0/1 背包 | 记忆化（0/1 背包） | O(n·target) | O(n·target) |
| 70 | 爬楼梯 | 递推 | 递推 DP（滚动更佳） | O(n) | O(1) |
| 118 | 杨辉三角 | 递推（二维） | 逐行递推 | O(r²) | O(r²) |
| 198 | 打家劫舍 | 递推（选/不选） | 记忆化 / 递推 | O(n) | O(n) |
| 300 | 最长递增子序列 | 递推（以 i 结尾） | 递推 DP（O(n²)；O(nlogn)优化仅提及） | O(n²) | O(n) |
| 152 | 乘积最大子数组 | 递推（双状态） | fmax/fmin 双状态 | O(n) | O(n) |
| 32 | 最长有效括号 | 栈+DP（DP另解仅提及） | 栈标记 + 最长连续段 | O(n) | O(n) |

> 70、300 含多解法（已全列）；32 的「纯 DP」解法源文档仅提及未附代码，已标注。

---

## 三、各题解法详情

### 背包模型

#### 1. 完全平方数（LeetCode 279 | 🟡 中等）
**简单思路**：完全背包 —— 物品是 `1²,2²,…`（可无限取），求凑出 `n` 的最少数量。`dfs(i,j)=min(不选 dfs(i-1,j), 选 dfs(i,j-i²)+1)`。

**对应解法**（取自 day29，记忆化）：
```java
class Solution {
    private static final int[][] memo = new int[101][10001];
    static { for (int[] row : memo) Arrays.fill(row, -1); }
    private static int dfs(int i, int j) {
        if (i == 0) return j == 0 ? 0 : Integer.MAX_VALUE;  // 没数可用
        if (memo[i][j] != -1) return memo[i][j];
        if (j < i * i) return memo[i][j] = dfs(i - 1, j);     // 放不下
        return memo[i][j] = Math.min(dfs(i - 1, j), dfs(i, j - i * i) + 1); // 选：i不变
    }
    public int numSquares(int n) { return dfs((int) Math.sqrt(n), n); }
}
```
- ✅ 面试最优解：完全背包记忆化 O(√n·n)。同思路可写一维递推 `f[j]=min(f[j], f[j-i²]+1)`。
- 时间 O(√n·n) / 空间 O(√n·n)（静态 memo 跨用例复用提速）。

---

#### 2. 零钱兑换（LeetCode 322 | 🟡 中等）
**简单思路**：与 279 同一模板，完全背包 —— 硬币可无限取，求凑 `amount` 最少数量。区别：物品是给定 `coins`、且「凑不出返回 -1」。

**对应解法**（取自 day29，记忆化）：
```java
class Solution {
    public int coinChange(int[] coins, int amount) {
        int n = coins.length;
        int[][] memo = new int[n][amount + 1];
        for (int[] row : memo) Arrays.fill(row, -1);
        int ans = dfs(n - 1, amount, coins, memo);
        return ans < Integer.MAX_VALUE / 2 ? ans : -1;  // INF/2 防溢出
    }
    private int dfs(int i, int c, int[] coins, int[][] memo) {
        if (i < 0) return c == 0 ? 0 : Integer.MAX_VALUE / 2;  // 用 INF/2 防 +1 溢出
        if (memo[i][c] != -1) return memo[i][c];
        if (c < coins[i]) return memo[i][c] = dfs(i - 1, c, coins, memo); // 放不下
        return memo[i][c] = Math.min(dfs(i - 1, c, coins, memo),
                                     dfs(i, c - coins[i], coins, memo) + 1); // 选：i不变
    }
}
```
- ✅ 面试最优解：完全背包记忆化。注意 `INF/2` 编码（防 +1 溢出）与 `ans<INF/2` 判 -1。时间 O(n·amount) / 空间 O(n·amount)。

---

#### 3. 单词拆分（LeetCode 139 | 🟡 中等）
**简单思路**：完全背包的"可行性布尔版" —— 字典词可重复用，问能否拼出 `s`。`dfs(i)=∃j: s[j..i)∈字典 且 dfs(j)`（枚举最后一段递归判断前半段）。`maxLen` 剪枝把枚举从 O(i) 压到 O(maxLen)。

**对应解法**（取自 day29，记忆化 + 剪枝）：
```java
class Solution {
    public boolean wordBreak(String s, List<String> wordDict) {
        int maxLen = 0;
        for (String w : wordDict) maxLen = Math.max(maxLen, w.length());
        Set<String> words = new HashSet<>(wordDict);
        int n = s.length();
        int[] memo = new int[n + 1];
        Arrays.fill(memo, -1);
        return dfs(n, maxLen, s, words, memo) == 1;
    }
    private int dfs(int i, int maxLen, String s, Set<String> words, int[] memo) {
        if (i == 0) return 1;                       // 空串可拆
        if (memo[i] != -1) return memo[i];
        for (int j = i - 1; j >= Math.max(i - maxLen, 0); j--) {  // 仅枚举 maxLen 长度
            if (words.contains(s.substring(j, i)) && dfs(j, maxLen, s, words, memo) == 1)
                return memo[i] = 1;
        }
        return memo[i] = 0;
    }
}
```
- ✅ 面试最优解：后缀拆分 + `maxLen` 剪枝，O(n·maxLen)。`int` 三态（-1/0/1）编码"未算/不能/能"。时间 O(n·maxLen) / 空间 O(n)。

---

#### 4. 分割等和子集（LeetCode 416 | 🟡 中等）
**简单思路**：**0/1 背包** —— 平分集合 ⟺ 能否选出子集凑出 `target=总和/2`。每个数只用一次，选了 `i-1` 推进（区别于完全背包的 `i` 不推进）。总和为奇数直接 false。

**对应解法**（取自 day30，记忆化）：
```java
class Solution {
    public boolean canPartition(int[] nums) {
        int s = 0; for (int x : nums) s += x;
        if (s % 2 != 0) return false;              // 奇数不可能平分
        int n = nums.length, target = s / 2;
        int[][] memo = new int[n][target + 1];
        for (int[] row : memo) Arrays.fill(row, -1);
        return dfs(n - 1, target, nums, memo);
    }
    private boolean dfs(int i, int j, int[] nums, int[][] memo) {
        if (i < 0) return j == 0;                  // 刚好凑到 0 才成功
        if (memo[i][j] != -1) return memo[i][j] == 1;
        boolean res;
        if (j < nums[i]) res = dfs(i - 1, j, nums, memo);          // 装不下
        else res = dfs(i - 1, j - nums[i], nums, memo)             // 选（0/1：i-1 推进）
                 || dfs(i - 1, j, nums, memo);                     // 不选
        memo[i][j] = res ? 1 : 0;
        return res;
    }
}
```
- ✅ 面试最优解：0/1 背包记忆化 O(n·target)。进阶可压一维 DP（倒序遍历）降到 O(target)。时间 O(n·target) / 空间 O(n·target)。

---

### 递推模型

#### 5. 爬楼梯（LeetCode 70 | 🟢 简单）
**简单思路**：`f[i]=f[i-1]+f[i-2]`（最后一步跨 1 或跨 2 阶）。源文档给了**三个版本**：朴素递归（超时）、记忆化、递推 DP。

**解法一 · 朴素递归（取自 day28，❌ 超时）**
```java
class Solution {                          // O(2ⁿ)，n=45 卡死，LeetCode 不通过
    public int climbStairs(int n) { return dfs(n); }
    private int dfs(int i) {
        if (i <= 1) return 1;
        return dfs(i - 1) + dfs(i - 2);   // 大量重复计算
    }
}
```

**解法二 · 记忆化搜索（取自 day28，✅ 通过）**
```java
class Solution {
    public int climbStairs(int n) {
        int[] memo = new int[n + 1];
        return dfs(n, memo);
    }
    private int dfs(int i, int[] memo) {
        if (i <= 1) return 1;
        if (memo[i] != 0) return memo[i];          // 已算过
        return memo[i] = dfs(i - 1, memo) + dfs(i - 2, memo);
    }
}
```

**解法三 · 递推 DP（取自 day28，✅ 标准写法）**
```java
class Solution {
    public int climbStairs(int n) {
        int[] f = new int[n + 1];
        f[0] = f[1] = 1;
        for (int i = 2; i < n + 1; i++) f[i] = f[i - 1] + f[i - 2];
        return f[n];
    }
}
```
- ✅ 面试最优解：**递推 DP**（最简洁）；进阶用滚动数组（两个变量）把空间压到 O(1)。解法一仅作"为什么超时"的反面教材，面试不要写。
- 时间 O(n) / 空间 O(n)（滚动后 O(1)）。

---

#### 6. 杨辉三角（LeetCode 118 | 🟢 简单）
**简单思路**：二维递推 —— `row[j]=上一行[j-1]+上一行[j]`，行首行尾固定 1。

**对应解法**（取自 day28）：
```java
class Solution {
    public List<List<Integer>> generate(int numRows) {
        List<List<Integer>> c = new ArrayList<>();
        c.add(List.of(1));
        for (int i = 1; i < numRows; i++) {
            List<Integer> row = new ArrayList<>(i + 1);
            row.add(1);
            for (int j = 1; j < i; j++)
                row.add(c.get(i - 1).get(j - 1) + c.get(i - 1).get(j));
            row.add(1);
            c.add(row);
        }
        return c;
    }
}
```
- ✅ 面试最优解：逐行递推构建，O(r²)。关联 119（只返回第 k 行，可滚动 O(k)）。时间 O(r²) / 空间 O(r²)。

---

#### 7. 打家劫舍（LeetCode 198 | 🟡 中等）
**简单思路**：选/不选决策 —— `dfs(i)=max(dfs(i-1), dfs(i-2)+nums[i])`（偷 i 就不能偷 i-1）。`memo` 填 -1（金额可为 0，不能用 0 判未算）。

**对应解法**（取自 day28，记忆化）：
```java
class Solution {
    public int rob(int[] nums) {
        int[] memo = new int[nums.length];
        Arrays.fill(memo, -1);
        return dfs(nums.length - 1, nums, memo);
    }
    private int dfs(int i, int[] nums, int[] memo) {
        if (i < 0) return 0;
        if (memo[i] != -1) return memo[i];
        int notChoose = dfs(i - 1, nums, memo);
        int choose = dfs(i - 2, nums, memo) + nums[i];
        return memo[i] = Math.max(notChoose, choose);
    }
}
```
- ✅ 面试最优解：记忆化（或等价递推 `f[i]=max(f[i-1], f[i-2]+nums[i])`，滚动数组 O(1)）。时间 O(n) / 空间 O(n)。关联 213（环形，分两种取 max）。

---

#### 8. 最长递增子序列（LeetCode 300 | 🟡 中等）
**简单思路**：`dfs(i)=1+max(dfs(j))`（`j<i` 且 `nums[j]<nums[i]`），结尾不固定故对每个 i 取 max。源文档给了**两版**：记忆化 + 递推 DP。

**解法一 · 记忆化（以 i 结尾）（取自 day30）**
```java
class Solution {
    public int lengthOfLIS(int[] nums) {
        int n = nums.length, ans = 0;
        int[] memo = new int[n];
        for (int i = 0; i < n; i++) ans = Math.max(ans, dfs(i, nums, memo));
        return ans;
    }
    private int dfs(int i, int[] nums, int[] memo) {
        if (memo[i] > 0) return memo[i];           // 长度≥1，>0 即已算
        int res = 0;
        for (int j = 0; j < i; j++)
            if (nums[j] < nums[i]) res = Math.max(res, dfs(j, nums, memo));
        return memo[i] = res + 1;
    }
}
```

**解法二 · 递推 DP（自底向上）（取自 day30）**
```java
class Solution {
    public int lengthOfLIS(int[] nums) {
        int n = nums.length, ans = 0;
        int[] f = new int[n];
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < i; j++)
                if (nums[j] < nums[i]) f[i] = Math.max(f[i], f[j]);
            ans = Math.max(ans, ++f[i]);           // 加自己并取答案
        }
        return ans;
    }
}
```
- ✅ 面试最优解：两版等价，递推 DP 无递归栈更紧凑。⚠️ **O(n log n) 优化（贪心 + 二分 patience sorting，`tails[]` 维护各长度最小末尾）源文档仅提及未附代码**——是面试常考进阶，建议后续补 day 文档。
- 时间 O(n²) / 空间 O(n)。

---

#### 9. 乘积最大子数组（LeetCode 152 | 🟡 中等）
**简单思路**：**负数让最大↔最小互换**，须同时维护 `fmax`/`fmin`。每步三候选：`fmax[i-1]*x`、`fmin[i-1]*x`、`x`（重新开始，顺带处理 0）。

**对应解法**（取自 day30，递推 DP）：
```java
class Solution {
    public int maxProduct(int[] nums) {
        int n = nums.length;
        int[] fmax = new int[n], fmin = new int[n];
        fmax[0] = fmin[0] = nums[0];
        int ans = nums[0];
        for (int i = 1; i < n; i++) {
            int x = nums[i];
            fmax[i] = Math.max(Math.max(fmax[i - 1] * x, fmin[i - 1] * x), x);
            fmin[i] = Math.min(Math.min(fmax[i - 1] * x, fmin[i - 1] * x), x);
        }
        for (int j = 1; j < n; j++) ans = Math.max(ans, fmax[j]);
        return ans;
    }
}
```
- ✅ 面试最优解：双状态 DP，O(n)。可滚动两个变量降到 O(1)。区别于「最大子数组和」（求和只需单状态）。时间 O(n) / 空间 O(n)（滚动 O(1)）。

---

#### 10. 最长有效括号（LeetCode 32 | 🔴 困难）
**简单思路**：源文档给的是**栈标记法**（非纯 DP）：Step1 用栈配对所有 `()` 并标记 `isValid`；Step2 找最长连续 `true` 段（连续才是一个有效子串）。

**对应解法**（取自 day30，栈标记 + 最长连续段）：
```java
class Solution {
    public int longestValidParentheses(String s) {
        int n = s.length();
        boolean[] isValid = new boolean[n];
        Deque<Integer> st = new ArrayDeque<>();
        for (int i = 0; i < n; i++) {
            if (s.charAt(i) == '(') st.push(i);
            else if (!st.isEmpty()) isValid[i] = isValid[st.pop()] = true;  // 配对标记
        }
        int cnt = 0, ans = 0;
        for (int i = 0; i < n; i++) {
            if (isValid[i]) { cnt++; ans = Math.max(ans, cnt); }
            else cnt = 0;                          // 隔断重新计数
        }
        return ans;
    }
}
```
- ✅ 面试最优解：栈标记 O(n) 直观好写。⚠️ **源文档提及的「纯 DP 解法」（`dp[i]` 以 i 结尾的最长有效括号长度）仅提及未附代码**——是另一主流写法，建议后续补 day 文档再汇总。
- 时间 O(n) / 空间 O(n)。

---

*整理自 day28-动态规划hot100-part1.md、day29-动态规划hot100-part2.md、day30-动态规划hot100-part3.md · 汇总日期：2026-08-10~08-13*

> **模型缺口提示（仅指出，未补充解法）**：本批 DP 已覆盖完全背包(279/322/139)、0/1 背包(416) 与基础递推(70/118/198/300/152/32)。Hot100 中**尚未出现**的 DP 经典题可后续补：二维 DP（62 不同路径、63、64 最小路径和、120 三角形最小路径和）、序列 DP（1143 最长公共子序列、72 编辑距离、5 最长回文子串、647 回文子串、115 不同子序列、97 交错字符串）、其他（53 最大子数组和、337 打家劫舍 III、213 打家劫舍 II、312 戳气球、343 整数拆分、309 最佳买卖股票时机含冷冻期）。300 的 O(n log n) 与 32 的纯 DP 两处进阶写法源文档仅提及，待补。
