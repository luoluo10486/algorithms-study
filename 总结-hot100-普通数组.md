# 总结 — Hot100（2）· 普通数组

> 覆盖 LeetCode 704 / 27 / 977 / 209 / 59 / 53 / 56 / 189 / 238 / 41，解法分别取自 `day1-数组代码随想录-part1.md`、`day2-数组代码随想录-part2.md`、`day3-数组hot100.md`。普通数组题的套路极杂，这里按"数组基本功"归类：查找、删除、平方、子段、区间、翻转、前后缀、原地哈希。

---

## 一、解题模型总述

普通数组没有单一套路，但高频手法就几种：

1. **二分查找**（704）：有序数组找值，O(log n)。
2. **快慢指针**（27）：原地删除/覆盖，O(n)/O(1)。
3. **头尾双指针**（977）：有序数组平方后从两端取大者，O(n)。
4. **滑动窗口**（209）：右扩左缩求最短子段，全正数才单调可用。
5. **模拟 + 循环不变量**（59）：螺旋填充坚持"每条边左闭右开"。
6. **前后缀分解**（238）：`ans[i] = 左积[i] × 右积[i]`，绕开除法。
7. **排序 + 贪心**（56）：区间按起点排序后重叠即合并。
8. **原地技巧**（189 三次翻转 / 41 换座位）：O(1) 空间改造数组。

> 一句话：**数组题先想"能不能 O(1) 原地"和"能不能用双指针/前后缀降维"。**

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 面试最优解 | 时间 | 空间 |
|------|------|-----------|------|------|
| 704 | 二分查找 | 闭区间二分（首尾剪枝） | O(log n) | O(1) |
| 27 | 移除元素 | 快慢指针 | O(n) | O(1) |
| 977 | 有序数组的平方 | 头尾双指针 | O(n) | O(1) |
| 209 | 长度最小的子数组 | 滑动窗口 | O(n) | O(1) |
| 59 | 螺旋矩阵 II | 模拟（左闭右开） | O(n²) | O(n²) |
| 53 | 最大子数组和 | Kadane DP | O(n) | O(1) |
| 56 | 合并区间 | 排序 + 贪心 | O(n log n) | O(n) |
| 189 | 轮转数组 | 三次翻转 | O(n) | O(1) |
| 238 | 除自身以外数组的乘积 | 前缀积 × 后缀积（O(1) 空间） | O(n) | O(1) |
| 41 | 缺失的第一个正数 | 原地哈希（换座位） | O(n) | O(1) |

---

## 三、各题解法详情

### 1. 二分查找（LeetCode 704 | 🟢 简单）

**简单思路**：有序数组闭区间 `[left,right]` 二分；`mid = left + (right-left)/2` 防溢出；首尾各越界直接返回 -1 剪枝。

**对应解法**（取自 day1）：

```java
class Solution {
    public int search(int[] nums, int target) {
        if (target < nums[0] || target > nums[nums.length - 1]) return -1;
        int left = 0, right = nums.length - 1;
        while (left <= right) {
            int mid = left + ((right - left) >> 1);
            if (nums[mid] == target) return mid;
            else if (nums[mid] < target) left = mid + 1;
            else right = mid - 1;
        }
        return -1;
    }
}
```

- ✅ 面试最优解：闭区间二分 O(log n)/O(1)。（更完整的二分专题见《总结-hot100-二分查找》。）
- 时间 O(log n) / 空间 O(1)。

### 2. 移除元素（LeetCode 27 | 🟢 简单）

**简单思路**：快慢指针，快指针探路、慢指针占位覆盖。

**对应解法**（取自 day1）：

```java
class Solution {
    public int removeElement(int[] nums, int val) {
        int slowIndex = 0;
        for (int fastIndex = 0; fastIndex < nums.length; fastIndex++) {
            if (nums[fastIndex] != val) {
                nums[slowIndex] = nums[fastIndex];
                slowIndex++;
            }
        }
        return slowIndex;
    }
}
```

- ✅ 面试最优解：快慢指针 O(n)/O(1)。
- 时间 O(n) / 空间 O(1)。

### 3. 有序数组的平方（LeetCode 977 | 🟢 简单）

**简单思路**：数组有序但含负数，平方后大值一定在两端。头尾双指针，比较平方取大者从结果尾部填。

**对应解法**（取自 day1）：

```java
class Solution {
    public int[] sortedSquares(int[] nums) {
        int right = nums.length - 1, left = 0;
        int[] result = new int[nums.length];
        int index = result.length - 1;
        while (left <= right) {
            if (nums[left] * nums[left] > nums[right] * nums[right]) {
                result[index--] = nums[left] * nums[left++];
            } else {
                result[index--] = nums[right] * nums[right--];
            }
        }
        return result;
    }
}
```

- ✅ 面试最优解：头尾双指针 O(n)/O(n)。比"先平方再排序" O(n log n) 更优。
- 时间 O(n) / 空间 O(n)。

### 4. 长度最小的子数组（LeetCode 209 | 🟡 中等）

**简单思路**：全正数 → 窗口和单调。右指针扩张累和，满足 `≥ s` 就收缩左边界求最短。

**对应解法**（取自 day2）：

```java
class Solution {
    public int minSubArrayLen(int s, int[] nums) {
        int left = 0, sum = 0, result = Integer.MAX_VALUE;
        for (int right = 0; right < nums.length; right++) {
            sum += nums[right];
            while (sum >= s) {
                result = Math.min(result, right - left + 1);
                sum -= nums[left++];
            }
        }
        return result == Integer.MAX_VALUE ? 0 : result;
    }
}
```

- ✅ 面试最优解：滑动窗口 O(n)/O(1)。前提"全正数"保证收缩时和确定性减小。
- 时间 O(n) / 空间 O(1)。

### 5. 螺旋矩阵 II（LeetCode 59 | 🟡 中等）

**简单思路**：一圈圈填，每条边统一"左闭右开"避免重漏；`n/2` 圈，奇数中心单独填。

**对应解法**（取自 day2）：

```java
class Solution {
    public int[][] generateMatrix(int n) {
        int[][] nums = new int[n][n];
        int startX = 0, startY = 0, offset = 1, count = 1, loop = 1;
        int i, j;
        while (loop <= n / 2) {
            for (j = startY; j < n - offset; j++) nums[startX][j] = count++;
            for (i = startX; i < n - offset; i++) nums[i][j] = count++;
            for (; j > startY; j--) nums[i][j] = count++;
            for (; i > startX; i--) nums[i][j] = count++;
            startX++; startY++; offset++; loop++;
        }
        if (n % 2 == 1) nums[startX][startY] = count;
        return nums;
    }
}
```

- ✅ 面试最优解：就是上面模拟写法 O(n²)/O(n²)。关键是"左闭右开"循环不变量。
- 时间 O(n²) / 空间 O(n²)。

### 6. 最大子数组和（LeetCode 53 | 🟡 中等）

**简单思路**：Kadane。`pre = max(pre+nums[i], nums[i])`——一旦 `pre` 为负就抛弃、从当前重新开始。

**对应解法**（取自 day3）：

```java
class Solution {
    public int maxSubArray(int[] nums) {
        int pre = 0, maxSum = nums[0];
        for (int i : nums) {
            pre = Math.max(pre + i, i);
            maxSum = Math.max(maxSum, pre);
        }
        return maxSum;
    }
}
```

- ✅ 面试最优解：Kadane 算法 O(n)/O(1)。进阶可聊分治法 O(n log n)。
- 时间 O(n) / 空间 O(1)。

### 7. 合并区间（LeetCode 56 | 🟡 中等）

**简单思路**：按起点排序后，重叠判断退化为"当前起点 ≤ 上一区间终点"，是就扩张终点，否则直接加入。

**对应解法**（取自 day3）：

```java
class Solution {
    public int[][] merge(int[][] intervals) {
        Arrays.sort(intervals, (p, q) -> p[0] - q[0]);
        List<int[]> ans = new ArrayList<>();
        for (int[] p : intervals) {
            int m = ans.size();
            if (m > 0 && p[0] <= ans.get(m - 1)[1]) {
                ans.get(m - 1)[1] = Math.max(ans.get(m - 1)[1], p[1]);
            } else {
                ans.add(p);
            }
        }
        return ans.toArray(new int[ans.size()][]);
    }
}
```

- ✅ 面试最优解：排序 + 贪心合并 O(n log n)/O(n)。
- 时间 O(n log n) / 空间 O(n)。

### 8. 轮转数组（LeetCode 189 | 🟡 中等）

**简单思路**：向右轮转 k 位 = 后 k 个挪到前面。三次翻转（全翻→前 k 翻→后 n-k 翻）原地完成；先 `k %= n`。

**对应解法**（取自 day3）：

```java
class Solution {
    public void rotate(int[] nums, int k) {
        int n = nums.length;
        k = k % n;
        reverse(nums, 0, n - 1);
        reverse(nums, 0, k - 1);
        reverse(nums, k, n - 1);
    }
    private void reverse(int[] nums, int i, int j) {
        while (i < j) {
            int temp = nums[i]; nums[i++] = nums[j]; nums[j--] = temp;
        }
    }
}
```

- ✅ 面试最优解：三次翻转 O(n)/O(1)。优于开新数组 O(n) 空间。
- 时间 O(n) / 空间 O(1)。

### 9. 除自身以外数组的乘积（LeetCode 238 | 🟡 中等）

**简单思路**：`ans[i] = 左积[i] × 右积[i]`。先算后缀积存入结果数组，再从左用一个变量滚动前缀积乘进去，做到 O(1) 额外空间、且不用除法。

**对应解法**（取自 day3，O(1) 空间版）：

```java
class Solution {
    public int[] productExceptSelf(int[] nums) {
        int n = nums.length;
        int[] suf = new int[n];
        suf[n - 1] = 1;
        for (int i = n - 2; i >= 0; i--) suf[i] = suf[i + 1] * nums[i + 1];
        int pre = 1;
        for (int i = 0; i < n; i++) {
            suf[i] *= pre;
            pre *= nums[i];
        }
        return suf;
    }
}
```

- ✅ 面试最优解：前缀积 × 后缀积 + 滚动变量，**O(1) 额外空间**版（进阶要求）。朴素双数组版是 O(n) 空间，作对比。
- 时间 O(n) / 空间 O(1)（不计输出）。

### 10. 缺失的第一个正数（LeetCode 41 | 🔴 困难）

**简单思路**：把值 `k∈[1,n]` 的元素"换座位"到下标 `k-1`，使数组本身当哈希表；再扫第一个 `nums[t]!=t+1` 即答案。

**对应解法**（取自 day3）：

```java
class Solution {
    public int firstMissingPositive(int[] nums) {
        int n = nums.length;
        for (int i = 0; i < n; i++) {
            while (nums[i] >= 1 && nums[i] <= n
                   && nums[nums[i] - 1] != nums[i]) {
                int j = nums[i] - 1;
                int temp = nums[i]; nums[i] = nums[j]; nums[j] = temp;
            }
        }
        for (int t = 0; t < n; t++) {
            if (nums[t] != t + 1) return t + 1;
        }
        return n + 1;
    }
}
```

- ✅ 面试最优解：原地哈希（cyclic sort）O(n)/O(1)，满足题目 O(n)+常数空间要求。
- 时间 O(n) / 空间 O(1)。

---

*整理自 `day1-数组代码随想录-part1.md`、`day2-数组代码随想录-part2.md`、`day3-数组hot100.md` · 汇总日期：2026-08-17*
