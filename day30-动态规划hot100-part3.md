# Day 30 — 动态规划 Hot100（Part 3）

## 题目：最长递增子序列（Longest Increasing Subsequence）

**LeetCode 300 | 动态规划 Hot100 | 难度：🟡 中等**

### 题目描述

给你一个整数数组 `nums`，找到其中最长严格递增子序列的长度。

**子序列**是由数组派生而来的序列，删除（或不删除）数组中的元素而不改变其余元素的顺序。例如，`[3,6,2,7]` 是数组 `[0,3,1,6,2,2,7]` 的子序列。

### 示例

```
输入：nums = [10,9,2,5,3,7,101,18]
输出：4
解释：最长递增子序列是 [2,3,7,101]，因此长度为 4

输入：nums = [0,1,0,3,2,3]
输出：4
解释：[0,1,2,3] 或 [0,1,3] 等

输入：nums = [7,7,7,7,7,7,7]
输出：1（严格递增，相等的元素不能连续选）
```

---

## 解法：记忆化搜索（以 i 结尾的最长递增子序列）

### 思路

核心定义：**`dfs(i)` = 以 `nums[i]` 结尾的最长递增子序列的长度**。

```
以 nums[i] 结尾的递增子序列，倒数第二个元素 j 必须满足：
  j < i 且 nums[j] < nums[i]

递推公式：
  dfs(i) = 1 + max(dfs(j))  （对所有 j < i 且 nums[j] < nums[i]）
  如果没有满足条件的 j → dfs(i) = 1（只有自己）

最终答案 = max(dfs(0), dfs(1), ..., dfs(n-1))
```

**为什么要求 max 而不是固定从 dfs(n-1) 开始？** 最长递增子序列的结尾可以是任意位置——以某个 `nums[i]` 结尾的序列长度最大，整个答案就是那个最大值，所以要对每个 i 求一次 dfs 再取 max。

### 思考方式图解

```
nums = [10, 9, 2, 5, 3, 7, 101, 18]

dfs(0): 没有 j < 0 → 1                    （[10]）
dfs(1): nums[j] < 9？10 > 9 → 无 → 1      （[9]）
dfs(2): 无更小 → 1                        （[2]）
dfs(3): j=2: 2<5 → dfs(2)+1=2 → 2         （[2,5]）
dfs(4): j=2: 2<3 → dfs(2)+1=2 → 2         （[2,3]）
dfs(5): j=2: 2<7 → 2; j=3: 5<7 → dfs(3)+1=3; j=4: 3<7 → dfs(4)+1=3
        → 3                               （[2,3,7] 或 [2,5,7]）
dfs(6): j=5: 7<101 → dfs(5)+1=4 → 4       （[2,3,7,101]）
dfs(7): j=5: 7<18 → dfs(5)+1=4 → 4        （[2,3,7,18]）

答案 = max(1,1,1,2,2,3,4,4) = 4 ✅
```

### 代码实现

```java
class Solution {
    public int lengthOfLIS(int[] nums) {
        int n = nums.length;
        int[] memo = new int[n];
        int ans = 0;

        // 最长递增子序列的结尾可以是任意位置 → 对每个 i 求 dfs 取 max
        for (int i = 0; i < n; i++) {
            ans = Math.max(ans, dfs(i, nums, memo));
        }
        return ans;
    }

    // dfs(i) = 以 nums[i] 结尾的最长递增子序列长度
    private int dfs(int i, int[] nums, int[] memo) {
        if (memo[i] > 0) {        // 已算过（长度至少为 1，>0 即已算）
            return memo[i];
        }

        int res = 0;
        for (int j = 0; j < i; j++) {
            if (nums[j] < nums[i]) {   // 只有更小的数才能接在前面
                res = Math.max(res, dfs(j, nums, memo));
            }
        }
        res++;                   // 加上 nums[i] 自己
        return memo[i] = res;
    }
}
```

### 复杂度分析

| 维度 | 结果 |
|------|------|
| ⏱ 时间复杂度 | **O(n²)** — 每个 i 枚举所有 j < i |
| 🧠 空间复杂度 | **O(n)** — memo 数组 + 递归栈 |

> **优化**：贪心 + 二分（patience sorting）可以做到 **O(n log n)**——用 `tails[]` 维护各长度递增子序列的最小末尾，二分替换。后续可补充。

---

## 小总结

| 要点 | 说明 |
|------|------|
| 算法名称 | 记忆化搜索（以 i 结尾） |
| 算法类型 | 动态规划 |
| 核心技巧 | **`dfs(i) = 1 + max(dfs(j))`，其中 `j < i` 且 `nums[j] < nums[i]`** |
| 为什么要 max 循环 | 最长递增子序列的结尾不固定，要对每个 i 求 dfs 取 max |
| memo 初始为 0 | 长度至少为 1，`memo[i] > 0` 即表示已算过 |
| 严格递增 | 用 `nums[j] < nums[i]`（不是 ≤），相等元素不能连续选 |
| 优化方向 | 贪心 + 二分 → O(n log n) |

### 一句话记住

> **「以 i 结尾，往前找更小的接——1 + max(dfs(j))，全局取最大。」**

---

## 解法二：递推 DP（自底向上，标准写法）

### 思路

把记忆化的自顶向下改成**自底向上的递推**——`f[i]` 表示以 `nums[i]` 结尾的最长递增子序列长度，从前往后依次计算：

```
f[i] = 1 + max(f[j])   对所有 j < i 且 nums[j] < nums[i]
如果没有满足条件的 j → f[i] = 1

计算顺序：从小到大 i（因为 f[i] 依赖更小的 f[j]）
```

**和记忆化的关系**：同一个递推式，方向相反——记忆化从 i 递归到 0，递推从 0 算到 i。递推版不需要递归栈，代码更紧凑。

### 思考方式图解

```
nums = [10, 9, 2, 5, 3, 7, 101, 18]

f[0]: 无更小 → f[0]=1               （[10]）
f[1]: 10>9 → 无更小 → f[1]=1        （[9]）
f[2]: 无更小 → f[2]=1               （[2]）
f[3]: j=2: 2<5 → f[2]=1 → f[3]=2    （[2,5]）
f[4]: j=2: 2<3 → f[2]=1 → f[4]=2    （[2,3]）
f[5]: j=2: f[2]=1; j=3: f[3]=2; j=4: f[4]=2 → max=2 → f[5]=3  （[2,3,7]）
f[6]: j=5: f[5]=3 → f[6]=4          （[2,3,7,101]）
f[7]: j=5: f[5]=3 → f[7]=4          （[2,3,7,18]）

答案 = max(f) = 4 ✅
```

### 代码实现

```java
class Solution {
    public int lengthOfLIS(int[] nums) {
        int n = nums.length, ans = 0;
        int[] f = new int[n];

        for (int i = 0; i < n; i++) {
            // 找前面所有比 nums[i] 小的数中，最大的 f[j]
            for (int j = 0; j < i; j++) {
                if (nums[j] < nums[i]) {
                    f[i] = Math.max(f[i], f[j]);
                }
            }
            ans = Math.max(ans, ++f[i]);   // f[i]+1（加上自己），并更新答案
        }

        return ans;
    }
}
```

### 复杂度分析

| 维度 | 结果 |
|------|------|
| ⏱ 时间复杂度 | **O(n²)** — 双层循环 |
| 🧠 空间复杂度 | **O(n)** — f 数组 |

---

## 小总结（第二版）

| 要点 | 说明 |
|------|------|
| 算法名称 | 递推 DP（自底向上） |
| 算法类型 | 动态规划 |
| 核心技巧 | **`f[i] = 1 + max(f[j])`（`j < i` 且 `nums[j] < nums[i]`），从 0 到 n-1 顺序计算** |
| 与记忆化的区别 | 同一递推式，方向相反；递推无递归栈、代码更紧凑 |
| 双写法对比 | 记忆化（自顶向下，`memo[i]` 懒计算）/ 递推（自底向上，`f[i]` 提前算） |
| 优化方向 | 贪心 + 二分 → O(n log n) |

### 一句话记住

> **「f[i] 从前往后算，找前面更小里最大——加一取答案。」**

---

*练习日期：2026-08-12*
