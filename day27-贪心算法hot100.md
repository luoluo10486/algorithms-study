# Day 27 — 贪心算法 Hot100

## 题目：买卖股票的最佳时机（Best Time to Buy and Sell Stock）

**LeetCode 121 | 贪心算法 Hot100 | 难度：🟢 简单**

### 题目描述

给定一个数组 `prices`，它的第 `i` 个元素 `prices[i]` 表示一支给定股票第 `i` 天的价格。

你只能选择**某一天**买入这只股票，并选择在**未来的某一个不同的日子**卖出该股票。设计一个算法来计算你所能获取的最大利润。

返回你可以从这笔交易中获取的最大利润。如果你不能获取任何利润，返回 `0`。

### 示例

```
输入：[7,1,5,3,6,4]
输出：5
解释：在第 2 天（价格 1）买入，在第 5 天（价格 6）卖出，利润 6-1=5

输入：[7,6,4,3,1]
输出：0
解释：无法获取利润（每天价格都在跌），返回 0
```

---

## 解法：一次遍历（贪心）

### 思路

核心观察：**最大利润 = 某一天卖出价 - 之前某一天的最低买入价**。

遍历过程中维护两个变量：

```
minPrice：截止当前天的最低价格（潜在的最佳买入点）
ans：当前能获得的最大利润

遍历每一天的价格 p：
  ans = max(ans, p - minPrice)   ← 如果今天卖出，能赚多少？
  minPrice = min(minPrice, p)    ← 更新到今天为止的最低价
```

**为什么是贪心？** 每一步都只关心"今天卖能赚多少"和"到今天为止的最低买入价"，不做任何回退——局部最优选择累积成全局最优。

### 思考方式图解

```
prices = [7, 1, 5, 3, 6, 4]

遍历过程：
  p=7:  ans=max(0, 7-7)=0     minPrice=min(7,7)=7
  p=1:  ans=max(0, 1-7)=0     minPrice=min(7,1)=1   ← 发现更低价 1
  p=5:  ans=max(0, 5-1)=4     minPrice=min(1,5)=1   ← 利润 4
  p=3:  ans=max(4, 3-1)=4     minPrice=min(1,3)=1
  p=6:  ans=max(4, 6-1)=5     minPrice=min(1,6)=1   ← 利润 5（第2天买第5天卖）
  p=4:  ans=max(5, 4-1)=5     minPrice=min(1,4)=1

结果：5 ✅
```

### 代码实现

```java
class Solution {
    public int maxProfit(int[] prices) {
        int ans = 0;
        int minPrice = prices[0];   // 截止当前天的最低买入价

        for (int p : prices) {
            ans = Math.max(ans, p - minPrice);   // 今天卖出能赚多少
            minPrice = Math.min(minPrice, p);    // 更新最低买入价
        }

        return ans;
    }
}
```

### 复杂度分析

| 维度 | 结果 |
|------|------|
| ⏱ 时间复杂度 | **O(n)** — 一次遍历 |
| 🧠 空间复杂度 | **O(1)** — 仅两个变量 |

---

## 小总结

| 要点 | 说明 |
|------|------|
| 算法名称 | 一次遍历（贪心） |
| 算法类型 | 贪心、数组 |
| 核心技巧 | **维护历史最低买入价 minPrice，每天计算"今天卖 - 历史最低"的利润取最大** |
| 为什么不用双循环 | O(n²) 枚举买卖日太慢，贪心 O(n) 一次搞定 |
| 买在最低点之前？ | 不会——`minPrice` 只记录**之前**的最低价，保证"先买后卖"顺序 |
| 关联题目 | [122. 买卖股票的最佳时机 II](https://leetcode.cn/problems/buy-and-sell-stock-ii/)——可以多次交易，累加每天正利润 |

### 一句话记住

> **「记住历史最低价，每天算算能赚多少——取最大就是答案。」**

---

## 题目：跳跃游戏（Jump Game）

**LeetCode 55 | 贪心算法 Hot100 | 难度：🟡 中等**

### 题目描述

给你一个非负整数数组 `nums`，你最初位于数组的**第一个下标**。数组中的每个元素代表你在该位置可以跳跃的最大长度。

判断你是否能够到达最后一个下标。

### 示例

```
输入：nums = [2,3,1,1,4]
输出：true
解释：可以先跳 1 步到下标 1，再跳 3 步到达最后一个下标

输入：nums = [3,2,1,0,4]
输出：false
解释：无论怎样，总会到达下标 3 的位置，但该位置的最大跳跃长度是 0，永远无法到达最后一个下标
```

---

## 解法：贪心（维护最远可达距离）

### 思路

核心思想：**不关心具体怎么跳，只维护一个"最远能到哪"的变量 `mx`**。

```
遍历每个下标 i：
  如果 i > mx → 当前位置都到不了 → 返回 false
  否则更新 mx = max(mx, i + nums[i])  ← 从 i 出发最远能到 i+nums[i]
遍历完都没返回 false → 能到达最后 → true
```

**为什么 `i > mx` 就返回 false？** `mx` 是所有已访问位置能到达的最远下标。如果当前下标 `i` 已经超过了 `mx`，说明没有任何路径能走到 `i`，更不可能走到终点。

### 思考方式图解

```
nums = [2,3,1,1,4]

i=0: i=0 ≤ mx=0 → mx = max(0, 0+2) = 2
i=1: i=1 ≤ mx=2 → mx = max(2, 1+3) = 4
i=2: i=2 ≤ mx=4 → mx = max(4, 2+1) = 4
i=3: i=3 ≤ mx=4 → mx = max(4, 3+1) = 4
i=4: i=4 ≤ mx=4 → mx = max(4, 4+4) = 8
遍历完 → true ✅

nums = [3,2,1,0,4]

i=0: i=0 ≤ mx=0 → mx = max(0, 0+3) = 3
i=1: i=1 ≤ mx=3 → mx = max(3, 1+2) = 3
i=2: i=2 ≤ mx=3 → mx = max(3, 2+1) = 3
i=3: i=3 ≤ mx=3 → mx = max(3, 3+0) = 3
i=4: i=4 > mx=3 → 到不了 → false ❌
```

### 代码实现

```java
class Solution {
    public boolean canJump(int[] nums) {
        int mx = 0;   // 最远能到达的下标

        for (int i = 0; i < nums.length; i++) {
            if (i > mx) {               // 当前位置都到不了
                return false;
            }
            mx = Math.max(mx, i + nums[i]);  // 更新最远可达距离
        }

        return true;
    }
}
```

### 复杂度分析

| 维度 | 结果 |
|------|------|
| ⏱ 时间复杂度 | **O(n)** — 一次遍历 |
| 🧠 空间复杂度 | **O(1)** — 仅一个变量 |

---

## 小总结

| 要点 | 说明 |
|------|------|
| 算法名称 | 贪心（最远可达距离） |
| 算法类型 | 贪心、数组 |
| 核心技巧 | **维护 `mx` 最远可达下标；`i > mx` 即无法到达；否则更新 `mx = max(mx, i+nums[i])`** |
| 为什么 i > mx 就失败 | mx 是已遍历位置能到的极限，超过它说明存在无法跨越的"断点" |
| 关联题目 | [45. 跳跃游戏 II](https://leetcode.cn/problems/jump-game-ii/)——保证能到达，求最少跳跃次数 |

### 一句话记住

> **「维护最远可达 mx，当前位置够不着就失败——一步到位贪心。」**

---

## 题目：跳跃游戏 II（Jump Game II）

**LeetCode 45 | 贪心算法 Hot100 | 难度：🟡 中等**

### 题目描述

给定一个长度为 `n` 的 0 索引整数数组 `nums`。初始位置为 `nums[0]`。

每个元素 `nums[i]` 表示从下标 `i` 向前跳转的最大长度。换句话说，如果你在 `nums[i]` 处，你可以跳转到任意 `nums[i + j]` 处：

- `0 <= j <= nums[i]`
- `i + j < n`

返回到达 `nums[n - 1]` 的**最小跳跃次数**。生成的测试用例可以到达 `nums[n - 1]`。

### 示例

```
输入：nums = [2,3,1,1,4]
输出：2
解释：跳到下标 1，再跳 3 步到达终点，共 2 次跳跃

输入：nums = [2,3,0,1,4]
输出：2
```

---

## 解法：贪心（BFS 按层跳跃）

### 思路

和 55 题一样维护最远可达距离，但需要**统计跳跃次数**。关键是用两个变量把"当前这一跳能到的边界"和"下一跳能到的边界"分开：

```
curEnd：当前这一跳能到达的最远下标（本层边界）
nextEnd：从 [0..curEnd] 中任意位置出发，下一跳能到达的最远下标

遍历每个位置 i：
  更新 nextEnd = max(nextEnd, i + nums[i])  ← 从 i 出发最远能到哪
  如果 i == curEnd：                        ← 走到了本层边界，必须再跳一次
    ans++                                   ← 跳跃次数 +1
    curEnd = nextEnd                        ← 进入下一层
```

**为什么 `i == curEnd` 时跳一次？** 在到达 `curEnd` 之前，我们一直在"当前这一跳"的覆盖范围内移动，不需要额外跳跃；一旦走到边界，说明必须再跳一次才能继续前进——此时 `nextEnd` 已经记录了从整个 [0..curEnd] 范围出发的最远可达点。

### 思考方式图解

```
nums = [2,3,1,1,4]

ans=0, curEnd=0, nextEnd=0

i=0: nextEnd=max(0, 0+2)=2
     i==curEnd(0) → ans=1, curEnd=2    ← 第 1 跳最远到下标 2
i=1: nextEnd=max(2, 1+3)=4
i=2: nextEnd=max(4, 2+1)=4
     i==curEnd(2) → ans=2, curEnd=4    ← 第 2 跳最远到下标 4
i=3: nextEnd=max(4, 3+1)=4

返回 ans=2 ✅（循环只到 nums.length-2 = 3）
```

### 代码实现

```java
class Solution {
    public int jump(int[] nums) {
        int ans = 0;
        int curEnd = 0;    // 当前这一跳能到达的最远下标
        int nextEnd = 0;   // 下一跳能到达的最远下标

        // 不需要遍历最后一个位置（到了终点就不用跳了）
        for (int i = 0; i < nums.length - 1; i++) {
            nextEnd = Math.max(nextEnd, i + nums[i]);  // 从 i 出发最远能到哪

            if (i == curEnd) {      // 走到了当前这一跳的边界
                ans++;              // 必须再跳一次
                curEnd = nextEnd;   // 更新边界
            }
        }

        return ans;
    }
}
```

### 复杂度分析

| 维度 | 结果 |
|------|------|
| ⏱ 时间复杂度 | **O(n)** — 一次遍历 |
| 🧠 空间复杂度 | **O(1)** — 仅两个变量 |

---

## 小总结

| 要点 | 说明 |
|------|------|
| 算法名称 | 贪心（BFS 按层跳跃） |
| 算法类型 | 贪心、数组 |
| 核心技巧 | **`curEnd` 当前跳边界 + `nextEnd` 下一跳边界；`i == curEnd` 时跳跃次数 +1** |
| 与 55 的区别 | 55 只问能否到达；45 保证能到达，求**最少跳跃次数** |
| 为什么遍历到 n-2 | 最后一个位置不用跳，到达即结束 |
| 本质是 BFS | 每一跳 = BFS 的一层，curEnd 是层边界，跳跃次数 = 层数 |

### 一句话记住

> **「curEnd 管本层，nextEnd 管下层——走到边界就跳一次。」**

---

*练习日期：2026-08-07*
