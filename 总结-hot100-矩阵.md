# 总结 — 矩阵 Hot100

> 覆盖 LeetCode 73 / 54 / 48 / 240，解法代码取自 `day19-矩阵hot100.md`，只保留「简单思路 + 解法代码 + 面试最优解标注 + 复杂度」。

---

## 一、解题模型总述

矩阵 Hot100 套路围绕「**原地操作**」与「**利用行列单调性**」两条主线。横向规律四条口诀：

1. **标记与执行分离**（73 矩阵置零）：不能边遍历边改，否则新 0 误当原 0 造成错误传播。先记哪些行/列需置 0，再统一执行；O(1) 空间可用首行首列代替标记数组。
2. **模拟题靠「边界 / 方向控制」**（54 螺旋矩阵）：螺旋本质是四方向循环走，用方向数组 + 动态收缩步数（每走完两组方向行列步数各减 1）保证不重不漏；也可「左闭右开」逐圈模拟（见 day2 螺旋矩阵 II）。
3. **原地旋转靠「对称变换」**（48 旋转图像）：顺时针 90° = 先沿主对角线转置 + 再反转每行。所有坐标变换题优先找对称规律，避免新建矩阵。
4. **利用行列双有序做「Z 字形」**（240 搜索二维矩阵 II）：从右上角（或左下角）出发，左移减小、下移增大，把矩阵当二叉搜索树，O(m+n) 走完。

---

## 二、速览表（面试最优解 + 复杂度）

| 题号 | 题目 | 面试最优解 | 时间 | 空间 |
|------|------|-----------|------|------|
| 73 | 矩阵置零 | 标记数组（O(1) 首行首列优化备选） | O(mn) | O(m+n) |
| 54 | 螺旋矩阵 | 方向数组 + 收缩步数 | O(mn) | O(1) |
| 48 | 旋转图像 | 转置 + 行反转 | O(n²) | O(1) |
| 240 | 搜索二维矩阵 II | 右上角 Z 字形查找 | O(m+n) | O(1) |

---

## 三、各题解法详情

### 1. 矩阵置零（LeetCode 73 | 🟡 中等）
**简单思路**：第一遍只标记哪些行/列含 0（布尔数组），第二遍统一置 0——标记与执行分离，避免新 0 污染。

**解法一 · 标记数组法**（取自 day19）：
```java
class Solution {
    public void setZeroes(int[][] matrix) {
        int m = matrix.length, n = matrix[0].length;
        boolean[] rowZero = new boolean[m];
        boolean[] colZero = new boolean[n];
        for (int i = 0; i < m; i++)
            for (int j = 0; j < n; j++)
                if (matrix[i][j] == 0) { rowZero[i] = true; colZero[j] = true; }
        for (int i = 0; i < m; i++)
            for (int j = 0; j < n; j++)
                if (rowZero[i] || colZero[j]) matrix[i][j] = 0;
    }
}
```
- 时间 O(mn) / 空间 O(m+n)。

**解法二 · O(1) 空间优化（源文档仅提及，未附代码）**：用矩阵第一行/第一列代替标记数组，另用两个变量记录首行首列本身是否需置 0，实现空间 O(1)。

- ✅ 面试最优解：标记数组法最易写对；若面试官要求 O(1) 空间，用首行首列优化版。

---

### 2. 螺旋矩阵（LeetCode 54 | 🟡 中等）
**简单思路**：方向数组 `DIRS={右,下,左,上}` 控制转向；每走完两个方向（右→下 或 左→上）行列步数各减 1。初始 `j=-1` 让第一步向右落到 `j=0`，`ans.size()<size` 控制终止。

**对应解法**（取自 day19）：
```java
class Solution {
    private static final int[][] DIRS = {{0,1},{1,0},{0,-1},{-1,0}};
    public List<Integer> spiralOrder(int[][] matrix) {
        int m = matrix.length, n = matrix[0].length;
        int size = m * n;
        List<Integer> ans = new ArrayList<>(size);
        int i = 0, j = -1;
        for (int di = 0; ans.size() < size; di = (di + 1) % 4) {
            for (int k = 0; k < n; k++) {
                i += DIRS[di][0];
                j += DIRS[di][1];
                ans.add(matrix[i][j]);
            }
            int temp = n; n = m - 1; m = temp;   // 收缩步数
        }
        return ans;
    }
}
```
- ✅ 面试最优解：方向数组 + 收缩步数，O(1) 空间，面试首选。也可「左闭右开」逐圈模拟（同 day2 螺旋矩阵 II）。
- 时间 O(mn) / 空间 O(1)。

---

### 3. 旋转图像（LeetCode 48 | 🟡 中等）
**简单思路**：顺时针 90° = 先沿主对角线转置 + 再反转每行（同一外层循环里完成）。只遍历上三角 `j>i` 避免重复交换。

**对应解法**（取自 day19）：
```java
class Solution {
    public void rotate(int[][] matrix) {
        int n = matrix.length;
        for (int i = 0; i < n; i++) {
            int[] row = matrix[i];
            for (int j = i + 1; j < n; j++) {       // Step1 转置
                int temp = row[j];
                row[j] = matrix[j][i];
                matrix[j][i] = temp;
            }
            for (int j = 0; j < n / 2; j++) {        // Step2 反转行
                int temp = row[j];
                row[j] = matrix[i][n - j - 1];
                matrix[i][n - j - 1] = temp;
            }
        }
    }
}
```
- ✅ 面试最优解：转置 + 行反转，原地 O(1)，面试首选。逆时针 90° 则「转置 + 列反转」。易错点：`j>i` 只走上三角。
- 时间 O(n²) / 空间 O(1)。

---

### 4. 搜索二维矩阵 II（LeetCode 240 | 🟡 中等）
**简单思路**：矩阵每行每列升序。从右上角出发——左移值变小、下移值变大，把矩阵当二叉搜索树遍历。`matrix[i][j]==target` 找到，`<target` 下移，`>target` 左移。也可从左下角出发（上移小 / 右移大）。

**对应解法**（取自 day19）：
```java
class Solution {
    public boolean searchMatrix(int[][] matrix, int target) {
        int i = 0;                                   // 第一行
        int j = matrix[0].length - 1;                // 最后一列（右上角）
        while (i < matrix.length && j >= 0) {
            if (matrix[i][j] == target) return true;
            else if (matrix[i][j] < target) i++;     // 下移找更大
            else j--;                                // 左移找更小
        }
        return false;
    }
}
```
- ✅ 面试最优解：右上角 Z 字形查找，O(1) 空间，面试首选。不能从左上/右下出发（只有一个可走方向，无法分支）。比逐行二分更直观。
- 时间 O(m+n) / 空间 O(1)。

---

## 四、模型缺口与提醒（基于现有 day 文档）

- **73 矩阵置零**：O(1) 空间优化（首行首列法）仅文字提及未附代码，面试如需可补。
- 矩阵 Hot100 还有「单词搜索（79，回溯）、解数独（37，回溯）、game of life（289）」等，源文档未收录，待用户后续补充。

*整理自 day19-矩阵hot100.md · 汇总日期：2026-08-17*
